import {Component} from "@angular/core";

@Component({
  selector: "kpn-link-authenticate",
  template: `
    <a routerLink="/authenticate">Authenticate</a>
  `
})
export class LinkAuthenticateComponent {
}
