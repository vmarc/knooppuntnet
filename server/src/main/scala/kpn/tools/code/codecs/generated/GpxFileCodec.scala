// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.gpx.GpxFile
import kpn.core.gpx.GpxSegment
import kpn.core.gpx.WayPoint
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class GpxFileCodec(registry: CodecRegistry) extends Codec[GpxFile] {

  private val gpxSegmentCodec = registry.get(classOf[GpxSegment])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val wayPointCodec = registry.get(classOf[WayPoint])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): GpxFile = {
    bsonReader.readStartDocument()

    var _id: Long = 0
    var networkId: Long = 0
    var name: String = null
    var wayPoints: Seq[WayPoint] = null
    var trackSegments: Seq[GpxSegment] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "networkId") {
        networkId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "wayPoints") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[WayPoint]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += wayPointCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        wayPoints = valueBuffer.toSeq
      }
      else if (fieldName == "trackSegments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[GpxSegment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += gpxSegmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        trackSegments = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in GpxFileCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    GpxFile(
      _id,
      networkId,
      name,
      wayPoints,
      trackSegments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: GpxFile, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    longCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("networkId")
    longCodec.encode(bsonWriter, value.networkId, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("wayPoints")
    bsonWriter.writeStartArray()
    value.wayPoints.foreach(v => wayPointCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("trackSegments")
    bsonWriter.writeStartArray()
    value.trackSegments.foreach(v => gpxSegmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[GpxFile] = {
    classOf[GpxFile]
  }
}
