// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorRouteSubRelation
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteSubRelationCodec(registry: CodecRegistry) extends Codec[MonitorRouteSubRelation] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteSubRelation = {
    bsonReader.readStartDocument()

    var subRelationIndex: Option[Long] = None
    var relationId: Long = 0
    var name: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "subRelationIndex") {
        subRelationIndex = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relationId") {
        relationId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteSubRelationCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteSubRelation(
      subRelationIndex,
      relationId,
      name,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteSubRelation, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.subRelationIndex.isDefined) {
      bsonWriter.writeName("subRelationIndex")
      longCodec.encode(bsonWriter, value.subRelationIndex.get, encoderContext)
    }

    bsonWriter.writeName("relationId")
    longCodec.encode(bsonWriter, value.relationId, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteSubRelation] = {
    classOf[MonitorRouteSubRelation]
  }
}
