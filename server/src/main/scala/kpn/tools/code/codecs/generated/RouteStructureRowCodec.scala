// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.data.MemberType
import kpn.api.common.route.Link
import kpn.api.common.route.RouteStructureRelation
import kpn.api.common.route.RouteStructureRow
import kpn.api.common.route.RouteStructureWay
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteStructureRowCodec(registry: CodecRegistry) extends Codec[RouteStructureRow] {

  private val linkCodec = registry.get(classOf[Link])
  private val longCodec = registry.get(classOf[Long])
  private val memberTypeCodec = registry.get(classOf[MemberType])
  private val routeStructureRelationCodec = registry.get(classOf[RouteStructureRelation])
  private val routeStructureWayCodec = registry.get(classOf[RouteStructureWay])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteStructureRow = {
    bsonReader.readStartDocument()

    var rowNumber: String = null
    var level: Long = 0
    var id: Long = 0
    var memberType: MemberType = null
    var role: Option[String] = None
    var link: Option[Link] = None
    var distance: Long = 0
    var name: Option[String] = None
    var poi: Option[String] = None
    var way: Option[RouteStructureWay] = None
    var relation: Option[RouteStructureRelation] = None
    var segmentIds: Seq[Long] = null
    var pathIds: Seq[Long] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "rowNumber") {
        rowNumber = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "level") {
        level = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "memberType") {
        memberType = memberTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "role") {
        role = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "link") {
        link = Some(linkCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "distance") {
        distance = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "poi") {
        poi = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "way") {
        way = Some(routeStructureWayCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relation") {
        relation = Some(routeStructureRelationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "segmentIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        segmentIds = valueBuffer.toSeq
      }
      else if (fieldName == "pathIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        pathIds = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in RouteStructureRowCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteStructureRow(
      rowNumber,
      level,
      id,
      memberType,
      role,
      link,
      distance,
      name,
      poi,
      way,
      relation,
      segmentIds,
      pathIds,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteStructureRow, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("rowNumber")
    stringCodec.encode(bsonWriter, value.rowNumber, encoderContext)

    bsonWriter.writeName("level")
    longCodec.encode(bsonWriter, value.level, encoderContext)

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("memberType")
    memberTypeCodec.encode(bsonWriter, value.memberType, encoderContext)

    if (value.role.isDefined) {
      bsonWriter.writeName("role")
      stringCodec.encode(bsonWriter, value.role.get, encoderContext)
    }

    if (value.link.isDefined) {
      bsonWriter.writeName("link")
      linkCodec.encode(bsonWriter, value.link.get, encoderContext)
    }

    bsonWriter.writeName("distance")
    longCodec.encode(bsonWriter, value.distance, encoderContext)

    if (value.name.isDefined) {
      bsonWriter.writeName("name")
      stringCodec.encode(bsonWriter, value.name.get, encoderContext)
    }

    if (value.poi.isDefined) {
      bsonWriter.writeName("poi")
      stringCodec.encode(bsonWriter, value.poi.get, encoderContext)
    }

    if (value.way.isDefined) {
      bsonWriter.writeName("way")
      routeStructureWayCodec.encode(bsonWriter, value.way.get, encoderContext)
    }

    if (value.relation.isDefined) {
      bsonWriter.writeName("relation")
      routeStructureRelationCodec.encode(bsonWriter, value.relation.get, encoderContext)
    }

    bsonWriter.writeName("segmentIds")
    bsonWriter.writeStartArray()
    value.segmentIds.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("pathIds")
    bsonWriter.writeStartArray()
    value.pathIds.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteStructureRow] = {
    classOf[RouteStructureRow]
  }
}
