// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.status.ActionTimestamp
import kpn.core.metrics.MinuteDiffInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MinuteDiffInfoCodec(registry: CodecRegistry) extends Codec[MinuteDiffInfo] {

  private val actionTimestampCodec = registry.get(classOf[ActionTimestamp])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MinuteDiffInfo = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var timestamp: ActionTimestamp = null
    var processed: ActionTimestamp = null
    var delay: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestamp") {
        timestamp = actionTimestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "processed") {
        processed = actionTimestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "delay") {
        delay = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MinuteDiffInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MinuteDiffInfo(
      id,
      timestamp,
      processed,
      delay,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MinuteDiffInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("timestamp")
    actionTimestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    bsonWriter.writeName("processed")
    actionTimestampCodec.encode(bsonWriter, value.processed, encoderContext)

    bsonWriter.writeName("delay")
    longCodec.encode(bsonWriter, value.delay, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MinuteDiffInfo] = {
    classOf[MinuteDiffInfo]
  }
}
