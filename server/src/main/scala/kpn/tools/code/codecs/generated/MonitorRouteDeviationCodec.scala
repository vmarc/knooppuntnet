// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.monitor.MonitorRouteDeviation
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteDeviationCodec(registry: CodecRegistry) extends Codec[MonitorRouteDeviation] {

  private val boundsCodec = registry.get(classOf[Bounds])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteDeviation = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var meters: Long = 0
    var distance: Long = 0
    var bounds: Bounds = null
    var lines: Seq[String] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "distance") {
        distance = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "bounds") {
        bounds = boundsCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lines") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        lines = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in MonitorRouteDeviationCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteDeviation(
      id,
      meters,
      distance,
      bounds,
      lines,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteDeviation, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("distance")
    longCodec.encode(bsonWriter, value.distance, encoderContext)

    bsonWriter.writeName("bounds")
    boundsCodec.encode(bsonWriter, value.bounds, encoderContext)

    bsonWriter.writeName("lines")
    bsonWriter.writeStartArray()
    value.lines.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteDeviation] = {
    classOf[MonitorRouteDeviation]
  }
}
