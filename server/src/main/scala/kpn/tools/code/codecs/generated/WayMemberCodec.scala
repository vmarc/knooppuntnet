// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.data.Way
import kpn.api.common.data.WayMember
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class WayMemberCodec(registry: CodecRegistry) extends Codec[WayMember] {

  private val stringCodec = registry.get(classOf[String])
  private val wayCodec = registry.get(classOf[Way])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): WayMember = {
    bsonReader.readStartDocument()

    var way: Way = null
    var role: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "way") {
        way = wayCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "role") {
        role = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in WayMemberCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    WayMember(
      way,
      role,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: WayMember, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("way")
    wayCodec.encode(bsonWriter, value.way, encoderContext)

    if (value.role.isDefined) {
      bsonWriter.writeName("role")
      stringCodec.encode(bsonWriter, value.role.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[WayMember] = {
    classOf[WayMember]
  }
}
