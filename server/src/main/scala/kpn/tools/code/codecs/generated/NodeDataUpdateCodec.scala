// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.diff.NodeData
import kpn.api.common.diff.NodeDataUpdate
import kpn.api.common.diff.TagDiffs
import kpn.api.common.diff.node.NodeMoved
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeDataUpdateCodec(registry: CodecRegistry) extends Codec[NodeDataUpdate] {

  private val nodeDataCodec = registry.get(classOf[NodeData])
  private val nodeMovedCodec = registry.get(classOf[NodeMoved])
  private val tagDiffsCodec = registry.get(classOf[TagDiffs])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeDataUpdate = {
    bsonReader.readStartDocument()

    var before: NodeData = null
    var after: NodeData = null
    var tagDiffs: Option[TagDiffs] = None
    var nodeMoved: Option[NodeMoved] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "before") {
        before = nodeDataCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "after") {
        after = nodeDataCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "tagDiffs") {
        tagDiffs = Some(tagDiffsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nodeMoved") {
        nodeMoved = Some(nodeMovedCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NodeDataUpdateCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeDataUpdate(
      before,
      after,
      tagDiffs,
      nodeMoved,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeDataUpdate, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("before")
    nodeDataCodec.encode(bsonWriter, value.before, encoderContext)

    bsonWriter.writeName("after")
    nodeDataCodec.encode(bsonWriter, value.after, encoderContext)

    if (value.tagDiffs.isDefined) {
      bsonWriter.writeName("tagDiffs")
      tagDiffsCodec.encode(bsonWriter, value.tagDiffs.get, encoderContext)
    }

    if (value.nodeMoved.isDefined) {
      bsonWriter.writeName("nodeMoved")
      nodeMovedCodec.encode(bsonWriter, value.nodeMoved.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeDataUpdate] = {
    classOf[NodeDataUpdate]
  }
}
