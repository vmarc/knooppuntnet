// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.route.RouteNetworkNodeInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteNetworkNodeInfoCodec(registry: CodecRegistry) extends Codec[RouteNetworkNodeInfo] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteNetworkNodeInfo = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var name: String = null
    var alternateName: String = null
    var longName: Option[String] = None
    var lat: String = null
    var lon: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "alternateName") {
        alternateName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "longName") {
        longName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "lat") {
        lat = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lon") {
        lon = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteNetworkNodeInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteNetworkNodeInfo(
      id,
      name,
      alternateName,
      longName,
      lat,
      lon,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteNetworkNodeInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("alternateName")
    stringCodec.encode(bsonWriter, value.alternateName, encoderContext)

    if (value.longName.isDefined) {
      bsonWriter.writeName("longName")
      stringCodec.encode(bsonWriter, value.longName.get, encoderContext)
    }

    bsonWriter.writeName("lat")
    stringCodec.encode(bsonWriter, value.lat, encoderContext)

    bsonWriter.writeName("lon")
    stringCodec.encode(bsonWriter, value.lon, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteNetworkNodeInfo] = {
    classOf[RouteNetworkNodeInfo]
  }
}
