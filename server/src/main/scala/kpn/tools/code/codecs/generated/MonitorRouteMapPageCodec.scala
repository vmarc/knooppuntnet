// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.monitor.MonitorReferenceInfo
import kpn.api.common.monitor.MonitorReferenceType
import kpn.api.common.monitor.MonitorRouteDeviation
import kpn.api.common.monitor.MonitorRouteMapPage
import kpn.api.common.monitor.MonitorRouteSegment
import kpn.api.common.monitor.MonitorRouteSubRelation
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteMapPageCodec(registry: CodecRegistry) extends Codec[MonitorRouteMapPage] {

  private val boundsCodec = registry.get(classOf[Bounds])
  private val longCodec = registry.get(classOf[Long])
  private val monitorReferenceInfoCodec = registry.get(classOf[MonitorReferenceInfo])
  private val monitorReferenceTypeCodec = registry.get(classOf[MonitorReferenceType])
  private val monitorRouteDeviationCodec = registry.get(classOf[MonitorRouteDeviation])
  private val monitorRouteSegmentCodec = registry.get(classOf[MonitorRouteSegment])
  private val monitorRouteSubRelationCodec = registry.get(classOf[MonitorRouteSubRelation])
  private val stringCodec = registry.get(classOf[String])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteMapPage = {
    bsonReader.readStartDocument()

    var relationId: Option[Long] = None
    var routeName: String = null
    var routeDescription: String = null
    var groupName: String = null
    var groupDescription: String = null
    var referenceType: MonitorReferenceType = null
    var bounds: Option[Bounds] = None
    var analysisTimestamp: Option[Timestamp] = None
    var currentSubRelation: Option[MonitorRouteSubRelation] = None
    var previousSubRelation: Option[MonitorRouteSubRelation] = None
    var nextSubRelation: Option[MonitorRouteSubRelation] = None
    var osmSegments: Seq[MonitorRouteSegment] = null
    var matchesGeoJson: Option[String] = None
    var deviations: Seq[MonitorRouteDeviation] = null
    var reference: Option[MonitorReferenceInfo] = None
    var subRelations: Seq[MonitorRouteSubRelation] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "relationId") {
        relationId = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "routeName") {
        routeName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeDescription") {
        routeDescription = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "groupName") {
        groupName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "groupDescription") {
        groupDescription = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "referenceType") {
        referenceType = monitorReferenceTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "bounds") {
        bounds = Some(boundsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "analysisTimestamp") {
        analysisTimestamp = Some(timestampCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "currentSubRelation") {
        currentSubRelation = Some(monitorRouteSubRelationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "previousSubRelation") {
        previousSubRelation = Some(monitorRouteSubRelationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nextSubRelation") {
        nextSubRelation = Some(monitorRouteSubRelationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "osmSegments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[MonitorRouteSegment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += monitorRouteSegmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        osmSegments = valueBuffer.toSeq
      }
      else if (fieldName == "matchesGeoJson") {
        matchesGeoJson = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "deviations") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[MonitorRouteDeviation]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += monitorRouteDeviationCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        deviations = valueBuffer.toSeq
      }
      else if (fieldName == "reference") {
        reference = Some(monitorReferenceInfoCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "subRelations") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[MonitorRouteSubRelation]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += monitorRouteSubRelationCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        subRelations = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteMapPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteMapPage(
      relationId,
      routeName,
      routeDescription,
      groupName,
      groupDescription,
      referenceType,
      bounds,
      analysisTimestamp,
      currentSubRelation,
      previousSubRelation,
      nextSubRelation,
      osmSegments,
      matchesGeoJson,
      deviations,
      reference,
      subRelations,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteMapPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.relationId.isDefined) {
      bsonWriter.writeName("relationId")
      longCodec.encode(bsonWriter, value.relationId.get, encoderContext)
    }

    bsonWriter.writeName("routeName")
    stringCodec.encode(bsonWriter, value.routeName, encoderContext)

    bsonWriter.writeName("routeDescription")
    stringCodec.encode(bsonWriter, value.routeDescription, encoderContext)

    bsonWriter.writeName("groupName")
    stringCodec.encode(bsonWriter, value.groupName, encoderContext)

    bsonWriter.writeName("groupDescription")
    stringCodec.encode(bsonWriter, value.groupDescription, encoderContext)

    bsonWriter.writeName("referenceType")
    monitorReferenceTypeCodec.encode(bsonWriter, value.referenceType, encoderContext)

    if (value.bounds.isDefined) {
      bsonWriter.writeName("bounds")
      boundsCodec.encode(bsonWriter, value.bounds.get, encoderContext)
    }

    if (value.analysisTimestamp.isDefined) {
      bsonWriter.writeName("analysisTimestamp")
      timestampCodec.encode(bsonWriter, value.analysisTimestamp.get, encoderContext)
    }

    if (value.currentSubRelation.isDefined) {
      bsonWriter.writeName("currentSubRelation")
      monitorRouteSubRelationCodec.encode(bsonWriter, value.currentSubRelation.get, encoderContext)
    }

    if (value.previousSubRelation.isDefined) {
      bsonWriter.writeName("previousSubRelation")
      monitorRouteSubRelationCodec.encode(bsonWriter, value.previousSubRelation.get, encoderContext)
    }

    if (value.nextSubRelation.isDefined) {
      bsonWriter.writeName("nextSubRelation")
      monitorRouteSubRelationCodec.encode(bsonWriter, value.nextSubRelation.get, encoderContext)
    }

    bsonWriter.writeName("osmSegments")
    bsonWriter.writeStartArray()
    value.osmSegments.foreach(v => monitorRouteSegmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.matchesGeoJson.isDefined) {
      bsonWriter.writeName("matchesGeoJson")
      stringCodec.encode(bsonWriter, value.matchesGeoJson.get, encoderContext)
    }

    bsonWriter.writeName("deviations")
    bsonWriter.writeStartArray()
    value.deviations.foreach(v => monitorRouteDeviationCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.reference.isDefined) {
      bsonWriter.writeName("reference")
      monitorReferenceInfoCodec.encode(bsonWriter, value.reference.get, encoderContext)
    }

    bsonWriter.writeName("subRelations")
    bsonWriter.writeStartArray()
    value.subRelations.foreach(v => monitorRouteSubRelationCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteMapPage] = {
    classOf[MonitorRouteMapPage]
  }
}
