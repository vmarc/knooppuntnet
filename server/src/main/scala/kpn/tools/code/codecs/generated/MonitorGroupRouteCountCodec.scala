// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.server.monitor.domain.MonitorGroupRouteCount
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry
import org.bson.types.ObjectId

class MonitorGroupRouteCountCodec(registry: CodecRegistry) extends Codec[MonitorGroupRouteCount] {

  private val longCodec = registry.get(classOf[Long])
  private val objectIdCodec = registry.get(classOf[ObjectId])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorGroupRouteCount = {
    bsonReader.readStartDocument()

    var groupId: ObjectId = null
    var routeCount: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "groupId") {
        groupId = objectIdCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeCount") {
        routeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorGroupRouteCountCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorGroupRouteCount(
      groupId,
      routeCount,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorGroupRouteCount, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("groupId")
    objectIdCodec.encode(bsonWriter, value.groupId, encoderContext)

    bsonWriter.writeName("routeCount")
    longCodec.encode(bsonWriter, value.routeCount, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorGroupRouteCount] = {
    classOf[MonitorGroupRouteCount]
  }
}
