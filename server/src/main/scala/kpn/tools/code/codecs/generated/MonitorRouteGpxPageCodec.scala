// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorRouteGpxPage
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteGpxPageCodec(registry: CodecRegistry) extends Codec[MonitorRouteGpxPage] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteGpxPage = {
    bsonReader.readStartDocument()

    var groupName: String = null
    var routeName: String = null
    var subRelationId: Long = 0
    var subRelationDescription: String = null
    var referenceTimestamp: Timestamp = null
    var referenceFilename: Option[String] = None
    var referenceDistance: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "groupName") {
        groupName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeName") {
        routeName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "subRelationId") {
        subRelationId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "subRelationDescription") {
        subRelationDescription = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "referenceTimestamp") {
        referenceTimestamp = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "referenceFilename") {
        referenceFilename = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "referenceDistance") {
        referenceDistance = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteGpxPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteGpxPage(
      groupName,
      routeName,
      subRelationId,
      subRelationDescription,
      referenceTimestamp,
      referenceFilename,
      referenceDistance,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteGpxPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("groupName")
    stringCodec.encode(bsonWriter, value.groupName, encoderContext)

    bsonWriter.writeName("routeName")
    stringCodec.encode(bsonWriter, value.routeName, encoderContext)

    bsonWriter.writeName("subRelationId")
    longCodec.encode(bsonWriter, value.subRelationId, encoderContext)

    bsonWriter.writeName("subRelationDescription")
    stringCodec.encode(bsonWriter, value.subRelationDescription, encoderContext)

    bsonWriter.writeName("referenceTimestamp")
    timestampCodec.encode(bsonWriter, value.referenceTimestamp, encoderContext)

    if (value.referenceFilename.isDefined) {
      bsonWriter.writeName("referenceFilename")
      stringCodec.encode(bsonWriter, value.referenceFilename.get, encoderContext)
    }

    bsonWriter.writeName("referenceDistance")
    longCodec.encode(bsonWriter, value.referenceDistance, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteGpxPage] = {
    classOf[MonitorRouteGpxPage]
  }
}
