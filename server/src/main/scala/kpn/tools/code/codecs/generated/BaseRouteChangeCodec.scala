// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.ChangeType
import kpn.api.common.changes.details.BaseRouteChange
import kpn.api.common.changes.details.ChangeKey
import kpn.api.common.diff.WayDiffsInfo
import kpn.api.common.route.GeometryDiff
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class BaseRouteChangeCodec(registry: CodecRegistry) extends Codec[BaseRouteChange] {

  private val boundsCodec = registry.get(classOf[Bounds])
  private val changeKeyCodec = registry.get(classOf[ChangeKey])
  private val changeTypeCodec = registry.get(classOf[ChangeType])
  private val geometryDiffCodec = registry.get(classOf[GeometryDiff])
  private val stringCodec = registry.get(classOf[String])
  private val wayDiffsInfoCodec = registry.get(classOf[WayDiffsInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): BaseRouteChange = {
    bsonReader.readStartDocument()

    var _id: String = null
    var key: ChangeKey = null
    var changeType: ChangeType = null
    var wayDiffs: Option[WayDiffsInfo] = None
    var geometryDiff: Option[GeometryDiff] = None
    var bounds: Option[Bounds] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "key") {
        key = changeKeyCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeType") {
        changeType = changeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "wayDiffs") {
        wayDiffs = Some(wayDiffsInfoCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "geometryDiff") {
        geometryDiff = Some(geometryDiffCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "bounds") {
        bounds = Some(boundsCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in BaseRouteChangeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    BaseRouteChange(
      _id,
      key,
      changeType,
      wayDiffs,
      geometryDiff,
      bounds,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: BaseRouteChange, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    stringCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("key")
    changeKeyCodec.encode(bsonWriter, value.key, encoderContext)

    bsonWriter.writeName("changeType")
    changeTypeCodec.encode(bsonWriter, value.changeType, encoderContext)

    if (value.wayDiffs.isDefined) {
      bsonWriter.writeName("wayDiffs")
      wayDiffsInfoCodec.encode(bsonWriter, value.wayDiffs.get, encoderContext)
    }

    if (value.geometryDiff.isDefined) {
      bsonWriter.writeName("geometryDiff")
      geometryDiffCodec.encode(bsonWriter, value.geometryDiff.get, encoderContext)
    }

    if (value.bounds.isDefined) {
      bsonWriter.writeName("bounds")
      boundsCodec.encode(bsonWriter, value.bounds.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[BaseRouteChange] = {
    classOf[BaseRouteChange]
  }
}
