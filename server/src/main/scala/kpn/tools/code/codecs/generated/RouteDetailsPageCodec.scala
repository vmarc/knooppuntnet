// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.route.RouteDetails
import kpn.api.common.route.RouteDetailsPage
import kpn.api.common.route.RouteInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteDetailsPageCodec(registry: CodecRegistry) extends Codec[RouteDetailsPage] {

  private val routeDetailsCodec = registry.get(classOf[RouteDetails])
  private val routeInfoCodec = registry.get(classOf[RouteInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteDetailsPage = {
    bsonReader.readStartDocument()

    var routeInfo: RouteInfo = null
    var details: RouteDetails = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "routeInfo") {
        routeInfo = routeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "details") {
        details = routeDetailsCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteDetailsPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteDetailsPage(
      routeInfo,
      details,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteDetailsPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("routeInfo")
    routeInfoCodec.encode(bsonWriter, value.routeInfo, encoderContext)

    bsonWriter.writeName("details")
    routeDetailsCodec.encode(bsonWriter, value.details, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteDetailsPage] = {
    classOf[RouteDetailsPage]
  }
}
