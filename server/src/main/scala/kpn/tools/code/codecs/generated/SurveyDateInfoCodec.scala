// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.SurveyDateInfo
import kpn.api.custom.Day
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class SurveyDateInfoCodec(registry: CodecRegistry) extends Codec[SurveyDateInfo] {

  private val dayCodec = registry.get(classOf[Day])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): SurveyDateInfo = {
    bsonReader.readStartDocument()

    var now: Day = null
    var lastWeekStart: Day = null
    var lastMonthStart: Day = null
    var lastHalfYearStart: Day = null
    var lastYearStart: Day = null
    var lastTwoYearsStart: Day = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "now") {
        now = dayCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastWeekStart") {
        lastWeekStart = dayCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastMonthStart") {
        lastMonthStart = dayCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastHalfYearStart") {
        lastHalfYearStart = dayCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastYearStart") {
        lastYearStart = dayCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastTwoYearsStart") {
        lastTwoYearsStart = dayCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in SurveyDateInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    SurveyDateInfo(
      now,
      lastWeekStart,
      lastMonthStart,
      lastHalfYearStart,
      lastYearStart,
      lastTwoYearsStart,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: SurveyDateInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("now")
    dayCodec.encode(bsonWriter, value.now, encoderContext)

    bsonWriter.writeName("lastWeekStart")
    dayCodec.encode(bsonWriter, value.lastWeekStart, encoderContext)

    bsonWriter.writeName("lastMonthStart")
    dayCodec.encode(bsonWriter, value.lastMonthStart, encoderContext)

    bsonWriter.writeName("lastHalfYearStart")
    dayCodec.encode(bsonWriter, value.lastHalfYearStart, encoderContext)

    bsonWriter.writeName("lastYearStart")
    dayCodec.encode(bsonWriter, value.lastYearStart, encoderContext)

    bsonWriter.writeName("lastTwoYearsStart")
    dayCodec.encode(bsonWriter, value.lastTwoYearsStart, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[SurveyDateInfo] = {
    classOf[SurveyDateInfo]
  }
}
