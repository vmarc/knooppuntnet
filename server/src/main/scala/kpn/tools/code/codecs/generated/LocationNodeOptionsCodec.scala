// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.filter.ServerFilterGroup
import kpn.api.common.location.LocationNodeOptions
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationNodeOptionsCodec(registry: CodecRegistry) extends Codec[LocationNodeOptions] {

  private val longCodec = registry.get(classOf[Long])
  private val serverFilterGroupCodec = registry.get(classOf[ServerFilterGroup])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationNodeOptions = {
    bsonReader.readStartDocument()

    var integrityCheck: ServerFilterGroup = null
    var integrityCheckFailed: ServerFilterGroup = null
    var fact: ServerFilterGroup = null
    var survey: ServerFilterGroup = null
    var lastUpdated: ServerFilterGroup = null
    var proposed: ServerFilterGroup = null
    var referencedInRoutes: ServerFilterGroup = null
    var totalNodeCount: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "integrityCheck") {
        integrityCheck = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "integrityCheckFailed") {
        integrityCheckFailed = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "fact") {
        fact = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "survey") {
        survey = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastUpdated") {
        lastUpdated = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "proposed") {
        proposed = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "referencedInRoutes") {
        referencedInRoutes = serverFilterGroupCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "totalNodeCount") {
        totalNodeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationNodeOptionsCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationNodeOptions(
      integrityCheck,
      integrityCheckFailed,
      fact,
      survey,
      lastUpdated,
      proposed,
      referencedInRoutes,
      totalNodeCount,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationNodeOptions, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("integrityCheck")
    serverFilterGroupCodec.encode(bsonWriter, value.integrityCheck, encoderContext)

    bsonWriter.writeName("integrityCheckFailed")
    serverFilterGroupCodec.encode(bsonWriter, value.integrityCheckFailed, encoderContext)

    bsonWriter.writeName("fact")
    serverFilterGroupCodec.encode(bsonWriter, value.fact, encoderContext)

    bsonWriter.writeName("survey")
    serverFilterGroupCodec.encode(bsonWriter, value.survey, encoderContext)

    bsonWriter.writeName("lastUpdated")
    serverFilterGroupCodec.encode(bsonWriter, value.lastUpdated, encoderContext)

    bsonWriter.writeName("proposed")
    serverFilterGroupCodec.encode(bsonWriter, value.proposed, encoderContext)

    bsonWriter.writeName("referencedInRoutes")
    serverFilterGroupCodec.encode(bsonWriter, value.referencedInRoutes, encoderContext)

    bsonWriter.writeName("totalNodeCount")
    longCodec.encode(bsonWriter, value.totalNodeCount, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationNodeOptions] = {
    classOf[LocationNodeOptions]
  }
}
