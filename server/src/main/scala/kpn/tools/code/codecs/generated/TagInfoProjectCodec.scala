// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.taginfo.TagInfoProject
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TagInfoProjectCodec(registry: CodecRegistry) extends Codec[TagInfoProject] {

  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TagInfoProject = {
    bsonReader.readStartDocument()

    var name: String = null
    var description: String = null
    var project_url: String = null
    var doc_url: Option[String] = None
    var icon_url: String = null
    var contact_name: String = null
    var contact_email: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "description") {
        description = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "project_url") {
        project_url = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "doc_url") {
        doc_url = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "icon_url") {
        icon_url = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "contact_name") {
        contact_name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "contact_email") {
        contact_email = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in TagInfoProjectCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TagInfoProject(
      name,
      description,
      project_url,
      doc_url,
      icon_url,
      contact_name,
      contact_email,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TagInfoProject, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("description")
    stringCodec.encode(bsonWriter, value.description, encoderContext)

    bsonWriter.writeName("project_url")
    stringCodec.encode(bsonWriter, value.project_url, encoderContext)

    if (value.doc_url.isDefined) {
      bsonWriter.writeName("doc_url")
      stringCodec.encode(bsonWriter, value.doc_url.get, encoderContext)
    }

    bsonWriter.writeName("icon_url")
    stringCodec.encode(bsonWriter, value.icon_url, encoderContext)

    bsonWriter.writeName("contact_name")
    stringCodec.encode(bsonWriter, value.contact_name, encoderContext)

    bsonWriter.writeName("contact_email")
    stringCodec.encode(bsonWriter, value.contact_email, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TagInfoProject] = {
    classOf[TagInfoProject]
  }
}
