// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.doc.BaseRouteSegmentElement
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class BaseRouteSegmentElementCodec(registry: CodecRegistry) extends Codec[BaseRouteSegmentElement] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): BaseRouteSegmentElement = {
    bsonReader.readStartDocument()

    var segmentId: Long = 0
    var segmentElementId: Long = 0
    var surface: String = null
    var memberIndexes: Seq[Long] = null
    var meters: Long = 0
    var coordinates: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "segmentId") {
        segmentId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "segmentElementId") {
        segmentElementId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "surface") {
        surface = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "memberIndexes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        memberIndexes = valueBuffer.toSeq
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "coordinates") {
        coordinates = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in BaseRouteSegmentElementCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    BaseRouteSegmentElement(
      segmentId,
      segmentElementId,
      surface,
      memberIndexes,
      meters,
      coordinates,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: BaseRouteSegmentElement, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("segmentId")
    longCodec.encode(bsonWriter, value.segmentId, encoderContext)

    bsonWriter.writeName("segmentElementId")
    longCodec.encode(bsonWriter, value.segmentElementId, encoderContext)

    bsonWriter.writeName("surface")
    stringCodec.encode(bsonWriter, value.surface, encoderContext)

    bsonWriter.writeName("memberIndexes")
    bsonWriter.writeStartArray()
    value.memberIndexes.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("coordinates")
    stringCodec.encode(bsonWriter, value.coordinates, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[BaseRouteSegmentElement] = {
    classOf[BaseRouteSegmentElement]
  }
}
