// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.RouteScope
import kpn.api.common.RouteType
import kpn.api.custom.ScopedRouteType
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ScopedRouteTypeCodec(registry: CodecRegistry) extends Codec[ScopedRouteType] {

  private val routeScopeCodec = registry.get(classOf[RouteScope])
  private val routeTypeCodec = registry.get(classOf[RouteType])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ScopedRouteType = {
    bsonReader.readStartDocument()

    var routeType: RouteType = null
    var routeScope: RouteScope = null
    var key: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "routeType") {
        routeType = routeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeScope") {
        routeScope = routeScopeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "key") {
        key = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in ScopedRouteTypeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ScopedRouteType(
      routeType,
      routeScope,
      key,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ScopedRouteType, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("routeType")
    routeTypeCodec.encode(bsonWriter, value.routeType, encoderContext)

    bsonWriter.writeName("routeScope")
    routeScopeCodec.encode(bsonWriter, value.routeScope, encoderContext)

    bsonWriter.writeName("key")
    stringCodec.encode(bsonWriter, value.key, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ScopedRouteType] = {
    classOf[ScopedRouteType]
  }
}
