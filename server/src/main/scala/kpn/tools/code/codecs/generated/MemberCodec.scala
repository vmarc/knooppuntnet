// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Relation
import kpn.api.common.data.Member
import kpn.api.common.data.Node
import kpn.api.common.data.Way
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MemberCodec(registry: CodecRegistry) extends Codec[Member] {

  private val longCodec = registry.get(classOf[Long])
  private val nodeCodec = registry.get(classOf[Node])
  private val relationCodec = registry.get(classOf[Relation])
  private val stringCodec = registry.get(classOf[String])
  private val wayCodec = registry.get(classOf[Way])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): Member = {
    bsonReader.readStartDocument()

    var node: Option[Node] = None
    var way: Option[Way] = None
    var relation: Option[Relation] = None
    var relationId: Option[Long] = None
    var role: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "node") {
        node = Some(nodeCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "way") {
        way = Some(wayCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relation") {
        relation = Some(relationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relationId") {
        relationId = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "role") {
        role = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MemberCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    Member(
      node,
      way,
      relation,
      relationId,
      role,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: Member, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.node.isDefined) {
      bsonWriter.writeName("node")
      nodeCodec.encode(bsonWriter, value.node.get, encoderContext)
    }

    if (value.way.isDefined) {
      bsonWriter.writeName("way")
      wayCodec.encode(bsonWriter, value.way.get, encoderContext)
    }

    if (value.relation.isDefined) {
      bsonWriter.writeName("relation")
      relationCodec.encode(bsonWriter, value.relation.get, encoderContext)
    }

    if (value.relationId.isDefined) {
      bsonWriter.writeName("relationId")
      longCodec.encode(bsonWriter, value.relationId.get, encoderContext)
    }

    if (value.role.isDefined) {
      bsonWriter.writeName("role")
      stringCodec.encode(bsonWriter, value.role.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[Member] = {
    classOf[Member]
  }
}
