// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorAction
import kpn.api.common.monitor.MonitorReferenceType
import kpn.api.common.monitor.MonitorRouteUpdate
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteUpdateCodec(registry: CodecRegistry) extends Codec[MonitorRouteUpdate] {

  private val longCodec = registry.get(classOf[Long])
  private val monitorActionCodec = registry.get(classOf[MonitorAction])
  private val monitorReferenceTypeCodec = registry.get(classOf[MonitorReferenceType])
  private val stringCodec = registry.get(classOf[String])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteUpdate = {
    bsonReader.readStartDocument()

    var action: MonitorAction = null
    var groupName: String = null
    var routeName: String = null
    var referenceType: MonitorReferenceType = null
    var description: Option[String] = None
    var comment: Option[String] = None
    var relationId: Option[Long] = None
    var referenceTimestamp: Option[Timestamp] = None
    var referenceFilename: Option[String] = None
    var referenceGpx: Option[String] = None
    var migrationGeojson: Option[String] = None
    var newGroupName: Option[String] = None
    var newRouteName: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "action") {
        action = monitorActionCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "groupName") {
        groupName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeName") {
        routeName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "referenceType") {
        referenceType = monitorReferenceTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "description") {
        description = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "comment") {
        comment = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relationId") {
        relationId = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "referenceTimestamp") {
        referenceTimestamp = Some(timestampCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "referenceFilename") {
        referenceFilename = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "referenceGpx") {
        referenceGpx = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "migrationGeojson") {
        migrationGeojson = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "newGroupName") {
        newGroupName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "newRouteName") {
        newRouteName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteUpdateCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteUpdate(
      action,
      groupName,
      routeName,
      referenceType,
      description,
      comment,
      relationId,
      referenceTimestamp,
      referenceFilename,
      referenceGpx,
      migrationGeojson,
      newGroupName,
      newRouteName,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteUpdate, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("action")
    monitorActionCodec.encode(bsonWriter, value.action, encoderContext)

    bsonWriter.writeName("groupName")
    stringCodec.encode(bsonWriter, value.groupName, encoderContext)

    bsonWriter.writeName("routeName")
    stringCodec.encode(bsonWriter, value.routeName, encoderContext)

    bsonWriter.writeName("referenceType")
    monitorReferenceTypeCodec.encode(bsonWriter, value.referenceType, encoderContext)

    if (value.description.isDefined) {
      bsonWriter.writeName("description")
      stringCodec.encode(bsonWriter, value.description.get, encoderContext)
    }

    if (value.comment.isDefined) {
      bsonWriter.writeName("comment")
      stringCodec.encode(bsonWriter, value.comment.get, encoderContext)
    }

    if (value.relationId.isDefined) {
      bsonWriter.writeName("relationId")
      longCodec.encode(bsonWriter, value.relationId.get, encoderContext)
    }

    if (value.referenceTimestamp.isDefined) {
      bsonWriter.writeName("referenceTimestamp")
      timestampCodec.encode(bsonWriter, value.referenceTimestamp.get, encoderContext)
    }

    if (value.referenceFilename.isDefined) {
      bsonWriter.writeName("referenceFilename")
      stringCodec.encode(bsonWriter, value.referenceFilename.get, encoderContext)
    }

    if (value.referenceGpx.isDefined) {
      bsonWriter.writeName("referenceGpx")
      stringCodec.encode(bsonWriter, value.referenceGpx.get, encoderContext)
    }

    if (value.migrationGeojson.isDefined) {
      bsonWriter.writeName("migrationGeojson")
      stringCodec.encode(bsonWriter, value.migrationGeojson.get, encoderContext)
    }

    if (value.newGroupName.isDefined) {
      bsonWriter.writeName("newGroupName")
      stringCodec.encode(bsonWriter, value.newGroupName.get, encoderContext)
    }

    if (value.newRouteName.isDefined) {
      bsonWriter.writeName("newRouteName")
      stringCodec.encode(bsonWriter, value.newRouteName.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteUpdate] = {
    classOf[MonitorRouteUpdate]
  }
}
