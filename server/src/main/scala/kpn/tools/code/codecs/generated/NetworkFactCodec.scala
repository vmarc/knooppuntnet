// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Check
import kpn.api.common.Fact
import kpn.api.common.NetworkFact
import kpn.api.common.common.Ref
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkFactCodec(registry: CodecRegistry) extends Codec[NetworkFact] {

  private val checkCodec = registry.get(classOf[Check])
  private val factCodec = registry.get(classOf[Fact])
  private val longCodec = registry.get(classOf[Long])
  private val refCodec = registry.get(classOf[Ref])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkFact = {
    bsonReader.readStartDocument()

    var fact: Fact = null
    var elementType: Option[String] = None
    var elementIds: Option[Seq[Long]] = None
    var elements: Option[Seq[Ref]] = None
    var checks: Option[Seq[Check]] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "fact") {
        fact = factCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "elementType") {
        elementType = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "elementIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        elementIds = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "elements") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Ref]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        elements = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "checks") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Check]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += checkCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        checks = Some(valueBuffer.toSeq)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in NetworkFactCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkFact(
      fact,
      elementType,
      elementIds,
      elements,
      checks,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkFact, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("fact")
    factCodec.encode(bsonWriter, value.fact, encoderContext)

    if (value.elementType.isDefined) {
      bsonWriter.writeName("elementType")
      stringCodec.encode(bsonWriter, value.elementType.get, encoderContext)
    }

    if (value.elementIds.isDefined) {
      bsonWriter.writeName("elementIds")
      bsonWriter.writeStartArray()
      value.elementIds.get.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    if (value.elements.isDefined) {
      bsonWriter.writeName("elements")
      bsonWriter.writeStartArray()
      value.elements.get.foreach(v => refCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    if (value.checks.isDefined) {
      bsonWriter.writeName("checks")
      bsonWriter.writeStartArray()
      value.checks.get.foreach(v => checkCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkFact] = {
    classOf[NetworkFact]
  }
}
