// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.diff.TagDiff
import kpn.api.common.diff.TagDiffType
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TagDiffCodec(registry: CodecRegistry) extends Codec[TagDiff] {

  private val stringCodec = registry.get(classOf[String])
  private val tagDiffTypeCodec = registry.get(classOf[TagDiffType])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TagDiff = {
    bsonReader.readStartDocument()

    var action: TagDiffType = null
    var key: String = null
    var valueBefore: Option[String] = None
    var valueAfter: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "action") {
        action = tagDiffTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "key") {
        key = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "valueBefore") {
        valueBefore = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "valueAfter") {
        valueAfter = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in TagDiffCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TagDiff(
      action,
      key,
      valueBefore,
      valueAfter,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TagDiff, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("action")
    tagDiffTypeCodec.encode(bsonWriter, value.action, encoderContext)

    bsonWriter.writeName("key")
    stringCodec.encode(bsonWriter, value.key, encoderContext)

    if (value.valueBefore.isDefined) {
      bsonWriter.writeName("valueBefore")
      stringCodec.encode(bsonWriter, value.valueBefore.get, encoderContext)
    }

    if (value.valueAfter.isDefined) {
      bsonWriter.writeName("valueAfter")
      stringCodec.encode(bsonWriter, value.valueAfter.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TagDiff] = {
    classOf[TagDiff]
  }
}
