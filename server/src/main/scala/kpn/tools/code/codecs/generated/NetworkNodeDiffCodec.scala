// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.diff.TagDiffs
import kpn.api.common.diff.network.NetworkNodeDiff
import kpn.api.common.diff.network.NodeIntegrityCheckDiff
import kpn.api.common.diff.network.NodeRouteReferenceDiffs
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkNodeDiffCodec(registry: CodecRegistry) extends Codec[NetworkNodeDiff] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val nodeIntegrityCheckDiffCodec = registry.get(classOf[NodeIntegrityCheckDiff])
  private val nodeRouteReferenceDiffsCodec = registry.get(classOf[NodeRouteReferenceDiffs])
  private val tagDiffsCodec = registry.get(classOf[TagDiffs])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkNodeDiff = {
    bsonReader.readStartDocument()

    var connection: Option[Boolean] = None
    var roleConnection: Option[Boolean] = None
    var definedInNetworkRelation: Option[Boolean] = None
    var routeReferenceDiffs: Option[NodeRouteReferenceDiffs] = None
    var nodeIntegrityCheckDiff: Option[NodeIntegrityCheckDiff] = None
    var tagDiffs: Option[TagDiffs] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "connection") {
        connection = Some(booleanCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "roleConnection") {
        roleConnection = Some(booleanCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "definedInNetworkRelation") {
        definedInNetworkRelation = Some(booleanCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "routeReferenceDiffs") {
        routeReferenceDiffs = Some(nodeRouteReferenceDiffsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nodeIntegrityCheckDiff") {
        nodeIntegrityCheckDiff = Some(nodeIntegrityCheckDiffCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "tagDiffs") {
        tagDiffs = Some(tagDiffsCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkNodeDiffCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkNodeDiff(
      connection,
      roleConnection,
      definedInNetworkRelation,
      routeReferenceDiffs,
      nodeIntegrityCheckDiff,
      tagDiffs,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkNodeDiff, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.connection.isDefined) {
      bsonWriter.writeName("connection")
      booleanCodec.encode(bsonWriter, value.connection.get, encoderContext)
    }

    if (value.roleConnection.isDefined) {
      bsonWriter.writeName("roleConnection")
      booleanCodec.encode(bsonWriter, value.roleConnection.get, encoderContext)
    }

    if (value.definedInNetworkRelation.isDefined) {
      bsonWriter.writeName("definedInNetworkRelation")
      booleanCodec.encode(bsonWriter, value.definedInNetworkRelation.get, encoderContext)
    }

    if (value.routeReferenceDiffs.isDefined) {
      bsonWriter.writeName("routeReferenceDiffs")
      nodeRouteReferenceDiffsCodec.encode(bsonWriter, value.routeReferenceDiffs.get, encoderContext)
    }

    if (value.nodeIntegrityCheckDiff.isDefined) {
      bsonWriter.writeName("nodeIntegrityCheckDiff")
      nodeIntegrityCheckDiffCodec.encode(bsonWriter, value.nodeIntegrityCheckDiff.get, encoderContext)
    }

    if (value.tagDiffs.isDefined) {
      bsonWriter.writeName("tagDiffs")
      tagDiffsCodec.encode(bsonWriter, value.tagDiffs.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkNodeDiff] = {
    classOf[NetworkNodeDiff]
  }
}
