// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.filter.ChangesFilterPeriod
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ChangesFilterPeriodCodec(registry: CodecRegistry) extends Codec[ChangesFilterPeriod] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val changesFilterPeriodCodec = registry.get(classOf[ChangesFilterPeriod])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ChangesFilterPeriod = {
    bsonReader.readStartDocument()

    var name: Long = 0
    var totalCount: Long = 0
    var impactedCount: Long = 0
    var current: Boolean = false
    var selected: Boolean = false
    var periods: Seq[ChangesFilterPeriod] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "name") {
        name = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "totalCount") {
        totalCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "impactedCount") {
        impactedCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "current") {
        current = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "selected") {
        selected = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "periods") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[ChangesFilterPeriod]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += changesFilterPeriodCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        periods = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ChangesFilterPeriodCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ChangesFilterPeriod(
      name,
      totalCount,
      impactedCount,
      current,
      selected,
      periods,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ChangesFilterPeriod, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("name")
    longCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("totalCount")
    longCodec.encode(bsonWriter, value.totalCount, encoderContext)

    bsonWriter.writeName("impactedCount")
    longCodec.encode(bsonWriter, value.impactedCount, encoderContext)

    bsonWriter.writeName("current")
    booleanCodec.encode(bsonWriter, value.current, encoderContext)

    bsonWriter.writeName("selected")
    booleanCodec.encode(bsonWriter, value.selected, encoderContext)

    bsonWriter.writeName("periods")
    bsonWriter.writeStartArray()
    value.periods.foreach(v => changesFilterPeriodCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ChangesFilterPeriod] = {
    classOf[ChangesFilterPeriod]
  }
}
