// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.PoiState
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PoiStateCodec(registry: CodecRegistry) extends Codec[PoiState] {

  private val stringCodec = registry.get(classOf[String])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PoiState = {
    bsonReader.readStartDocument()

    var _id: String = null
    var imageLink: Option[String] = None
    var imageStatus: Option[String] = None
    var imageStatusDetail: Option[String] = None
    var imageFirstSeen: Option[Timestamp] = None
    var imageLastSeen: Option[Timestamp] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "imageLink") {
        imageLink = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "imageStatus") {
        imageStatus = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "imageStatusDetail") {
        imageStatusDetail = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "imageFirstSeen") {
        imageFirstSeen = Some(timestampCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "imageLastSeen") {
        imageLastSeen = Some(timestampCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in PoiStateCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PoiState(
      _id,
      imageLink,
      imageStatus,
      imageStatusDetail,
      imageFirstSeen,
      imageLastSeen,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PoiState, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    stringCodec.encode(bsonWriter, value._id, encoderContext)

    if (value.imageLink.isDefined) {
      bsonWriter.writeName("imageLink")
      stringCodec.encode(bsonWriter, value.imageLink.get, encoderContext)
    }

    if (value.imageStatus.isDefined) {
      bsonWriter.writeName("imageStatus")
      stringCodec.encode(bsonWriter, value.imageStatus.get, encoderContext)
    }

    if (value.imageStatusDetail.isDefined) {
      bsonWriter.writeName("imageStatusDetail")
      stringCodec.encode(bsonWriter, value.imageStatusDetail.get, encoderContext)
    }

    if (value.imageFirstSeen.isDefined) {
      bsonWriter.writeName("imageFirstSeen")
      timestampCodec.encode(bsonWriter, value.imageFirstSeen.get, encoderContext)
    }

    if (value.imageLastSeen.isDefined) {
      bsonWriter.writeName("imageLastSeen")
      timestampCodec.encode(bsonWriter, value.imageLastSeen.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PoiState] = {
    classOf[PoiState]
  }
}
