// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.tools.support.NodeImageTag
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeImageTagCodec(registry: CodecRegistry) extends Codec[NodeImageTag] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeImageTag = {
    bsonReader.readStartDocument()

    var nodeId: Long = 0
    var key: String = null
    var value: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "nodeId") {
        nodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "key") {
        key = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "value") {
        value = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NodeImageTagCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeImageTag(
      nodeId,
      key,
      value,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeImageTag, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("nodeId")
    longCodec.encode(bsonWriter, value.nodeId, encoderContext)

    bsonWriter.writeName("key")
    stringCodec.encode(bsonWriter, value.key, encoderContext)

    bsonWriter.writeName("value")
    stringCodec.encode(bsonWriter, value.value, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeImageTag] = {
    classOf[NodeImageTag]
  }
}
