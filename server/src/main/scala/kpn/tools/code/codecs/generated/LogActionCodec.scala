// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.status.ActionTimestamp
import kpn.core.metrics.LogAction
import kpn.core.metrics.LogValue
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LogActionCodec(registry: CodecRegistry) extends Codec[LogAction] {

  private val actionTimestampCodec = registry.get(classOf[ActionTimestamp])
  private val logValueCodec = registry.get(classOf[LogValue])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LogAction = {
    bsonReader.readStartDocument()

    var timestamp: ActionTimestamp = null
    var logfile: String = null
    var values: Seq[LogValue] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "timestamp") {
        timestamp = actionTimestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "logfile") {
        logfile = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "values") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LogValue]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += logValueCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        values = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LogActionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LogAction(
      timestamp,
      logfile,
      values,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LogAction, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("timestamp")
    actionTimestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    bsonWriter.writeName("logfile")
    stringCodec.encode(bsonWriter, value.logfile, encoderContext)

    bsonWriter.writeName("values")
    bsonWriter.writeStartArray()
    value.values.foreach(v => logValueCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LogAction] = {
    classOf[LogAction]
  }
}
