// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.tools.location.LocationGeometry
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry
import org.locationtech.jts.geom.Envelope
import org.locationtech.jts.geom.Geometry

class LocationGeometryCodec(registry: CodecRegistry) extends Codec[LocationGeometry] {

  private val envelopeCodec = registry.get(classOf[Envelope])
  private val geometryCodec = registry.get(classOf[Geometry])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationGeometry = {
    bsonReader.readStartDocument()

    var geometry: Geometry = null
    var envelope: Envelope = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "geometry") {
        geometry = geometryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "envelope") {
        envelope = envelopeCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationGeometryCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationGeometry(
      geometry,
      envelope,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationGeometry, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("geometry")
    geometryCodec.encode(bsonWriter, value.geometry, encoderContext)

    bsonWriter.writeName("envelope")
    envelopeCodec.encode(bsonWriter, value.envelope, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationGeometry] = {
    classOf[LocationGeometry]
  }
}
