// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.TimeInfo
import kpn.api.common.location.LocationEditPage
import kpn.api.common.location.LocationSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationEditPageCodec(registry: CodecRegistry) extends Codec[LocationEditPage] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val boundsCodec = registry.get(classOf[Bounds])
  private val locationSummaryCodec = registry.get(classOf[LocationSummary])
  private val longCodec = registry.get(classOf[Long])
  private val timeInfoCodec = registry.get(classOf[TimeInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationEditPage = {
    bsonReader.readStartDocument()

    var timeInfo: TimeInfo = null
    var summary: LocationSummary = null
    var tooManyNodes: Boolean = false
    var maxNodes: Long = 0
    var bounds: Bounds = null
    var nodeIds: Seq[Long] = null
    var routeIds: Seq[Long] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "timeInfo") {
        timeInfo = timeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "summary") {
        summary = locationSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "tooManyNodes") {
        tooManyNodes = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "maxNodes") {
        maxNodes = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "bounds") {
        bounds = boundsCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodeIds = valueBuffer.toSeq
      }
      else if (fieldName == "routeIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routeIds = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LocationEditPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationEditPage(
      timeInfo,
      summary,
      tooManyNodes,
      maxNodes,
      bounds,
      nodeIds,
      routeIds,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationEditPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("timeInfo")
    timeInfoCodec.encode(bsonWriter, value.timeInfo, encoderContext)

    bsonWriter.writeName("summary")
    locationSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("tooManyNodes")
    booleanCodec.encode(bsonWriter, value.tooManyNodes, encoderContext)

    bsonWriter.writeName("maxNodes")
    longCodec.encode(bsonWriter, value.maxNodes, encoderContext)

    bsonWriter.writeName("bounds")
    boundsCodec.encode(bsonWriter, value.bounds, encoderContext)

    bsonWriter.writeName("nodeIds")
    bsonWriter.writeStartArray()
    value.nodeIds.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("routeIds")
    bsonWriter.writeStartArray()
    value.routeIds.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationEditPage] = {
    classOf[LocationEditPage]
  }
}
