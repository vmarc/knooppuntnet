// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Fact
import kpn.api.common.location.BooleanParameter
import kpn.api.common.location.LastUpdatedParameter
import kpn.api.common.location.LocationRoutesParameters
import kpn.api.common.location.SurveyParameter
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationRoutesParametersCodec(registry: CodecRegistry) extends Codec[LocationRoutesParameters] {

  private val booleanParameterCodec = registry.get(classOf[BooleanParameter])
  private val factCodec = registry.get(classOf[Fact])
  private val lastUpdatedParameterCodec = registry.get(classOf[LastUpdatedParameter])
  private val longCodec = registry.get(classOf[Long])
  private val surveyParameterCodec = registry.get(classOf[SurveyParameter])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationRoutesParameters = {
    bsonReader.readStartDocument()

    var fact: Option[Fact] = None
    var survey: Option[SurveyParameter] = None
    var lastUpdated: Option[LastUpdatedParameter] = None
    var proposed: Option[BooleanParameter] = None
    var pageSize: Long = 0
    var pageIndex: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "fact") {
        fact = Some(factCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "survey") {
        survey = Some(surveyParameterCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "lastUpdated") {
        lastUpdated = Some(lastUpdatedParameterCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "proposed") {
        proposed = Some(booleanParameterCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "pageSize") {
        pageSize = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "pageIndex") {
        pageIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LocationRoutesParametersCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationRoutesParameters(
      fact,
      survey,
      lastUpdated,
      proposed,
      pageSize,
      pageIndex,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationRoutesParameters, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.fact.isDefined) {
      bsonWriter.writeName("fact")
      factCodec.encode(bsonWriter, value.fact.get, encoderContext)
    }

    if (value.survey.isDefined) {
      bsonWriter.writeName("survey")
      surveyParameterCodec.encode(bsonWriter, value.survey.get, encoderContext)
    }

    if (value.lastUpdated.isDefined) {
      bsonWriter.writeName("lastUpdated")
      lastUpdatedParameterCodec.encode(bsonWriter, value.lastUpdated.get, encoderContext)
    }

    if (value.proposed.isDefined) {
      bsonWriter.writeName("proposed")
      booleanParameterCodec.encode(bsonWriter, value.proposed.get, encoderContext)
    }

    bsonWriter.writeName("pageSize")
    longCodec.encode(bsonWriter, value.pageSize, encoderContext)

    bsonWriter.writeName("pageIndex")
    longCodec.encode(bsonWriter, value.pageIndex, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationRoutesParameters] = {
    classOf[LocationRoutesParameters]
  }
}
