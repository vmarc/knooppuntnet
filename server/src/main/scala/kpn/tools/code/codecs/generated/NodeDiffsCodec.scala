// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.diff.NodeData
import kpn.api.common.diff.NodeDataUpdate
import kpn.api.common.diff.NodeDiffs
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeDiffsCodec(registry: CodecRegistry) extends Codec[NodeDiffs] {

  private val nodeDataCodec = registry.get(classOf[NodeData])
  private val nodeDataUpdateCodec = registry.get(classOf[NodeDataUpdate])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeDiffs = {
    bsonReader.readStartDocument()

    var removed: Seq[NodeData] = null
    var added: Seq[NodeData] = null
    var updated: Seq[NodeDataUpdate] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "removed") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NodeData]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += nodeDataCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        removed = valueBuffer.toSeq
      }
      else if (fieldName == "added") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NodeData]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += nodeDataCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        added = valueBuffer.toSeq
      }
      else if (fieldName == "updated") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NodeDataUpdate]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += nodeDataUpdateCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        updated = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in NodeDiffsCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeDiffs(
      removed,
      added,
      updated,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeDiffs, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("removed")
    bsonWriter.writeStartArray()
    value.removed.foreach(v => nodeDataCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("added")
    bsonWriter.writeStartArray()
    value.added.foreach(v => nodeDataCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("updated")
    bsonWriter.writeStartArray()
    value.updated.foreach(v => nodeDataUpdateCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeDiffs] = {
    classOf[NodeDiffs]
  }
}
