// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.RouteScope
import kpn.api.common.search.RouteSearchResult
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteSearchResultCodec(registry: CodecRegistry) extends Codec[RouteSearchResult] {

  private val boundsCodec = registry.get(classOf[Bounds])
  private val longCodec = registry.get(classOf[Long])
  private val routeScopeCodec = registry.get(classOf[RouteScope])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteSearchResult = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var name: String = null
    var scopes: Seq[RouteScope] = null
    var distance: Long = 0
    var symbol: Option[String] = None
    var bounds: Option[Bounds] = None
    var routeIds: Seq[Long] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "scopes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteScope]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeScopeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        scopes = valueBuffer.toSeq
      }
      else if (fieldName == "distance") {
        distance = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "symbol") {
        symbol = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "bounds") {
        bounds = Some(boundsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "routeIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routeIds = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteSearchResultCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteSearchResult(
      id,
      name,
      scopes,
      distance,
      symbol,
      bounds,
      routeIds,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteSearchResult, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("scopes")
    bsonWriter.writeStartArray()
    value.scopes.foreach(v => routeScopeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("distance")
    longCodec.encode(bsonWriter, value.distance, encoderContext)

    if (value.symbol.isDefined) {
      bsonWriter.writeName("symbol")
      stringCodec.encode(bsonWriter, value.symbol.get, encoderContext)
    }

    if (value.bounds.isDefined) {
      bsonWriter.writeName("bounds")
      boundsCodec.encode(bsonWriter, value.bounds.get, encoderContext)
    }

    bsonWriter.writeName("routeIds")
    bsonWriter.writeStartArray()
    value.routeIds.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteSearchResult] = {
    classOf[RouteSearchResult]
  }
}
