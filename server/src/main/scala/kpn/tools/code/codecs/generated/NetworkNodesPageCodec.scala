// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.SurveyDateInfo
import kpn.api.common.TimeInfo
import kpn.api.common.network.NetworkNodeRow
import kpn.api.common.network.NetworkNodesPage
import kpn.api.common.network.NetworkSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkNodesPageCodec(registry: CodecRegistry) extends Codec[NetworkNodesPage] {

  private val networkNodeRowCodec = registry.get(classOf[NetworkNodeRow])
  private val networkSummaryCodec = registry.get(classOf[NetworkSummary])
  private val surveyDateInfoCodec = registry.get(classOf[SurveyDateInfo])
  private val timeInfoCodec = registry.get(classOf[TimeInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkNodesPage = {
    bsonReader.readStartDocument()

    var timeInfo: TimeInfo = null
    var surveyDateInfo: SurveyDateInfo = null
    var summary: NetworkSummary = null
    var nodes: Seq[NetworkNodeRow] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "timeInfo") {
        timeInfo = timeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "surveyDateInfo") {
        surveyDateInfo = surveyDateInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "summary") {
        summary = networkSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NetworkNodeRow]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += networkNodeRowCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodes = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in NetworkNodesPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkNodesPage(
      timeInfo,
      surveyDateInfo,
      summary,
      nodes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkNodesPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("timeInfo")
    timeInfoCodec.encode(bsonWriter, value.timeInfo, encoderContext)

    bsonWriter.writeName("surveyDateInfo")
    surveyDateInfoCodec.encode(bsonWriter, value.surveyDateInfo, encoderContext)

    bsonWriter.writeName("summary")
    networkSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("nodes")
    bsonWriter.writeStartArray()
    value.nodes.foreach(v => networkNodeRowCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkNodesPage] = {
    classOf[NetworkNodesPage]
  }
}
