// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.taginfo.TagInfoTag
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TagInfoTagCodec(registry: CodecRegistry) extends Codec[TagInfoTag] {

  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TagInfoTag = {
    bsonReader.readStartDocument()

    var key: String = null
    var value: Option[String] = None
    var description: String = null
    var object_types: Option[Seq[String]] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "key") {
        key = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "value") {
        value = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "description") {
        description = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "object_types") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        object_types = Some(valueBuffer.toSeq)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in TagInfoTagCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TagInfoTag(
      key,
      value,
      description,
      object_types,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TagInfoTag, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("key")
    stringCodec.encode(bsonWriter, value.key, encoderContext)

    if (value.value.isDefined) {
      bsonWriter.writeName("value")
      stringCodec.encode(bsonWriter, value.value.get, encoderContext)
    }

    bsonWriter.writeName("description")
    stringCodec.encode(bsonWriter, value.description, encoderContext)

    if (value.object_types.isDefined) {
      bsonWriter.writeName("object_types")
      bsonWriter.writeStartArray()
      value.object_types.get.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TagInfoTag] = {
    classOf[TagInfoTag]
  }
}
