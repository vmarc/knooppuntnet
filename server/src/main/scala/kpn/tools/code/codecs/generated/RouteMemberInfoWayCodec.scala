// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.RouteMemberInfoWay
import kpn.api.common.route.Link
import kpn.api.common.route.RouteNetworkNodeInfo
import kpn.api.common.route.WayDirection
import kpn.api.custom.Tag
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteMemberInfoWayCodec(registry: CodecRegistry) extends Codec[RouteMemberInfoWay] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val linkCodec = registry.get(classOf[Link])
  private val longCodec = registry.get(classOf[Long])
  private val routeNetworkNodeInfoCodec = registry.get(classOf[RouteNetworkNodeInfo])
  private val stringCodec = registry.get(classOf[String])
  private val tagCodec = registry.get(classOf[Tag])
  private val timestampCodec = registry.get(classOf[Timestamp])
  private val wayDirectionCodec = registry.get(classOf[WayDirection])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteMemberInfoWay = {
    bsonReader.readStartDocument()

    var wayType: Option[String] = None
    var nodes: Seq[RouteNetworkNodeInfo] = null
    var timestamp: Timestamp = null
    var surface: String = null
    var accessible: Boolean = false
    var ferry: Boolean = false
    var distance: Long = 0
    var nodeCount: String = null
    var oneWay: WayDirection = null
    var oneWayTags: Seq[Tag] = null
    var link: Link = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "wayType") {
        wayType = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNetworkNodeInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNetworkNodeInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodes = valueBuffer.toSeq
      }
      else if (fieldName == "timestamp") {
        timestamp = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "surface") {
        surface = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "accessible") {
        accessible = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "ferry") {
        ferry = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "distance") {
        distance = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeCount") {
        nodeCount = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "oneWay") {
        oneWay = wayDirectionCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "oneWayTags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        oneWayTags = valueBuffer.toSeq
      }
      else if (fieldName == "link") {
        link = linkCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteMemberInfoWayCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteMemberInfoWay(
      wayType,
      nodes,
      timestamp,
      surface,
      accessible,
      ferry,
      distance,
      nodeCount,
      oneWay,
      oneWayTags,
      link,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteMemberInfoWay, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.wayType.isDefined) {
      bsonWriter.writeName("wayType")
      stringCodec.encode(bsonWriter, value.wayType.get, encoderContext)
    }

    bsonWriter.writeName("nodes")
    bsonWriter.writeStartArray()
    value.nodes.foreach(v => routeNetworkNodeInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("timestamp")
    timestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    bsonWriter.writeName("surface")
    stringCodec.encode(bsonWriter, value.surface, encoderContext)

    bsonWriter.writeName("accessible")
    booleanCodec.encode(bsonWriter, value.accessible, encoderContext)

    bsonWriter.writeName("ferry")
    booleanCodec.encode(bsonWriter, value.ferry, encoderContext)

    bsonWriter.writeName("distance")
    longCodec.encode(bsonWriter, value.distance, encoderContext)

    bsonWriter.writeName("nodeCount")
    stringCodec.encode(bsonWriter, value.nodeCount, encoderContext)

    bsonWriter.writeName("oneWay")
    wayDirectionCodec.encode(bsonWriter, value.oneWay, encoderContext)

    bsonWriter.writeName("oneWayTags")
    bsonWriter.writeStartArray()
    value.oneWayTags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("link")
    linkCodec.encode(bsonWriter, value.link, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteMemberInfoWay] = {
    classOf[RouteMemberInfoWay]
  }
}
