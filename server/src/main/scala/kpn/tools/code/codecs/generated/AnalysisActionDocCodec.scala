// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.metrics.AnalysisAction
import kpn.core.metrics.AnalysisActionDoc
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class AnalysisActionDocCodec(registry: CodecRegistry) extends Codec[AnalysisActionDoc] {

  private val analysisActionCodec = registry.get(classOf[AnalysisAction])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): AnalysisActionDoc = {
    bsonReader.readStartDocument()

    var _id: String = null
    var analysis: AnalysisAction = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "analysis") {
        analysis = analysisActionCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in AnalysisActionDocCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    AnalysisActionDoc(
      _id,
      analysis,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: AnalysisActionDoc, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    stringCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("analysis")
    analysisActionCodec.encode(bsonWriter, value.analysis, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[AnalysisActionDoc] = {
    classOf[AnalysisActionDoc]
  }
}
