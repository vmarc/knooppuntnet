// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Country
import kpn.api.common.RouteType
import kpn.api.common.subset.SubsetInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class SubsetInfoCodec(registry: CodecRegistry) extends Codec[SubsetInfo] {

  private val countryCodec = registry.get(classOf[Country])
  private val longCodec = registry.get(classOf[Long])
  private val routeTypeCodec = registry.get(classOf[RouteType])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): SubsetInfo = {
    bsonReader.readStartDocument()

    var country: Country = null
    var routeType: RouteType = null
    var networkCount: Long = 0
    var factCount: Long = 0
    var changesCount: Long = 0
    var orphanNodeCount: Long = 0
    var orphanRouteCount: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "country") {
        country = countryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeType") {
        routeType = routeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "networkCount") {
        networkCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "factCount") {
        factCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changesCount") {
        changesCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "orphanNodeCount") {
        orphanNodeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "orphanRouteCount") {
        orphanRouteCount = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in SubsetInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    SubsetInfo(
      country,
      routeType,
      networkCount,
      factCount,
      changesCount,
      orphanNodeCount,
      orphanRouteCount,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: SubsetInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("country")
    countryCodec.encode(bsonWriter, value.country, encoderContext)

    bsonWriter.writeName("routeType")
    routeTypeCodec.encode(bsonWriter, value.routeType, encoderContext)

    bsonWriter.writeName("networkCount")
    longCodec.encode(bsonWriter, value.networkCount, encoderContext)

    bsonWriter.writeName("factCount")
    longCodec.encode(bsonWriter, value.factCount, encoderContext)

    bsonWriter.writeName("changesCount")
    longCodec.encode(bsonWriter, value.changesCount, encoderContext)

    bsonWriter.writeName("orphanNodeCount")
    longCodec.encode(bsonWriter, value.orphanNodeCount, encoderContext)

    bsonWriter.writeName("orphanRouteCount")
    longCodec.encode(bsonWriter, value.orphanRouteCount, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[SubsetInfo] = {
    classOf[SubsetInfo]
  }
}
