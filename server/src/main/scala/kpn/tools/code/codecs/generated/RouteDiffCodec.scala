// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.diff.TagDiffs
import kpn.api.common.diff.common.FactDiffs
import kpn.api.common.diff.route.RouteDiff
import kpn.api.common.diff.route.RouteNameDiff
import kpn.api.common.diff.route.RouteNodeDiff
import kpn.api.common.diff.route.RouteRoleDiff
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteDiffCodec(registry: CodecRegistry) extends Codec[RouteDiff] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val factDiffsCodec = registry.get(classOf[FactDiffs])
  private val routeNameDiffCodec = registry.get(classOf[RouteNameDiff])
  private val routeNodeDiffCodec = registry.get(classOf[RouteNodeDiff])
  private val routeRoleDiffCodec = registry.get(classOf[RouteRoleDiff])
  private val tagDiffsCodec = registry.get(classOf[TagDiffs])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteDiff = {
    bsonReader.readStartDocument()

    var nameDiff: Option[RouteNameDiff] = None
    var roleDiff: Option[RouteRoleDiff] = None
    var factDiffs: Option[FactDiffs] = None
    var nodeDiffs: Seq[RouteNodeDiff] = null
    var memberOrderChanged: Boolean = false
    var tagDiffs: Option[TagDiffs] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "nameDiff") {
        nameDiff = Some(routeNameDiffCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "roleDiff") {
        roleDiff = Some(routeRoleDiffCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "factDiffs") {
        factDiffs = Some(factDiffsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nodeDiffs") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNodeDiff]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNodeDiffCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodeDiffs = valueBuffer.toSeq
      }
      else if (fieldName == "memberOrderChanged") {
        memberOrderChanged = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "tagDiffs") {
        tagDiffs = Some(tagDiffsCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in RouteDiffCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteDiff(
      nameDiff,
      roleDiff,
      factDiffs,
      nodeDiffs,
      memberOrderChanged,
      tagDiffs,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteDiff, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.nameDiff.isDefined) {
      bsonWriter.writeName("nameDiff")
      routeNameDiffCodec.encode(bsonWriter, value.nameDiff.get, encoderContext)
    }

    if (value.roleDiff.isDefined) {
      bsonWriter.writeName("roleDiff")
      routeRoleDiffCodec.encode(bsonWriter, value.roleDiff.get, encoderContext)
    }

    if (value.factDiffs.isDefined) {
      bsonWriter.writeName("factDiffs")
      factDiffsCodec.encode(bsonWriter, value.factDiffs.get, encoderContext)
    }

    bsonWriter.writeName("nodeDiffs")
    bsonWriter.writeStartArray()
    value.nodeDiffs.foreach(v => routeNodeDiffCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("memberOrderChanged")
    booleanCodec.encode(bsonWriter, value.memberOrderChanged, encoderContext)

    if (value.tagDiffs.isDefined) {
      bsonWriter.writeName("tagDiffs")
      tagDiffsCodec.encode(bsonWriter, value.tagDiffs.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteDiff] = {
    classOf[RouteDiff]
  }
}
