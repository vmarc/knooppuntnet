// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.planner.PlanNode
import kpn.api.common.planner.PlanRoute
import kpn.api.common.planner.PlanSegment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PlanRouteCodec(registry: CodecRegistry) extends Codec[PlanRoute] {

  private val longCodec = registry.get(classOf[Long])
  private val planNodeCodec = registry.get(classOf[PlanNode])
  private val planSegmentCodec = registry.get(classOf[PlanSegment])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PlanRoute = {
    bsonReader.readStartDocument()

    var sourceNode: PlanNode = null
    var sinkNode: PlanNode = null
    var meters: Long = 0
    var segments: Seq[PlanSegment] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "sourceNode") {
        sourceNode = planNodeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "sinkNode") {
        sinkNode = planNodeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "segments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[PlanSegment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += planSegmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        segments = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in PlanRouteCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PlanRoute(
      sourceNode,
      sinkNode,
      meters,
      segments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PlanRoute, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("sourceNode")
    planNodeCodec.encode(bsonWriter, value.sourceNode, encoderContext)

    bsonWriter.writeName("sinkNode")
    planNodeCodec.encode(bsonWriter, value.sinkNode, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("segments")
    bsonWriter.writeStartArray()
    value.segments.foreach(v => planSegmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PlanRoute] = {
    classOf[PlanRoute]
  }
}
