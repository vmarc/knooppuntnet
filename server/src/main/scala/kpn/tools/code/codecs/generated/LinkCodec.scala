// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.route.Link
import kpn.api.common.route.LinkDirection
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LinkCodec(registry: CodecRegistry) extends Codec[Link] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val linkDirectionCodec = registry.get(classOf[LinkDirection])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): Link = {
    bsonReader.readStartDocument()

    var memberIndex: Long = 0
    var direction: LinkDirection = null
    var hasPrev: Boolean = false
    var hasNext: Boolean = false
    var isLoop: Boolean = false
    var isOnewayLoopForwardPart: Boolean = false
    var isOnewayLoopBackwardPart: Boolean = false
    var isOnewayHead: Boolean = false
    var isOnewayTail: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "memberIndex") {
        memberIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "direction") {
        direction = linkDirectionCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "hasPrev") {
        hasPrev = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "hasNext") {
        hasNext = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "isLoop") {
        isLoop = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "isOnewayLoopForwardPart") {
        isOnewayLoopForwardPart = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "isOnewayLoopBackwardPart") {
        isOnewayLoopBackwardPart = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "isOnewayHead") {
        isOnewayHead = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "isOnewayTail") {
        isOnewayTail = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LinkCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    Link(
      memberIndex,
      direction,
      hasPrev,
      hasNext,
      isLoop,
      isOnewayLoopForwardPart,
      isOnewayLoopBackwardPart,
      isOnewayHead,
      isOnewayTail,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: Link, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("memberIndex")
    longCodec.encode(bsonWriter, value.memberIndex, encoderContext)

    bsonWriter.writeName("direction")
    linkDirectionCodec.encode(bsonWriter, value.direction, encoderContext)

    bsonWriter.writeName("hasPrev")
    booleanCodec.encode(bsonWriter, value.hasPrev, encoderContext)

    bsonWriter.writeName("hasNext")
    booleanCodec.encode(bsonWriter, value.hasNext, encoderContext)

    bsonWriter.writeName("isLoop")
    booleanCodec.encode(bsonWriter, value.isLoop, encoderContext)

    bsonWriter.writeName("isOnewayLoopForwardPart")
    booleanCodec.encode(bsonWriter, value.isOnewayLoopForwardPart, encoderContext)

    bsonWriter.writeName("isOnewayLoopBackwardPart")
    booleanCodec.encode(bsonWriter, value.isOnewayLoopBackwardPart, encoderContext)

    bsonWriter.writeName("isOnewayHead")
    booleanCodec.encode(bsonWriter, value.isOnewayHead, encoderContext)

    bsonWriter.writeName("isOnewayTail")
    booleanCodec.encode(bsonWriter, value.isOnewayTail, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[Link] = {
    classOf[Link]
  }
}
