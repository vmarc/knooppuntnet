// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.poi.LocationPoiInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationPoiInfoCodec(registry: CodecRegistry) extends Codec[LocationPoiInfo] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationPoiInfo = {
    bsonReader.readStartDocument()

    var rowIndex: Long = 0
    var _id: String = null
    var elementType: String = null
    var elementId: Long = 0
    var layers: Seq[String] = null
    var description: Option[String] = None
    var address: Option[String] = None
    var link: Boolean = false
    var image: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "rowIndex") {
        rowIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "_id") {
        _id = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "elementType") {
        elementType = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "elementId") {
        elementId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "layers") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        layers = valueBuffer.toSeq
      }
      else if (fieldName == "description") {
        description = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "address") {
        address = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "link") {
        link = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "image") {
        image = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LocationPoiInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationPoiInfo(
      rowIndex,
      _id,
      elementType,
      elementId,
      layers,
      description,
      address,
      link,
      image,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationPoiInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("rowIndex")
    longCodec.encode(bsonWriter, value.rowIndex, encoderContext)

    bsonWriter.writeName("_id")
    stringCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("elementType")
    stringCodec.encode(bsonWriter, value.elementType, encoderContext)

    bsonWriter.writeName("elementId")
    longCodec.encode(bsonWriter, value.elementId, encoderContext)

    bsonWriter.writeName("layers")
    bsonWriter.writeStartArray()
    value.layers.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.description.isDefined) {
      bsonWriter.writeName("description")
      stringCodec.encode(bsonWriter, value.description.get, encoderContext)
    }

    if (value.address.isDefined) {
      bsonWriter.writeName("address")
      stringCodec.encode(bsonWriter, value.address.get, encoderContext)
    }

    bsonWriter.writeName("link")
    booleanCodec.encode(bsonWriter, value.link, encoderContext)

    bsonWriter.writeName("image")
    booleanCodec.encode(bsonWriter, value.image, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationPoiInfo] = {
    classOf[LocationPoiInfo]
  }
}
