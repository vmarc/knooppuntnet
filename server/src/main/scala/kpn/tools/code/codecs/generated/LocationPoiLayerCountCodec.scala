// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.poi.LocationPoiLayerCount
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationPoiLayerCountCodec(registry: CodecRegistry) extends Codec[LocationPoiLayerCount] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationPoiLayerCount = {
    bsonReader.readStartDocument()

    var layer: String = null
    var count: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "layer") {
        layer = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "count") {
        count = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationPoiLayerCountCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationPoiLayerCount(
      layer,
      count,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationPoiLayerCount, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("layer")
    stringCodec.encode(bsonWriter, value.layer, encoderContext)

    bsonWriter.writeName("count")
    longCodec.encode(bsonWriter, value.count, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationPoiLayerCount] = {
    classOf[LocationPoiLayerCount]
  }
}
