// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.server.analyzer.engine.analysis.location.LocationTree
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationTreeCodec(registry: CodecRegistry) extends Codec[LocationTree] {

  private val locationTreeCodec = registry.get(classOf[LocationTree])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationTree = {
    bsonReader.readStartDocument()

    var name: String = null
    var children: Option[Seq[LocationTree]] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "children") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationTree]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationTreeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        children = Some(valueBuffer.toSeq)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationTreeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationTree(
      name,
      children,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationTree, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    if (value.children.isDefined) {
      bsonWriter.writeName("children")
      bsonWriter.writeStartArray()
      value.children.get.foreach(v => locationTreeCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationTree] = {
    classOf[LocationTree]
  }
}
