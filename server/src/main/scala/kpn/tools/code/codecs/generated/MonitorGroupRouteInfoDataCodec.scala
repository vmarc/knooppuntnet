// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.database.actions.monitor.MonitorGroupRouteInfoData
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorGroupRouteInfoDataCodec(registry: CodecRegistry) extends Codec[MonitorGroupRouteInfoData] {

  private val boundsCodec = registry.get(classOf[Bounds])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorGroupRouteInfoData = {
    bsonReader.readStartDocument()

    var groupId: String = null
    var monitorRouteId: String = null
    var bounds: Option[Bounds] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "groupId") {
        groupId = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "monitorRouteId") {
        monitorRouteId = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "bounds") {
        bounds = Some(boundsCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorGroupRouteInfoDataCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorGroupRouteInfoData(
      groupId,
      monitorRouteId,
      bounds,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorGroupRouteInfoData, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("groupId")
    stringCodec.encode(bsonWriter, value.groupId, encoderContext)

    bsonWriter.writeName("monitorRouteId")
    stringCodec.encode(bsonWriter, value.monitorRouteId, encoderContext)

    if (value.bounds.isDefined) {
      bsonWriter.writeName("bounds")
      boundsCodec.encode(bsonWriter, value.bounds.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorGroupRouteInfoData] = {
    classOf[MonitorGroupRouteInfoData]
  }
}
