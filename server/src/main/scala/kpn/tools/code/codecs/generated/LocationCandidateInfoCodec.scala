// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LocationInfo
import kpn.api.common.location.LocationCandidateInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationCandidateInfoCodec(registry: CodecRegistry) extends Codec[LocationCandidateInfo] {

  private val locationInfoCodec = registry.get(classOf[LocationInfo])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationCandidateInfo = {
    bsonReader.readStartDocument()

    var locationInfos: Seq[LocationInfo] = null
    var percentage: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "locationInfos") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        locationInfos = valueBuffer.toSeq
      }
      else if (fieldName == "percentage") {
        percentage = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationCandidateInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationCandidateInfo(
      locationInfos,
      percentage,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationCandidateInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("locationInfos")
    bsonWriter.writeStartArray()
    value.locationInfos.foreach(v => locationInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("percentage")
    longCodec.encode(bsonWriter, value.percentage, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationCandidateInfo] = {
    classOf[LocationCandidateInfo]
  }
}
