// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LatLonImpl
import kpn.api.common.network.Integrity
import kpn.api.common.network.NetworkDetail
import kpn.api.custom.Day
import kpn.api.custom.Tag
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkDetailCodec(registry: CodecRegistry) extends Codec[NetworkDetail] {

  private val dayCodec = registry.get(classOf[Day])
  private val integrityCodec = registry.get(classOf[Integrity])
  private val latLonImplCodec = registry.get(classOf[LatLonImpl])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val tagCodec = registry.get(classOf[Tag])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkDetail = {
    bsonReader.readStartDocument()

    var km: Long = 0
    var meters: Long = 0
    var version: Long = 0
    var changeSetId: Long = 0
    var lastUpdated: Timestamp = null
    var relationLastUpdated: Timestamp = null
    var lastSurvey: Option[Day] = None
    var tags: Seq[Tag] = null
    var brokenRouteCount: Long = 0
    var brokenRoutePercentage: String = null
    var integrity: Integrity = null
    var inaccessibleRouteCount: Long = 0
    var connectionCount: Long = 0
    var center: Option[LatLonImpl] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "km") {
        km = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "version") {
        version = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeSetId") {
        changeSetId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastUpdated") {
        lastUpdated = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "relationLastUpdated") {
        relationLastUpdated = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastSurvey") {
        lastSurvey = Some(dayCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "tags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        tags = valueBuffer.toSeq
      }
      else if (fieldName == "brokenRouteCount") {
        brokenRouteCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "brokenRoutePercentage") {
        brokenRoutePercentage = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "integrity") {
        integrity = integrityCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "inaccessibleRouteCount") {
        inaccessibleRouteCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "connectionCount") {
        connectionCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "center") {
        center = Some(latLonImplCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkDetailCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkDetail(
      km,
      meters,
      version,
      changeSetId,
      lastUpdated,
      relationLastUpdated,
      lastSurvey,
      tags,
      brokenRouteCount,
      brokenRoutePercentage,
      integrity,
      inaccessibleRouteCount,
      connectionCount,
      center,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkDetail, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("km")
    longCodec.encode(bsonWriter, value.km, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("version")
    longCodec.encode(bsonWriter, value.version, encoderContext)

    bsonWriter.writeName("changeSetId")
    longCodec.encode(bsonWriter, value.changeSetId, encoderContext)

    bsonWriter.writeName("lastUpdated")
    timestampCodec.encode(bsonWriter, value.lastUpdated, encoderContext)

    bsonWriter.writeName("relationLastUpdated")
    timestampCodec.encode(bsonWriter, value.relationLastUpdated, encoderContext)

    if (value.lastSurvey.isDefined) {
      bsonWriter.writeName("lastSurvey")
      dayCodec.encode(bsonWriter, value.lastSurvey.get, encoderContext)
    }

    bsonWriter.writeName("tags")
    bsonWriter.writeStartArray()
    value.tags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("brokenRouteCount")
    longCodec.encode(bsonWriter, value.brokenRouteCount, encoderContext)

    bsonWriter.writeName("brokenRoutePercentage")
    stringCodec.encode(bsonWriter, value.brokenRoutePercentage, encoderContext)

    bsonWriter.writeName("integrity")
    integrityCodec.encode(bsonWriter, value.integrity, encoderContext)

    bsonWriter.writeName("inaccessibleRouteCount")
    longCodec.encode(bsonWriter, value.inaccessibleRouteCount, encoderContext)

    bsonWriter.writeName("connectionCount")
    longCodec.encode(bsonWriter, value.connectionCount, encoderContext)

    if (value.center.isDefined) {
      bsonWriter.writeName("center")
      latLonImplCodec.encode(bsonWriter, value.center.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkDetail] = {
    classOf[NetworkDetail]
  }
}
