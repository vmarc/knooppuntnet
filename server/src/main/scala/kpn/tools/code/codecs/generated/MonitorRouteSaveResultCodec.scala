// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorRouteSaveResult
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteSaveResultCodec(registry: CodecRegistry) extends Codec[MonitorRouteSaveResult] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteSaveResult = {
    bsonReader.readStartDocument()

    var analyzed: Boolean = false
    var errors: Seq[String] = null
    var exception: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "analyzed") {
        analyzed = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "errors") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        errors = valueBuffer.toSeq
      }
      else if (fieldName == "exception") {
        exception = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteSaveResultCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteSaveResult(
      analyzed,
      errors,
      exception,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteSaveResult, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("analyzed")
    booleanCodec.encode(bsonWriter, value.analyzed, encoderContext)

    bsonWriter.writeName("errors")
    bsonWriter.writeStartArray()
    value.errors.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.exception.isDefined) {
      bsonWriter.writeName("exception")
      stringCodec.encode(bsonWriter, value.exception.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteSaveResult] = {
    classOf[MonitorRouteSaveResult]
  }
}
