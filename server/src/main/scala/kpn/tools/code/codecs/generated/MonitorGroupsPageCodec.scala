// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorGroupsPage
import kpn.api.common.monitor.MonitorGroupsPageGroup
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorGroupsPageCodec(registry: CodecRegistry) extends Codec[MonitorGroupsPage] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val monitorGroupsPageGroupCodec = registry.get(classOf[MonitorGroupsPageGroup])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorGroupsPage = {
    bsonReader.readStartDocument()

    var adminUser: Boolean = false
    var routeCount: Long = 0
    var groups: Seq[MonitorGroupsPageGroup] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "adminUser") {
        adminUser = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeCount") {
        routeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "groups") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[MonitorGroupsPageGroup]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += monitorGroupsPageGroupCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        groups = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorGroupsPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorGroupsPage(
      adminUser,
      routeCount,
      groups,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorGroupsPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("adminUser")
    booleanCodec.encode(bsonWriter, value.adminUser, encoderContext)

    bsonWriter.writeName("routeCount")
    longCodec.encode(bsonWriter, value.routeCount, encoderContext)

    bsonWriter.writeName("groups")
    bsonWriter.writeStartArray()
    value.groups.foreach(v => monitorGroupsPageGroupCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorGroupsPage] = {
    classOf[MonitorGroupsPage]
  }
}
