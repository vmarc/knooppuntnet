// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.route.GeometryDiffInfo
import kpn.api.common.route.GeometryDiffInfoDetail
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class GeometryDiffInfoCodec(registry: CodecRegistry) extends Codec[GeometryDiffInfo] {

  private val geometryDiffInfoDetailCodec = registry.get(classOf[GeometryDiffInfoDetail])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): GeometryDiffInfo = {
    bsonReader.readStartDocument()

    var common: GeometryDiffInfoDetail = null
    var added: GeometryDiffInfoDetail = null
    var removed: GeometryDiffInfoDetail = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "common") {
        common = geometryDiffInfoDetailCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "added") {
        added = geometryDiffInfoDetailCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "removed") {
        removed = geometryDiffInfoDetailCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in GeometryDiffInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    GeometryDiffInfo(
      common,
      added,
      removed,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: GeometryDiffInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("common")
    geometryDiffInfoDetailCodec.encode(bsonWriter, value.common, encoderContext)

    bsonWriter.writeName("added")
    geometryDiffInfoDetailCodec.encode(bsonWriter, value.added, encoderContext)

    bsonWriter.writeName("removed")
    geometryDiffInfoDetailCodec.encode(bsonWriter, value.removed, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[GeometryDiffInfo] = {
    classOf[GeometryDiffInfo]
  }
}
