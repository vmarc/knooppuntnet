// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.RouteLocationAnalysis
import kpn.api.common.location.Location
import kpn.api.common.location.LocationCandidate
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteLocationAnalysisCodec(registry: CodecRegistry) extends Codec[RouteLocationAnalysis] {

  private val locationCodec = registry.get(classOf[Location])
  private val locationCandidateCodec = registry.get(classOf[LocationCandidate])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteLocationAnalysis = {
    bsonReader.readStartDocument()

    var location: Option[Location] = None
    var candidates: Seq[LocationCandidate] = null
    var locationNames: Seq[String] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "location") {
        location = Some(locationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "candidates") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationCandidate]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationCandidateCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        candidates = valueBuffer.toSeq
      }
      else if (fieldName == "locationNames") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        locationNames = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in RouteLocationAnalysisCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteLocationAnalysis(
      location,
      candidates,
      locationNames,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteLocationAnalysis, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.location.isDefined) {
      bsonWriter.writeName("location")
      locationCodec.encode(bsonWriter, value.location.get, encoderContext)
    }

    bsonWriter.writeName("candidates")
    bsonWriter.writeStartArray()
    value.candidates.foreach(v => locationCandidateCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("locationNames")
    bsonWriter.writeStartArray()
    value.locationNames.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteLocationAnalysis] = {
    classOf[RouteLocationAnalysis]
  }
}
