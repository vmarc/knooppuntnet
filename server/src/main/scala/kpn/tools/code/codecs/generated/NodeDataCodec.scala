// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.data.Node
import kpn.api.common.diff.NodeData
import kpn.api.custom.Subset
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeDataCodec(registry: CodecRegistry) extends Codec[NodeData] {

  private val nodeCodec = registry.get(classOf[Node])
  private val stringCodec = registry.get(classOf[String])
  private val subsetCodec = registry.get(classOf[Subset])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeData = {
    bsonReader.readStartDocument()

    var subsets: Seq[Subset] = null
    var name: String = null
    var node: Node = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "subsets") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Subset]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += subsetCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        subsets = valueBuffer.toSeq
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "node") {
        node = nodeCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in NodeDataCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeData(
      subsets,
      name,
      node,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeData, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("subsets")
    bsonWriter.writeStartArray()
    value.subsets.foreach(v => subsetCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("node")
    nodeCodec.encode(bsonWriter, value.node, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeData] = {
    classOf[NodeData]
  }
}
