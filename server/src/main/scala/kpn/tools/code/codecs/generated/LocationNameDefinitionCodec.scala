// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.custom.Tag
import kpn.core.doc.LocationName
import kpn.core.doc.LocationPath
import kpn.core.tools.location.LocationNameDefinition
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationNameDefinitionCodec(registry: CodecRegistry) extends Codec[LocationNameDefinition] {

  private val locationNameCodec = registry.get(classOf[LocationName])
  private val locationPathCodec = registry.get(classOf[LocationPath])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val tagCodec = registry.get(classOf[Tag])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationNameDefinition = {
    bsonReader.readStartDocument()

    var id: String = null
    var relationId: Long = 0
    var paths: Seq[LocationPath] = null
    var name: String = null
    var names: Option[Seq[LocationName]] = None
    var tags: Seq[Tag] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "relationId") {
        relationId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "paths") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationPath]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationPathCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        paths = valueBuffer.toSeq
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "names") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationName]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationNameCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        names = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "tags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        tags = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationNameDefinitionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationNameDefinition(
      id,
      relationId,
      paths,
      name,
      names,
      tags,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationNameDefinition, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    stringCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("relationId")
    longCodec.encode(bsonWriter, value.relationId, encoderContext)

    bsonWriter.writeName("paths")
    bsonWriter.writeStartArray()
    value.paths.foreach(v => locationPathCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    if (value.names.isDefined) {
      bsonWriter.writeName("names")
      bsonWriter.writeStartArray()
      value.names.get.foreach(v => locationNameCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    bsonWriter.writeName("tags")
    bsonWriter.writeStartArray()
    value.tags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationNameDefinition] = {
    classOf[LocationNameDefinition]
  }
}
