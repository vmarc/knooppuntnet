// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.planner.PlanFragment
import kpn.api.common.planner.PlanSegment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PlanSegmentCodec(registry: CodecRegistry) extends Codec[PlanSegment] {

  private val longCodec = registry.get(classOf[Long])
  private val planFragmentCodec = registry.get(classOf[PlanFragment])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PlanSegment = {
    bsonReader.readStartDocument()

    var meters: Long = 0
    var surface: String = null
    var colour: Option[String] = None
    var fragments: Seq[PlanFragment] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "surface") {
        surface = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "colour") {
        colour = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "fragments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[PlanFragment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += planFragmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        fragments = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in PlanSegmentCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PlanSegment(
      meters,
      surface,
      colour,
      fragments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PlanSegment, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("surface")
    stringCodec.encode(bsonWriter, value.surface, encoderContext)

    if (value.colour.isDefined) {
      bsonWriter.writeName("colour")
      stringCodec.encode(bsonWriter, value.colour.get, encoderContext)
    }

    bsonWriter.writeName("fragments")
    bsonWriter.writeStartArray()
    value.fragments.foreach(v => planFragmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PlanSegment] = {
    classOf[PlanSegment]
  }
}
