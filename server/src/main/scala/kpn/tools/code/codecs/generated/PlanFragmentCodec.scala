// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LatLonImpl
import kpn.api.common.planner.PlanCoordinate
import kpn.api.common.planner.PlanFragment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PlanFragmentCodec(registry: CodecRegistry) extends Codec[PlanFragment] {

  private val latLonImplCodec = registry.get(classOf[LatLonImpl])
  private val longCodec = registry.get(classOf[Long])
  private val planCoordinateCodec = registry.get(classOf[PlanCoordinate])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PlanFragment = {
    bsonReader.readStartDocument()

    var meters: Long = 0
    var coordinate: PlanCoordinate = null
    var latLon: LatLonImpl = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "coordinate") {
        coordinate = planCoordinateCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "latLon") {
        latLon = latLonImplCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in PlanFragmentCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PlanFragment(
      meters,
      coordinate,
      latLon,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PlanFragment, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("coordinate")
    planCoordinateCodec.encode(bsonWriter, value.coordinate, encoderContext)

    bsonWriter.writeName("latLon")
    latLonImplCodec.encode(bsonWriter, value.latLon, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PlanFragment] = {
    classOf[PlanFragment]
  }
}
