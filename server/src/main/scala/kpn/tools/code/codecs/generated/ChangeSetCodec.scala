// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.ChangeSet
import kpn.api.custom.Change
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ChangeSetCodec(registry: CodecRegistry) extends Codec[ChangeSet] {

  private val changeCodec = registry.get(classOf[Change])
  private val longCodec = registry.get(classOf[Long])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ChangeSet = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var timestamp: Timestamp = null
    var timestampFrom: Timestamp = null
    var timestampUntil: Timestamp = null
    var timestampBefore: Timestamp = null
    var timestampAfter: Timestamp = null
    var changes: Seq[Change] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestamp") {
        timestamp = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestampFrom") {
        timestampFrom = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestampUntil") {
        timestampUntil = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestampBefore") {
        timestampBefore = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestampAfter") {
        timestampAfter = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Change]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += changeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        changes = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ChangeSetCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ChangeSet(
      id,
      timestamp,
      timestampFrom,
      timestampUntil,
      timestampBefore,
      timestampAfter,
      changes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ChangeSet, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("timestamp")
    timestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    bsonWriter.writeName("timestampFrom")
    timestampCodec.encode(bsonWriter, value.timestampFrom, encoderContext)

    bsonWriter.writeName("timestampUntil")
    timestampCodec.encode(bsonWriter, value.timestampUntil, encoderContext)

    bsonWriter.writeName("timestampBefore")
    timestampCodec.encode(bsonWriter, value.timestampBefore, encoderContext)

    bsonWriter.writeName("timestampAfter")
    timestampCodec.encode(bsonWriter, value.timestampAfter, encoderContext)

    bsonWriter.writeName("changes")
    bsonWriter.writeStartArray()
    value.changes.foreach(v => changeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ChangeSet] = {
    classOf[ChangeSet]
  }
}
