// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.TimeInfo
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TimeInfoCodec(registry: CodecRegistry) extends Codec[TimeInfo] {

  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TimeInfo = {
    bsonReader.readStartDocument()

    var now: Timestamp = null
    var lastWeekStart: Timestamp = null
    var lastMonthStart: Timestamp = null
    var lastYearStart: Timestamp = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "now") {
        now = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastWeekStart") {
        lastWeekStart = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastMonthStart") {
        lastMonthStart = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastYearStart") {
        lastYearStart = timestampCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in TimeInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TimeInfo(
      now,
      lastWeekStart,
      lastMonthStart,
      lastYearStart,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TimeInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("now")
    timestampCodec.encode(bsonWriter, value.now, encoderContext)

    bsonWriter.writeName("lastWeekStart")
    timestampCodec.encode(bsonWriter, value.lastWeekStart, encoderContext)

    bsonWriter.writeName("lastMonthStart")
    timestampCodec.encode(bsonWriter, value.lastMonthStart, encoderContext)

    bsonWriter.writeName("lastYearStart")
    timestampCodec.encode(bsonWriter, value.lastYearStart, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TimeInfo] = {
    classOf[TimeInfo]
  }
}
