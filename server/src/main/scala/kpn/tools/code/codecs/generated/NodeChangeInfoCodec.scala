// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.ChangeType
import kpn.api.common.Fact
import kpn.api.common.LatLonImpl
import kpn.api.common.changes.details.ChangeKey
import kpn.api.common.changes.details.RefBooleanChange
import kpn.api.common.common.Ref
import kpn.api.common.data.MetaData
import kpn.api.common.diff.TagDiffs
import kpn.api.common.diff.common.FactDiffs
import kpn.api.common.diff.node.NodeMoved
import kpn.api.common.node.NodeChangeInfo
import kpn.api.custom.Tag
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeChangeInfoCodec(registry: CodecRegistry) extends Codec[NodeChangeInfo] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val changeKeyCodec = registry.get(classOf[ChangeKey])
  private val changeTypeCodec = registry.get(classOf[ChangeType])
  private val factCodec = registry.get(classOf[Fact])
  private val factDiffsCodec = registry.get(classOf[FactDiffs])
  private val latLonImplCodec = registry.get(classOf[LatLonImpl])
  private val longCodec = registry.get(classOf[Long])
  private val metaDataCodec = registry.get(classOf[MetaData])
  private val nodeMovedCodec = registry.get(classOf[NodeMoved])
  private val refCodec = registry.get(classOf[Ref])
  private val refBooleanChangeCodec = registry.get(classOf[RefBooleanChange])
  private val stringCodec = registry.get(classOf[String])
  private val tagCodec = registry.get(classOf[Tag])
  private val tagDiffsCodec = registry.get(classOf[TagDiffs])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeChangeInfo = {
    bsonReader.readStartDocument()

    var rowIndex: Long = 0
    var id: Long = 0
    var version: Option[Long] = None
    var changeKey: ChangeKey = null
    var changeType: ChangeType = null
    var changeTags: Seq[Tag] = null
    var comment: Option[String] = None
    var before: Option[MetaData] = None
    var after: Option[MetaData] = None
    var connectionChanges: Seq[RefBooleanChange] = null
    var roleConnectionChanges: Seq[RefBooleanChange] = null
    var definedInNetworkChanges: Seq[RefBooleanChange] = null
    var tagDiffs: Option[TagDiffs] = None
    var nodeMoved: Option[NodeMoved] = None
    var addedToRoute: Seq[Ref] = null
    var removedFromRoute: Seq[Ref] = null
    var addedToNetwork: Seq[Ref] = null
    var removedFromNetwork: Seq[Ref] = null
    var factDiffs: Option[FactDiffs] = None
    var facts: Seq[Fact] = null
    var initialTags: Option[Seq[Tag]] = None
    var initialLatLon: Option[LatLonImpl] = None
    var happy: Boolean = false
    var investigate: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "rowIndex") {
        rowIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "version") {
        version = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "changeKey") {
        changeKey = changeKeyCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeType") {
        changeType = changeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeTags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        changeTags = valueBuffer.toSeq
      }
      else if (fieldName == "comment") {
        comment = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "before") {
        before = Some(metaDataCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "after") {
        after = Some(metaDataCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "connectionChanges") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RefBooleanChange]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refBooleanChangeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        connectionChanges = valueBuffer.toSeq
      }
      else if (fieldName == "roleConnectionChanges") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RefBooleanChange]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refBooleanChangeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        roleConnectionChanges = valueBuffer.toSeq
      }
      else if (fieldName == "definedInNetworkChanges") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RefBooleanChange]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refBooleanChangeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        definedInNetworkChanges = valueBuffer.toSeq
      }
      else if (fieldName == "tagDiffs") {
        tagDiffs = Some(tagDiffsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nodeMoved") {
        nodeMoved = Some(nodeMovedCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "addedToRoute") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Ref]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        addedToRoute = valueBuffer.toSeq
      }
      else if (fieldName == "removedFromRoute") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Ref]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        removedFromRoute = valueBuffer.toSeq
      }
      else if (fieldName == "addedToNetwork") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Ref]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        addedToNetwork = valueBuffer.toSeq
      }
      else if (fieldName == "removedFromNetwork") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Ref]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        removedFromNetwork = valueBuffer.toSeq
      }
      else if (fieldName == "factDiffs") {
        factDiffs = Some(factDiffsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "facts") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Fact]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += factCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        facts = valueBuffer.toSeq
      }
      else if (fieldName == "initialTags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        initialTags = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "initialLatLon") {
        initialLatLon = Some(latLonImplCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "happy") {
        happy = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "investigate") {
        investigate = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in NodeChangeInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeChangeInfo(
      rowIndex,
      id,
      version,
      changeKey,
      changeType,
      changeTags,
      comment,
      before,
      after,
      connectionChanges,
      roleConnectionChanges,
      definedInNetworkChanges,
      tagDiffs,
      nodeMoved,
      addedToRoute,
      removedFromRoute,
      addedToNetwork,
      removedFromNetwork,
      factDiffs,
      facts,
      initialTags,
      initialLatLon,
      happy,
      investigate,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeChangeInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("rowIndex")
    longCodec.encode(bsonWriter, value.rowIndex, encoderContext)

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    if (value.version.isDefined) {
      bsonWriter.writeName("version")
      longCodec.encode(bsonWriter, value.version.get, encoderContext)
    }

    bsonWriter.writeName("changeKey")
    changeKeyCodec.encode(bsonWriter, value.changeKey, encoderContext)

    bsonWriter.writeName("changeType")
    changeTypeCodec.encode(bsonWriter, value.changeType, encoderContext)

    bsonWriter.writeName("changeTags")
    bsonWriter.writeStartArray()
    value.changeTags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.comment.isDefined) {
      bsonWriter.writeName("comment")
      stringCodec.encode(bsonWriter, value.comment.get, encoderContext)
    }

    if (value.before.isDefined) {
      bsonWriter.writeName("before")
      metaDataCodec.encode(bsonWriter, value.before.get, encoderContext)
    }

    if (value.after.isDefined) {
      bsonWriter.writeName("after")
      metaDataCodec.encode(bsonWriter, value.after.get, encoderContext)
    }

    bsonWriter.writeName("connectionChanges")
    bsonWriter.writeStartArray()
    value.connectionChanges.foreach(v => refBooleanChangeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("roleConnectionChanges")
    bsonWriter.writeStartArray()
    value.roleConnectionChanges.foreach(v => refBooleanChangeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("definedInNetworkChanges")
    bsonWriter.writeStartArray()
    value.definedInNetworkChanges.foreach(v => refBooleanChangeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.tagDiffs.isDefined) {
      bsonWriter.writeName("tagDiffs")
      tagDiffsCodec.encode(bsonWriter, value.tagDiffs.get, encoderContext)
    }

    if (value.nodeMoved.isDefined) {
      bsonWriter.writeName("nodeMoved")
      nodeMovedCodec.encode(bsonWriter, value.nodeMoved.get, encoderContext)
    }

    bsonWriter.writeName("addedToRoute")
    bsonWriter.writeStartArray()
    value.addedToRoute.foreach(v => refCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("removedFromRoute")
    bsonWriter.writeStartArray()
    value.removedFromRoute.foreach(v => refCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("addedToNetwork")
    bsonWriter.writeStartArray()
    value.addedToNetwork.foreach(v => refCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("removedFromNetwork")
    bsonWriter.writeStartArray()
    value.removedFromNetwork.foreach(v => refCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.factDiffs.isDefined) {
      bsonWriter.writeName("factDiffs")
      factDiffsCodec.encode(bsonWriter, value.factDiffs.get, encoderContext)
    }

    bsonWriter.writeName("facts")
    bsonWriter.writeStartArray()
    value.facts.foreach(v => factCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.initialTags.isDefined) {
      bsonWriter.writeName("initialTags")
      bsonWriter.writeStartArray()
      value.initialTags.get.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    if (value.initialLatLon.isDefined) {
      bsonWriter.writeName("initialLatLon")
      latLonImplCodec.encode(bsonWriter, value.initialLatLon.get, encoderContext)
    }

    bsonWriter.writeName("happy")
    booleanCodec.encode(bsonWriter, value.happy, encoderContext)

    bsonWriter.writeName("investigate")
    booleanCodec.encode(bsonWriter, value.investigate, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeChangeInfo] = {
    classOf[NodeChangeInfo]
  }
}
