// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.tiles.ClientPoiDefinition
import kpn.api.common.tiles.ClientPoiGroupDefinition
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ClientPoiGroupDefinitionCodec(registry: CodecRegistry) extends Codec[ClientPoiGroupDefinition] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val clientPoiDefinitionCodec = registry.get(classOf[ClientPoiDefinition])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ClientPoiGroupDefinition = {
    bsonReader.readStartDocument()

    var name: String = null
    var enabledDefault: Boolean = false
    var poiDefinitions: Seq[ClientPoiDefinition] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "enabledDefault") {
        enabledDefault = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "poiDefinitions") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[ClientPoiDefinition]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += clientPoiDefinitionCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        poiDefinitions = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in ClientPoiGroupDefinitionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ClientPoiGroupDefinition(
      name,
      enabledDefault,
      poiDefinitions,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ClientPoiGroupDefinition, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("enabledDefault")
    booleanCodec.encode(bsonWriter, value.enabledDefault, encoderContext)

    bsonWriter.writeName("poiDefinitions")
    bsonWriter.writeStartArray()
    value.poiDefinitions.foreach(v => clientPoiDefinitionCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ClientPoiGroupDefinition] = {
    classOf[ClientPoiGroupDefinition]
  }
}
