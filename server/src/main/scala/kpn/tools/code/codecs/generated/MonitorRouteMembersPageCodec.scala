// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.RouteType
import kpn.api.common.monitor.MonitorReferenceType
import kpn.api.common.monitor.MonitorRouteMembersPage
import kpn.api.common.monitor.MonitorRouteSummary
import kpn.api.common.route.StructureRow
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteMembersPageCodec(registry: CodecRegistry) extends Codec[MonitorRouteMembersPage] {

  private val monitorReferenceTypeCodec = registry.get(classOf[MonitorReferenceType])
  private val monitorRouteSummaryCodec = registry.get(classOf[MonitorRouteSummary])
  private val routeTypeCodec = registry.get(classOf[RouteType])
  private val structureRowCodec = registry.get(classOf[StructureRow])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteMembersPage = {
    bsonReader.readStartDocument()

    var summary: MonitorRouteSummary = null
    var referenceType: MonitorReferenceType = null
    var routeTypes: Seq[RouteType] = null
    var structureRows: Seq[StructureRow] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "summary") {
        summary = monitorRouteSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "referenceType") {
        referenceType = monitorReferenceTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeTypes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteType]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeTypeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routeTypes = valueBuffer.toSeq
      }
      else if (fieldName == "structureRows") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[StructureRow]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += structureRowCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        structureRows = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteMembersPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteMembersPage(
      summary,
      referenceType,
      routeTypes,
      structureRows,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteMembersPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("summary")
    monitorRouteSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("referenceType")
    monitorReferenceTypeCodec.encode(bsonWriter, value.referenceType, encoderContext)

    bsonWriter.writeName("routeTypes")
    bsonWriter.writeStartArray()
    value.routeTypes.foreach(v => routeTypeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("structureRows")
    bsonWriter.writeStartArray()
    value.structureRows.foreach(v => structureRowCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteMembersPage] = {
    classOf[MonitorRouteMembersPage]
  }
}
