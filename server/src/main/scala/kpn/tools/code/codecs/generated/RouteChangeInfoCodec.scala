// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.ChangeType
import kpn.api.common.changes.ChangeSetInfo
import kpn.api.common.changes.details.ChangeKey
import kpn.api.common.data.MetaData
import kpn.api.common.diff.WayDiffsInfo
import kpn.api.common.diff.route.RouteDiff
import kpn.api.common.route.GeometryDiff
import kpn.api.common.route.RouteChangeInfo
import kpn.api.common.route.RouteNode
import kpn.api.common.route.RouteNodeChange
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteChangeInfoCodec(registry: CodecRegistry) extends Codec[RouteChangeInfo] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val boundsCodec = registry.get(classOf[Bounds])
  private val changeKeyCodec = registry.get(classOf[ChangeKey])
  private val changeSetInfoCodec = registry.get(classOf[ChangeSetInfo])
  private val changeTypeCodec = registry.get(classOf[ChangeType])
  private val geometryDiffCodec = registry.get(classOf[GeometryDiff])
  private val longCodec = registry.get(classOf[Long])
  private val metaDataCodec = registry.get(classOf[MetaData])
  private val routeDiffCodec = registry.get(classOf[RouteDiff])
  private val routeNodeCodec = registry.get(classOf[RouteNode])
  private val routeNodeChangeCodec = registry.get(classOf[RouteNodeChange])
  private val stringCodec = registry.get(classOf[String])
  private val wayDiffsInfoCodec = registry.get(classOf[WayDiffsInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteChangeInfo = {
    bsonReader.readStartDocument()

    var rowIndex: Long = 0
    var id: Long = 0
    var version: Long = 0
    var changeKey: ChangeKey = null
    var changeType: ChangeType = null
    var comment: Option[String] = None
    var before: Option[MetaData] = None
    var after: Option[MetaData] = None
    var diffs: RouteDiff = null
    var nodes: Seq[RouteNode] = null
    var nodeChanges: Seq[RouteNodeChange] = null
    var changeSetInfo: Option[ChangeSetInfo] = None
    var wayDiffs: Option[WayDiffsInfo] = None
    var geometryDiff: Option[GeometryDiff] = None
    var bounds: Option[Bounds] = None
    var happy: Boolean = false
    var investigate: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "rowIndex") {
        rowIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "version") {
        version = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeKey") {
        changeKey = changeKeyCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeType") {
        changeType = changeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "comment") {
        comment = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "before") {
        before = Some(metaDataCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "after") {
        after = Some(metaDataCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "diffs") {
        diffs = routeDiffCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNode]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNodeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodes = valueBuffer.toSeq
      }
      else if (fieldName == "nodeChanges") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNodeChange]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNodeChangeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodeChanges = valueBuffer.toSeq
      }
      else if (fieldName == "changeSetInfo") {
        changeSetInfo = Some(changeSetInfoCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "wayDiffs") {
        wayDiffs = Some(wayDiffsInfoCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "geometryDiff") {
        geometryDiff = Some(geometryDiffCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "bounds") {
        bounds = Some(boundsCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "happy") {
        happy = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "investigate") {
        investigate = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteChangeInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteChangeInfo(
      rowIndex,
      id,
      version,
      changeKey,
      changeType,
      comment,
      before,
      after,
      diffs,
      nodes,
      nodeChanges,
      changeSetInfo,
      wayDiffs,
      geometryDiff,
      bounds,
      happy,
      investigate,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteChangeInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("rowIndex")
    longCodec.encode(bsonWriter, value.rowIndex, encoderContext)

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("version")
    longCodec.encode(bsonWriter, value.version, encoderContext)

    bsonWriter.writeName("changeKey")
    changeKeyCodec.encode(bsonWriter, value.changeKey, encoderContext)

    bsonWriter.writeName("changeType")
    changeTypeCodec.encode(bsonWriter, value.changeType, encoderContext)

    if (value.comment.isDefined) {
      bsonWriter.writeName("comment")
      stringCodec.encode(bsonWriter, value.comment.get, encoderContext)
    }

    if (value.before.isDefined) {
      bsonWriter.writeName("before")
      metaDataCodec.encode(bsonWriter, value.before.get, encoderContext)
    }

    if (value.after.isDefined) {
      bsonWriter.writeName("after")
      metaDataCodec.encode(bsonWriter, value.after.get, encoderContext)
    }

    bsonWriter.writeName("diffs")
    routeDiffCodec.encode(bsonWriter, value.diffs, encoderContext)

    bsonWriter.writeName("nodes")
    bsonWriter.writeStartArray()
    value.nodes.foreach(v => routeNodeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("nodeChanges")
    bsonWriter.writeStartArray()
    value.nodeChanges.foreach(v => routeNodeChangeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.changeSetInfo.isDefined) {
      bsonWriter.writeName("changeSetInfo")
      changeSetInfoCodec.encode(bsonWriter, value.changeSetInfo.get, encoderContext)
    }

    if (value.wayDiffs.isDefined) {
      bsonWriter.writeName("wayDiffs")
      wayDiffsInfoCodec.encode(bsonWriter, value.wayDiffs.get, encoderContext)
    }

    if (value.geometryDiff.isDefined) {
      bsonWriter.writeName("geometryDiff")
      geometryDiffCodec.encode(bsonWriter, value.geometryDiff.get, encoderContext)
    }

    if (value.bounds.isDefined) {
      bsonWriter.writeName("bounds")
      boundsCodec.encode(bsonWriter, value.bounds.get, encoderContext)
    }

    bsonWriter.writeName("happy")
    booleanCodec.encode(bsonWriter, value.happy, encoderContext)

    bsonWriter.writeName("investigate")
    booleanCodec.encode(bsonWriter, value.investigate, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteChangeInfo] = {
    classOf[RouteChangeInfo]
  }
}
