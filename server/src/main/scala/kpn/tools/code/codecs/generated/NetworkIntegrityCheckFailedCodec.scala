// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.NetworkIntegrityCheckFailed
import kpn.api.common.NodeIntegrityCheck
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkIntegrityCheckFailedCodec(registry: CodecRegistry) extends Codec[NetworkIntegrityCheckFailed] {

  private val longCodec = registry.get(classOf[Long])
  private val nodeIntegrityCheckCodec = registry.get(classOf[NodeIntegrityCheck])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkIntegrityCheckFailed = {
    bsonReader.readStartDocument()

    var count: Long = 0
    var checks: Seq[NodeIntegrityCheck] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "count") {
        count = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "checks") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NodeIntegrityCheck]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += nodeIntegrityCheckCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        checks = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in NetworkIntegrityCheckFailedCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkIntegrityCheckFailed(
      count,
      checks,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkIntegrityCheckFailed, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("count")
    longCodec.encode(bsonWriter, value.count, encoderContext)

    bsonWriter.writeName("checks")
    bsonWriter.writeStartArray()
    value.checks.foreach(v => nodeIntegrityCheckCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkIntegrityCheckFailed] = {
    classOf[NetworkIntegrityCheckFailed]
  }
}
