// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LatLonImpl
import kpn.api.common.subset.SubsetMapNetwork
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class SubsetMapNetworkCodec(registry: CodecRegistry) extends Codec[SubsetMapNetwork] {

  private val latLonImplCodec = registry.get(classOf[LatLonImpl])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): SubsetMapNetwork = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var name: String = null
    var km: Long = 0
    var nodeCount: Long = 0
    var routeCount: Long = 0
    var center: LatLonImpl = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "km") {
        km = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeCount") {
        nodeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeCount") {
        routeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "center") {
        center = latLonImplCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in SubsetMapNetworkCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    SubsetMapNetwork(
      id,
      name,
      km,
      nodeCount,
      routeCount,
      center,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: SubsetMapNetwork, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("km")
    longCodec.encode(bsonWriter, value.km, encoderContext)

    bsonWriter.writeName("nodeCount")
    longCodec.encode(bsonWriter, value.nodeCount, encoderContext)

    bsonWriter.writeName("routeCount")
    longCodec.encode(bsonWriter, value.routeCount, encoderContext)

    bsonWriter.writeName("center")
    latLonImplCodec.encode(bsonWriter, value.center, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[SubsetMapNetwork] = {
    classOf[SubsetMapNetwork]
  }
}
