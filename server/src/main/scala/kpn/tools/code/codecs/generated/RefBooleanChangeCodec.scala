// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.details.RefBooleanChange
import kpn.api.common.common.Ref
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RefBooleanChangeCodec(registry: CodecRegistry) extends Codec[RefBooleanChange] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val refCodec = registry.get(classOf[Ref])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RefBooleanChange = {
    bsonReader.readStartDocument()

    var ref: Ref = null
    var after: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "ref") {
        ref = refCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "after") {
        after = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RefBooleanChangeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RefBooleanChange(
      ref,
      after,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RefBooleanChange, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("ref")
    refCodec.encode(bsonWriter, value.ref, encoderContext)

    bsonWriter.writeName("after")
    booleanCodec.encode(bsonWriter, value.after, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RefBooleanChange] = {
    classOf[RefBooleanChange]
  }
}
