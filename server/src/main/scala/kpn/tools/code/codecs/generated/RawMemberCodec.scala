// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.data.MemberType
import kpn.api.common.data.raw.RawMember
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RawMemberCodec(registry: CodecRegistry) extends Codec[RawMember] {

  private val longCodec = registry.get(classOf[Long])
  private val memberTypeCodec = registry.get(classOf[MemberType])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RawMember = {
    bsonReader.readStartDocument()

    var memberType: MemberType = null
    var ref: Long = 0
    var role: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "memberType") {
        memberType = memberTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "ref") {
        ref = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "role") {
        role = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in RawMemberCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RawMember(
      memberType,
      ref,
      role,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RawMember, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("memberType")
    memberTypeCodec.encode(bsonWriter, value.memberType, encoderContext)

    bsonWriter.writeName("ref")
    longCodec.encode(bsonWriter, value.ref, encoderContext)

    if (value.role.isDefined) {
      bsonWriter.writeName("role")
      stringCodec.encode(bsonWriter, value.role.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RawMember] = {
    classOf[RawMember]
  }
}
