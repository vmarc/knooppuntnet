// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.TimeInfo
import kpn.api.common.location.LocationRouteInfo
import kpn.api.common.location.LocationRouteOptions
import kpn.api.common.location.LocationRoutesPage
import kpn.api.common.location.LocationSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationRoutesPageCodec(registry: CodecRegistry) extends Codec[LocationRoutesPage] {

  private val locationRouteInfoCodec = registry.get(classOf[LocationRouteInfo])
  private val locationRouteOptionsCodec = registry.get(classOf[LocationRouteOptions])
  private val locationSummaryCodec = registry.get(classOf[LocationSummary])
  private val longCodec = registry.get(classOf[Long])
  private val timeInfoCodec = registry.get(classOf[TimeInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationRoutesPage = {
    bsonReader.readStartDocument()

    var timeInfo: TimeInfo = null
    var summary: LocationSummary = null
    var routeCount: Long = 0
    var filter: LocationRouteOptions = null
    var routes: Seq[LocationRouteInfo] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "timeInfo") {
        timeInfo = timeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "summary") {
        summary = locationSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeCount") {
        routeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "filter") {
        filter = locationRouteOptionsCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationRouteInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationRouteInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routes = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationRoutesPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationRoutesPage(
      timeInfo,
      summary,
      routeCount,
      filter,
      routes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationRoutesPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("timeInfo")
    timeInfoCodec.encode(bsonWriter, value.timeInfo, encoderContext)

    bsonWriter.writeName("summary")
    locationSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("routeCount")
    longCodec.encode(bsonWriter, value.routeCount, encoderContext)

    bsonWriter.writeName("filter")
    locationRouteOptionsCodec.encode(bsonWriter, value.filter, encoderContext)

    bsonWriter.writeName("routes")
    bsonWriter.writeStartArray()
    value.routes.foreach(v => locationRouteInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationRoutesPage] = {
    classOf[LocationRoutesPage]
  }
}
