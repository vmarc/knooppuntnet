// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.FeatureLayer
import kpn.api.common.RouteScope
import kpn.api.common.RouteType
import kpn.server.analyzer.engine.analysis.route.domain.RouteTileInfo
import kpn.server.analyzer.engine.analysis.route.domain.RouteTileSegment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteTileInfoCodec(registry: CodecRegistry) extends Codec[RouteTileInfo] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val featureLayerCodec = registry.get(classOf[FeatureLayer])
  private val longCodec = registry.get(classOf[Long])
  private val routeScopeCodec = registry.get(classOf[RouteScope])
  private val routeTileSegmentCodec = registry.get(classOf[RouteTileSegment])
  private val routeTypeCodec = registry.get(classOf[RouteType])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteTileInfo = {
    bsonReader.readStartDocument()

    var _id: String = null
    var routeId: Long = 0
    var routeName: String = null
    var routeTypes: Seq[RouteType] = null
    var z: Long = 0
    var x: Long = 0
    var y: Long = 0
    var layer: FeatureLayer = null
    var scope: Option[RouteScope] = None
    var survey: Option[String] = None
    var error: Option[String] = None
    var proposed: Boolean = false
    var segments: Seq[RouteTileSegment] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeId") {
        routeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeName") {
        routeName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeTypes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteType]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeTypeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routeTypes = valueBuffer.toSeq
      }
      else if (fieldName == "z") {
        z = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "x") {
        x = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "y") {
        y = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "layer") {
        layer = featureLayerCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "scope") {
        scope = Some(routeScopeCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "survey") {
        survey = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "error") {
        error = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "proposed") {
        proposed = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "segments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteTileSegment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeTileSegmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        segments = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in RouteTileInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteTileInfo(
      _id,
      routeId,
      routeName,
      routeTypes,
      z,
      x,
      y,
      layer,
      scope,
      survey,
      error,
      proposed,
      segments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteTileInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    stringCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("routeId")
    longCodec.encode(bsonWriter, value.routeId, encoderContext)

    bsonWriter.writeName("routeName")
    stringCodec.encode(bsonWriter, value.routeName, encoderContext)

    bsonWriter.writeName("routeTypes")
    bsonWriter.writeStartArray()
    value.routeTypes.foreach(v => routeTypeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("z")
    longCodec.encode(bsonWriter, value.z, encoderContext)

    bsonWriter.writeName("x")
    longCodec.encode(bsonWriter, value.x, encoderContext)

    bsonWriter.writeName("y")
    longCodec.encode(bsonWriter, value.y, encoderContext)

    bsonWriter.writeName("layer")
    featureLayerCodec.encode(bsonWriter, value.layer, encoderContext)

    if (value.scope.isDefined) {
      bsonWriter.writeName("scope")
      routeScopeCodec.encode(bsonWriter, value.scope.get, encoderContext)
    }

    if (value.survey.isDefined) {
      bsonWriter.writeName("survey")
      stringCodec.encode(bsonWriter, value.survey.get, encoderContext)
    }

    if (value.error.isDefined) {
      bsonWriter.writeName("error")
      stringCodec.encode(bsonWriter, value.error.get, encoderContext)
    }

    bsonWriter.writeName("proposed")
    booleanCodec.encode(bsonWriter, value.proposed, encoderContext)

    bsonWriter.writeName("segments")
    bsonWriter.writeStartArray()
    value.segments.foreach(v => routeTileSegmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteTileInfo] = {
    classOf[RouteTileInfo]
  }
}
