// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.TimeInfo
import kpn.api.common.location.LocationNodeInfo
import kpn.api.common.location.LocationNodeOptions
import kpn.api.common.location.LocationNodesPage
import kpn.api.common.location.LocationSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationNodesPageCodec(registry: CodecRegistry) extends Codec[LocationNodesPage] {

  private val locationNodeInfoCodec = registry.get(classOf[LocationNodeInfo])
  private val locationNodeOptionsCodec = registry.get(classOf[LocationNodeOptions])
  private val locationSummaryCodec = registry.get(classOf[LocationSummary])
  private val longCodec = registry.get(classOf[Long])
  private val timeInfoCodec = registry.get(classOf[TimeInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationNodesPage = {
    bsonReader.readStartDocument()

    var timeInfo: TimeInfo = null
    var summary: LocationSummary = null
    var nodeCount: Long = 0
    var filter: LocationNodeOptions = null
    var nodes: Seq[LocationNodeInfo] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "timeInfo") {
        timeInfo = timeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "summary") {
        summary = locationSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeCount") {
        nodeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "filter") {
        filter = locationNodeOptionsCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationNodeInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationNodeInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        nodes = valueBuffer.toSeq
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationNodesPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationNodesPage(
      timeInfo,
      summary,
      nodeCount,
      filter,
      nodes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationNodesPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("timeInfo")
    timeInfoCodec.encode(bsonWriter, value.timeInfo, encoderContext)

    bsonWriter.writeName("summary")
    locationSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("nodeCount")
    longCodec.encode(bsonWriter, value.nodeCount, encoderContext)

    bsonWriter.writeName("filter")
    locationNodeOptionsCodec.encode(bsonWriter, value.filter, encoderContext)

    bsonWriter.writeName("nodes")
    bsonWriter.writeStartArray()
    value.nodes.foreach(v => locationNodeInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationNodesPage] = {
    classOf[LocationNodesPage]
  }
}
