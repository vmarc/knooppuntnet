// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.common.TrackPath
import kpn.api.common.common.TrackSegment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TrackPathCodec(registry: CodecRegistry) extends Codec[TrackPath] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val trackSegmentCodec = registry.get(classOf[TrackSegment])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TrackPath = {
    bsonReader.readStartDocument()

    var pathId: Long = 0
    var startNodeId: Long = 0
    var endNodeId: Long = 0
    var meters: Long = 0
    var oneWay: Boolean = false
    var segments: Seq[TrackSegment] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "pathId") {
        pathId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "startNodeId") {
        startNodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "endNodeId") {
        endNodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "oneWay") {
        oneWay = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "segments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[TrackSegment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += trackSegmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        segments = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in TrackPathCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TrackPath(
      pathId,
      startNodeId,
      endNodeId,
      meters,
      oneWay,
      segments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TrackPath, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("pathId")
    longCodec.encode(bsonWriter, value.pathId, encoderContext)

    bsonWriter.writeName("startNodeId")
    longCodec.encode(bsonWriter, value.startNodeId, encoderContext)

    bsonWriter.writeName("endNodeId")
    longCodec.encode(bsonWriter, value.endNodeId, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("oneWay")
    booleanCodec.encode(bsonWriter, value.oneWay, encoderContext)

    bsonWriter.writeName("segments")
    bsonWriter.writeStartArray()
    value.segments.foreach(v => trackSegmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TrackPath] = {
    classOf[TrackPath]
  }
}
