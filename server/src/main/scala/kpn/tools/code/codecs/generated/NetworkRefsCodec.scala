// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Country
import kpn.api.common.RouteType
import kpn.api.common.common.NetworkRefs
import kpn.api.common.common.Ref
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkRefsCodec(registry: CodecRegistry) extends Codec[NetworkRefs] {

  private val countryCodec = registry.get(classOf[Country])
  private val refCodec = registry.get(classOf[Ref])
  private val routeTypeCodec = registry.get(classOf[RouteType])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkRefs = {
    bsonReader.readStartDocument()

    var country: Country = null
    var routeType: RouteType = null
    var networkRef: Option[Ref] = None
    var refType: String = null
    var refs: Seq[Ref] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "country") {
        country = countryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeType") {
        routeType = routeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "networkRef") {
        networkRef = Some(refCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "refType") {
        refType = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "refs") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Ref]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += refCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        refs = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkRefsCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkRefs(
      country,
      routeType,
      networkRef,
      refType,
      refs,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkRefs, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("country")
    countryCodec.encode(bsonWriter, value.country, encoderContext)

    bsonWriter.writeName("routeType")
    routeTypeCodec.encode(bsonWriter, value.routeType, encoderContext)

    if (value.networkRef.isDefined) {
      bsonWriter.writeName("networkRef")
      refCodec.encode(bsonWriter, value.networkRef.get, encoderContext)
    }

    bsonWriter.writeName("refType")
    stringCodec.encode(bsonWriter, value.refType, encoderContext)

    bsonWriter.writeName("refs")
    bsonWriter.writeStartArray()
    value.refs.foreach(v => refCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkRefs] = {
    classOf[NetworkRefs]
  }
}
