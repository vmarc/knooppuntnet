// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.tiles.ClientPoiDefinition
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ClientPoiDefinitionCodec(registry: CodecRegistry) extends Codec[ClientPoiDefinition] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ClientPoiDefinition = {
    bsonReader.readStartDocument()

    var name: String = null
    var icon: String = null
    var minLevel: Long = 0
    var defaultLevel: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "icon") {
        icon = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "minLevel") {
        minLevel = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "defaultLevel") {
        defaultLevel = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in ClientPoiDefinitionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ClientPoiDefinition(
      name,
      icon,
      minLevel,
      defaultLevel,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ClientPoiDefinition, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("icon")
    stringCodec.encode(bsonWriter, value.icon, encoderContext)

    bsonWriter.writeName("minLevel")
    longCodec.encode(bsonWriter, value.minLevel, encoderContext)

    bsonWriter.writeName("defaultLevel")
    longCodec.encode(bsonWriter, value.defaultLevel, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ClientPoiDefinition] = {
    classOf[ClientPoiDefinition]
  }
}
