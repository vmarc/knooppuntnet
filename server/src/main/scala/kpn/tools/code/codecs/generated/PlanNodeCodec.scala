// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LatLonImpl
import kpn.api.common.planner.PlanCoordinate
import kpn.api.common.planner.PlanNode
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PlanNodeCodec(registry: CodecRegistry) extends Codec[PlanNode] {

  private val latLonImplCodec = registry.get(classOf[LatLonImpl])
  private val planCoordinateCodec = registry.get(classOf[PlanCoordinate])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PlanNode = {
    bsonReader.readStartDocument()

    var featureId: String = null
    var nodeId: String = null
    var nodeName: String = null
    var nodeLongName: Option[String] = None
    var coordinate: PlanCoordinate = null
    var latLon: LatLonImpl = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "featureId") {
        featureId = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeId") {
        nodeId = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeName") {
        nodeName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeLongName") {
        nodeLongName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "coordinate") {
        coordinate = planCoordinateCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "latLon") {
        latLon = latLonImplCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in PlanNodeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PlanNode(
      featureId,
      nodeId,
      nodeName,
      nodeLongName,
      coordinate,
      latLon,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PlanNode, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("featureId")
    stringCodec.encode(bsonWriter, value.featureId, encoderContext)

    bsonWriter.writeName("nodeId")
    stringCodec.encode(bsonWriter, value.nodeId, encoderContext)

    bsonWriter.writeName("nodeName")
    stringCodec.encode(bsonWriter, value.nodeName, encoderContext)

    if (value.nodeLongName.isDefined) {
      bsonWriter.writeName("nodeLongName")
      stringCodec.encode(bsonWriter, value.nodeLongName.get, encoderContext)
    }

    bsonWriter.writeName("coordinate")
    planCoordinateCodec.encode(bsonWriter, value.coordinate, encoderContext)

    bsonWriter.writeName("latLon")
    latLonImplCodec.encode(bsonWriter, value.latLon, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PlanNode] = {
    classOf[PlanNode]
  }
}
