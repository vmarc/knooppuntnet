// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.network.Integrity
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class IntegrityCodec(registry: CodecRegistry) extends Codec[Integrity] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): Integrity = {
    bsonReader.readStartDocument()

    var isOk: Boolean = false
    var hasChecks: Boolean = false
    var count: Long = 0
    var okCount: Long = 0
    var nokCount: Long = 0
    var coverage: String = null
    var okRate: String = null
    var nokRate: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "isOk") {
        isOk = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "hasChecks") {
        hasChecks = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "count") {
        // TODO count is a string in the database, but a long in the API. Cleanup after regenerating network docs.
        val countString = stringCodec.decode(bsonReader, decoderContext)
        if (countString.forall(_.isDigit)) {
          count = countString.toLong
        }
      }
      else if (fieldName == "okCount") {
        okCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nokCount") {
        nokCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "coverage") {
        coverage = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "okRate") {
        okRate = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nokRate") {
        nokRate = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in IntegrityCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    Integrity(
      isOk,
      hasChecks,
      count,
      okCount,
      nokCount,
      coverage,
      okRate,
      nokRate,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: Integrity, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("isOk")
    booleanCodec.encode(bsonWriter, value.isOk, encoderContext)

    bsonWriter.writeName("hasChecks")
    booleanCodec.encode(bsonWriter, value.hasChecks, encoderContext)

    bsonWriter.writeName("count")
    longCodec.encode(bsonWriter, value.count, encoderContext)

    bsonWriter.writeName("okCount")
    longCodec.encode(bsonWriter, value.okCount, encoderContext)

    bsonWriter.writeName("nokCount")
    longCodec.encode(bsonWriter, value.nokCount, encoderContext)

    bsonWriter.writeName("coverage")
    stringCodec.encode(bsonWriter, value.coverage, encoderContext)

    bsonWriter.writeName("okRate")
    stringCodec.encode(bsonWriter, value.okRate, encoderContext)

    bsonWriter.writeName("nokRate")
    stringCodec.encode(bsonWriter, value.nokRate, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[Integrity] = {
    classOf[Integrity]
  }
}
