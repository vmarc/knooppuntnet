// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.NodeIntegrityCheck
import kpn.api.common.NodeIntegrityCheckChange
import kpn.api.common.RouteType
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeIntegrityCheckChangeCodec(registry: CodecRegistry) extends Codec[NodeIntegrityCheckChange] {

  private val nodeIntegrityCheckCodec = registry.get(classOf[NodeIntegrityCheck])
  private val routeTypeCodec = registry.get(classOf[RouteType])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeIntegrityCheckChange = {
    bsonReader.readStartDocument()

    var routeType: RouteType = null
    var before: Option[NodeIntegrityCheck] = None
    var after: Option[NodeIntegrityCheck] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "routeType") {
        routeType = routeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "before") {
        before = Some(nodeIntegrityCheckCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "after") {
        after = Some(nodeIntegrityCheckCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NodeIntegrityCheckChangeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeIntegrityCheckChange(
      routeType,
      before,
      after,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeIntegrityCheckChange, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("routeType")
    routeTypeCodec.encode(bsonWriter, value.routeType, encoderContext)

    if (value.before.isDefined) {
      bsonWriter.writeName("before")
      nodeIntegrityCheckCodec.encode(bsonWriter, value.before.get, encoderContext)
    }

    if (value.after.isDefined) {
      bsonWriter.writeName("after")
      nodeIntegrityCheckCodec.encode(bsonWriter, value.after.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeIntegrityCheckChange] = {
    classOf[NodeIntegrityCheckChange]
  }
}
