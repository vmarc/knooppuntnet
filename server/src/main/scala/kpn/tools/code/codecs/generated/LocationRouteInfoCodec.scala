// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.location.LocationRouteInfo
import kpn.api.custom.Day
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationRouteInfoCodec(registry: CodecRegistry) extends Codec[LocationRouteInfo] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val dayCodec = registry.get(classOf[Day])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationRouteInfo = {
    bsonReader.readStartDocument()

    var rowIndex: Long = 0
    var id: Long = 0
    var name: String = null
    var meters: Long = 0
    var lastUpdated: Timestamp = null
    var lastSurvey: Option[Day] = None
    var symbol: Option[String] = None
    var broken: Boolean = false
    var inaccessible: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "rowIndex") {
        rowIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastUpdated") {
        lastUpdated = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastSurvey") {
        lastSurvey = Some(dayCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "symbol") {
        symbol = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "broken") {
        broken = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "inaccessible") {
        inaccessible = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in LocationRouteInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationRouteInfo(
      rowIndex,
      id,
      name,
      meters,
      lastUpdated,
      lastSurvey,
      symbol,
      broken,
      inaccessible,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationRouteInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("rowIndex")
    longCodec.encode(bsonWriter, value.rowIndex, encoderContext)

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("lastUpdated")
    timestampCodec.encode(bsonWriter, value.lastUpdated, encoderContext)

    if (value.lastSurvey.isDefined) {
      bsonWriter.writeName("lastSurvey")
      dayCodec.encode(bsonWriter, value.lastSurvey.get, encoderContext)
    }

    if (value.symbol.isDefined) {
      bsonWriter.writeName("symbol")
      stringCodec.encode(bsonWriter, value.symbol.get, encoderContext)
    }

    bsonWriter.writeName("broken")
    booleanCodec.encode(bsonWriter, value.broken, encoderContext)

    bsonWriter.writeName("inaccessible")
    booleanCodec.encode(bsonWriter, value.inaccessible, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationRouteInfo] = {
    classOf[LocationRouteInfo]
  }
}
