// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.RouteType
import kpn.api.common.SurveyDateInfo
import kpn.api.common.TimeInfo
import kpn.api.common.network.NetworkRouteRow
import kpn.api.common.network.NetworkRoutesPage
import kpn.api.common.network.NetworkSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkRoutesPageCodec(registry: CodecRegistry) extends Codec[NetworkRoutesPage] {

  private val networkRouteRowCodec = registry.get(classOf[NetworkRouteRow])
  private val networkSummaryCodec = registry.get(classOf[NetworkSummary])
  private val routeTypeCodec = registry.get(classOf[RouteType])
  private val surveyDateInfoCodec = registry.get(classOf[SurveyDateInfo])
  private val timeInfoCodec = registry.get(classOf[TimeInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkRoutesPage = {
    bsonReader.readStartDocument()

    var timeInfo: TimeInfo = null
    var surveyDateInfo: SurveyDateInfo = null
    var routeType: RouteType = null
    var summary: NetworkSummary = null
    var routes: Seq[NetworkRouteRow] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "timeInfo") {
        timeInfo = timeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "surveyDateInfo") {
        surveyDateInfo = surveyDateInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeType") {
        routeType = routeTypeCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "summary") {
        summary = networkSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NetworkRouteRow]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += networkRouteRowCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routes = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkRoutesPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkRoutesPage(
      timeInfo,
      surveyDateInfo,
      routeType,
      summary,
      routes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkRoutesPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("timeInfo")
    timeInfoCodec.encode(bsonWriter, value.timeInfo, encoderContext)

    bsonWriter.writeName("surveyDateInfo")
    surveyDateInfoCodec.encode(bsonWriter, value.surveyDateInfo, encoderContext)

    bsonWriter.writeName("routeType")
    routeTypeCodec.encode(bsonWriter, value.routeType, encoderContext)

    bsonWriter.writeName("summary")
    networkSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("routes")
    bsonWriter.writeStartArray()
    value.routes.foreach(v => networkRouteRowCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkRoutesPage] = {
    classOf[NetworkRoutesPage]
  }
}
