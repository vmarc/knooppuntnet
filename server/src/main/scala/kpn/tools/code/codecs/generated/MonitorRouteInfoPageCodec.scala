// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorRouteInfoPage
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteInfoPageCodec(registry: CodecRegistry) extends Codec[MonitorRouteInfoPage] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteInfoPage = {
    bsonReader.readStartDocument()

    var relationId: Long = 0
    var active: Boolean = false
    var hasRouteTags: Boolean = false
    var name: Option[String] = None
    var ref: Option[String] = None
    var from: Option[String] = None
    var to: Option[String] = None
    var operator: Option[String] = None
    var website: Option[String] = None
    var symbol: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "relationId") {
        relationId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "active") {
        active = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "hasRouteTags") {
        hasRouteTags = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "ref") {
        ref = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "from") {
        from = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "to") {
        to = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "operator") {
        operator = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "website") {
        website = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "symbol") {
        symbol = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteInfoPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteInfoPage(
      relationId,
      active,
      hasRouteTags,
      name,
      ref,
      from,
      to,
      operator,
      website,
      symbol,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteInfoPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("relationId")
    longCodec.encode(bsonWriter, value.relationId, encoderContext)

    bsonWriter.writeName("active")
    booleanCodec.encode(bsonWriter, value.active, encoderContext)

    bsonWriter.writeName("hasRouteTags")
    booleanCodec.encode(bsonWriter, value.hasRouteTags, encoderContext)

    if (value.name.isDefined) {
      bsonWriter.writeName("name")
      stringCodec.encode(bsonWriter, value.name.get, encoderContext)
    }

    if (value.ref.isDefined) {
      bsonWriter.writeName("ref")
      stringCodec.encode(bsonWriter, value.ref.get, encoderContext)
    }

    if (value.from.isDefined) {
      bsonWriter.writeName("from")
      stringCodec.encode(bsonWriter, value.from.get, encoderContext)
    }

    if (value.to.isDefined) {
      bsonWriter.writeName("to")
      stringCodec.encode(bsonWriter, value.to.get, encoderContext)
    }

    if (value.operator.isDefined) {
      bsonWriter.writeName("operator")
      stringCodec.encode(bsonWriter, value.operator.get, encoderContext)
    }

    if (value.website.isDefined) {
      bsonWriter.writeName("website")
      stringCodec.encode(bsonWriter, value.website.get, encoderContext)
    }

    if (value.symbol.isDefined) {
      bsonWriter.writeName("symbol")
      stringCodec.encode(bsonWriter, value.symbol.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteInfoPage] = {
    classOf[MonitorRouteInfoPage]
  }
}
