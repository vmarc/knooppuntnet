// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.details.ChangeKey
import kpn.api.common.monitor.MonitorRouteChangeSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteChangeSummaryCodec(registry: CodecRegistry) extends Codec[MonitorRouteChangeSummary] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val changeKeyCodec = registry.get(classOf[ChangeKey])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteChangeSummary = {
    bsonReader.readStartDocument()

    var key: ChangeKey = null
    var groupName: Option[String] = None
    var routeName: Option[String] = None
    var groupDescription: Option[String] = None
    var comment: Option[String] = None
    var wayCount: Long = 0
    var waysAdded: Long = 0
    var waysRemoved: Long = 0
    var waysUpdated: Long = 0
    var osmDistance: Long = 0
    var routeSegmentCount: Long = 0
    var newNokSegmentCount: Long = 0
    var resolvedNokSegmentCount: Long = 0
    var happy: Boolean = false
    var investigate: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "key") {
        key = changeKeyCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "groupName") {
        groupName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "routeName") {
        routeName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "groupDescription") {
        groupDescription = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "comment") {
        comment = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "wayCount") {
        wayCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "waysAdded") {
        waysAdded = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "waysRemoved") {
        waysRemoved = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "waysUpdated") {
        waysUpdated = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "osmDistance") {
        osmDistance = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeSegmentCount") {
        routeSegmentCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "newNokSegmentCount") {
        newNokSegmentCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "resolvedNokSegmentCount") {
        resolvedNokSegmentCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "happy") {
        happy = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "investigate") {
        investigate = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.log.warn(s"Unknown field name: $fieldName in MonitorRouteChangeSummaryCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteChangeSummary(
      key,
      groupName,
      routeName,
      groupDescription,
      comment,
      wayCount,
      waysAdded,
      waysRemoved,
      waysUpdated,
      osmDistance,
      routeSegmentCount,
      newNokSegmentCount,
      resolvedNokSegmentCount,
      happy,
      investigate,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteChangeSummary, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("key")
    changeKeyCodec.encode(bsonWriter, value.key, encoderContext)

    if (value.groupName.isDefined) {
      bsonWriter.writeName("groupName")
      stringCodec.encode(bsonWriter, value.groupName.get, encoderContext)
    }

    if (value.routeName.isDefined) {
      bsonWriter.writeName("routeName")
      stringCodec.encode(bsonWriter, value.routeName.get, encoderContext)
    }

    if (value.groupDescription.isDefined) {
      bsonWriter.writeName("groupDescription")
      stringCodec.encode(bsonWriter, value.groupDescription.get, encoderContext)
    }

    if (value.comment.isDefined) {
      bsonWriter.writeName("comment")
      stringCodec.encode(bsonWriter, value.comment.get, encoderContext)
    }

    bsonWriter.writeName("wayCount")
    longCodec.encode(bsonWriter, value.wayCount, encoderContext)

    bsonWriter.writeName("waysAdded")
    longCodec.encode(bsonWriter, value.waysAdded, encoderContext)

    bsonWriter.writeName("waysRemoved")
    longCodec.encode(bsonWriter, value.waysRemoved, encoderContext)

    bsonWriter.writeName("waysUpdated")
    longCodec.encode(bsonWriter, value.waysUpdated, encoderContext)

    bsonWriter.writeName("osmDistance")
    longCodec.encode(bsonWriter, value.osmDistance, encoderContext)

    bsonWriter.writeName("routeSegmentCount")
    longCodec.encode(bsonWriter, value.routeSegmentCount, encoderContext)

    bsonWriter.writeName("newNokSegmentCount")
    longCodec.encode(bsonWriter, value.newNokSegmentCount, encoderContext)

    bsonWriter.writeName("resolvedNokSegmentCount")
    longCodec.encode(bsonWriter, value.resolvedNokSegmentCount, encoderContext)

    bsonWriter.writeName("happy")
    booleanCodec.encode(bsonWriter, value.happy, encoderContext)

    bsonWriter.writeName("investigate")
    booleanCodec.encode(bsonWriter, value.investigate, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteChangeSummary] = {
    classOf[MonitorRouteChangeSummary]
  }
}
