// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.search.Condition
import kpn.api.common.search.ConditionGroup
import kpn.api.common.search.ConditionLocation
import kpn.api.common.search.ConditionName
import kpn.api.common.search.ConditionTag
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ConditionCodec(registry: CodecRegistry) extends Codec[Condition] {

  private val conditionGroupCodec = registry.get(classOf[ConditionGroup])
  private val conditionLocationCodec = registry.get(classOf[ConditionLocation])
  private val conditionNameCodec = registry.get(classOf[ConditionName])
  private val conditionTagCodec = registry.get(classOf[ConditionTag])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): Condition = {
    bsonReader.readStartDocument()

    var tag: Option[ConditionTag] = None
    var location: Option[ConditionLocation] = None
    var name: Option[ConditionName] = None
    var group: Option[ConditionGroup] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "tag") {
        tag = Some(conditionTagCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "location") {
        location = Some(conditionLocationCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "name") {
        name = Some(conditionNameCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "group") {
        group = Some(conditionGroupCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ConditionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    Condition(
      tag,
      location,
      name,
      group,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: Condition, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.tag.isDefined) {
      bsonWriter.writeName("tag")
      conditionTagCodec.encode(bsonWriter, value.tag.get, encoderContext)
    }

    if (value.location.isDefined) {
      bsonWriter.writeName("location")
      conditionLocationCodec.encode(bsonWriter, value.location.get, encoderContext)
    }

    if (value.name.isDefined) {
      bsonWriter.writeName("name")
      conditionNameCodec.encode(bsonWriter, value.name.get, encoderContext)
    }

    if (value.group.isDefined) {
      bsonWriter.writeName("group")
      conditionGroupCodec.encode(bsonWriter, value.group.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[Condition] = {
    classOf[Condition]
  }
}
