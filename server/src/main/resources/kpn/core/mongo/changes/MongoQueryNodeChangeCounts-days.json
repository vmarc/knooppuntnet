[
  {
    "$match": {
      "nodeChange.key.elementId": @nodeId,
      "nodeChange.key.time.year": @year,
      "nodeChange.key.time.month": @month
    }
  },
  {
    "$group": {
      "_id": {
        "nodeId": "$nodeChange.key.elementId",
        "year": "$nodeChange.key.time.year",
        "monMongoQuerynodeChangeCountsth": "$nodeChange.key.time.month",
        "day": "$nodeChange.key.time.day",
        "impact": "$nodeChange.impact"
      },
      "count": {
        "$sum": 1
      }
    }
  },
  {
    "$project": {
      "_id": 0,
      "year": "$_id.year",
      "month": "$_id.month",
      "day": "$_id.day",
      "impact": {
        "$cond": {
          "if": "$_id.impact",
          "then": "$count",
          "else": 0
        }
      },
      "total": "$count"
    }
  },
  {
    "$group": {
      "_id": {
        "year": "$year",
        "month": "$month",
        "day": "$day"
      },
      "impact": {
        "$sum": "$impact"
      },
      "total": {
        "$sum": "$total"
      }
    }
  },
  {
    "$sort": {
      "_id": -1
    }
  },
  {
    "$project": {
      "_id": 0,
      "year": "$_id.year",
      "month": "$_id.month",
      "day": "$_id.day",
      "impact": "$impact",
      "total": "$total"
    }
  }
]
