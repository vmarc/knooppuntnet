// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.planner.LegEnd
import kpn.api.common.planner.LegEndNode
import kpn.api.common.planner.LegEndRoute
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LegEndCodec(registry: CodecRegistry) extends Codec[LegEnd] {

  private val legEndNodeCodec = registry.get(classOf[LegEndNode])
  private val legEndRouteCodec = registry.get(classOf[LegEndRoute])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LegEnd = {
    bsonReader.readStartDocument()

    var node: Option[LegEndNode] = None
    var route: Option[LegEndRoute] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "node") {
        node = Some(legEndNodeCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "route") {
        route = Some(legEndRouteCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LegEndCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LegEnd(
      node,
      route,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LegEnd, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.node.isDefined) {
      bsonWriter.writeName("node")
      legEndNodeCodec.encode(bsonWriter, value.node.get, encoderContext)
    }

    if (value.route.isDefined) {
      bsonWriter.writeName("route")
      legEndRouteCodec.encode(bsonWriter, value.route.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LegEnd] = {
    classOf[LegEnd]
  }
}
