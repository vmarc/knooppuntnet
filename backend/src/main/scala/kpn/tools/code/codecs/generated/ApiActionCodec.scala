// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.status.ActionTimestamp
import kpn.core.metrics.ApiAction
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ApiActionCodec(registry: CodecRegistry) extends Codec[ApiAction] {

  private val actionTimestampCodec = registry.get(classOf[ActionTimestamp])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ApiAction = {
    bsonReader.readStartDocument()

    var remoteAddress: String = null
    var userAgentString: Option[String] = None
    var deviceName: Option[String] = None
    var deviceClass: Option[String] = None
    var user: Option[String] = None
    var name: String = null
    var args: String = null
    var timestamp: ActionTimestamp = null
    var elapsed: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "remoteAddress") {
        remoteAddress = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "userAgentString") {
        userAgentString = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "deviceName") {
        deviceName = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "deviceClass") {
        deviceClass = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "user") {
        user = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "args") {
        args = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestamp") {
        timestamp = actionTimestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "elapsed") {
        elapsed = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ApiActionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ApiAction(
      remoteAddress,
      userAgentString,
      deviceName,
      deviceClass,
      user,
      name,
      args,
      timestamp,
      elapsed,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ApiAction, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("remoteAddress")
    stringCodec.encode(bsonWriter, value.remoteAddress, encoderContext)

    if (value.userAgentString.isDefined) {
      bsonWriter.writeName("userAgentString")
      stringCodec.encode(bsonWriter, value.userAgentString.get, encoderContext)
    }

    if (value.deviceName.isDefined) {
      bsonWriter.writeName("deviceName")
      stringCodec.encode(bsonWriter, value.deviceName.get, encoderContext)
    }

    if (value.deviceClass.isDefined) {
      bsonWriter.writeName("deviceClass")
      stringCodec.encode(bsonWriter, value.deviceClass.get, encoderContext)
    }

    if (value.user.isDefined) {
      bsonWriter.writeName("user")
      stringCodec.encode(bsonWriter, value.user.get, encoderContext)
    }

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("args")
    stringCodec.encode(bsonWriter, value.args, encoderContext)

    bsonWriter.writeName("timestamp")
    actionTimestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    bsonWriter.writeName("elapsed")
    longCodec.encode(bsonWriter, value.elapsed, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ApiAction] = {
    classOf[ApiAction]
  }
}
