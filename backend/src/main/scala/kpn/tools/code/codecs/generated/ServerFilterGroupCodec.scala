// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.filter.ServerFilterGroup
import kpn.api.common.changes.filter.ServerFilterOption
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ServerFilterGroupCodec(registry: CodecRegistry) extends Codec[ServerFilterGroup] {

  private val serverFilterOptionCodec = registry.get(classOf[ServerFilterOption])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ServerFilterGroup = {
    bsonReader.readStartDocument()

    var selected: String = null
    var options: Seq[ServerFilterOption] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "selected") {
        selected = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "options") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[ServerFilterOption]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += serverFilterOptionCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        options = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ServerFilterGroupCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ServerFilterGroup(
      selected,
      options,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ServerFilterGroup, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("selected")
    stringCodec.encode(bsonWriter, value.selected, encoderContext)

    bsonWriter.writeName("options")
    bsonWriter.writeStartArray()
    value.options.foreach(v => serverFilterOptionCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ServerFilterGroup] = {
    classOf[ServerFilterGroup]
  }
}
