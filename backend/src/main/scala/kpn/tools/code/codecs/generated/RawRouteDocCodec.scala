// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Relation
import kpn.core.doc.RawRouteDoc
import kpn.core.doc.RouteRelation
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RawRouteDocCodec(registry: CodecRegistry) extends Codec[RawRouteDoc] {

  private val longCodec = registry.get(classOf[Long])
  private val relationCodec = registry.get(classOf[Relation])
  private val routeRelationCodec = registry.get(classOf[RouteRelation])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RawRouteDoc = {
    bsonReader.readStartDocument()

    var _id: Long = 0
    var relation: Relation = null
    var subRelationTree: Option[RouteRelation] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "relation") {
        relation = relationCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "subRelationTree") {
        subRelationTree = Some(routeRelationCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RawRouteDocCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RawRouteDoc(
      _id,
      relation,
      subRelationTree,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RawRouteDoc, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    longCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("relation")
    relationCodec.encode(bsonWriter, value.relation, encoderContext)

    if (value.subRelationTree.isDefined) {
      bsonWriter.writeName("subRelationTree")
      routeRelationCodec.encode(bsonWriter, value.subRelationTree.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RawRouteDoc] = {
    classOf[RawRouteDoc]
  }
}
