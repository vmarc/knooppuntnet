// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.planner.LegEnd
import kpn.api.common.planner.PlanLegDetail
import kpn.api.common.planner.PlanRoute
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PlanLegDetailCodec(registry: CodecRegistry) extends Codec[PlanLegDetail] {

  private val legEndCodec = registry.get(classOf[LegEnd])
  private val planRouteCodec = registry.get(classOf[PlanRoute])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PlanLegDetail = {
    bsonReader.readStartDocument()

    var source: LegEnd = null
    var sink: LegEnd = null
    var routes: Seq[PlanRoute] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "source") {
        source = legEndCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "sink") {
        sink = legEndCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[PlanRoute]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += planRouteCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        routes = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in PlanLegDetailCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PlanLegDetail(
      source,
      sink,
      routes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PlanLegDetail, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("source")
    legEndCodec.encode(bsonWriter, value.source, encoderContext)

    bsonWriter.writeName("sink")
    legEndCodec.encode(bsonWriter, value.sink, encoderContext)

    bsonWriter.writeName("routes")
    bsonWriter.writeStartArray()
    value.routes.foreach(v => planRouteCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PlanLegDetail] = {
    classOf[PlanLegDetail]
  }
}
