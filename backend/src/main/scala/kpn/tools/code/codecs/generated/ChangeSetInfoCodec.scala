// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.ChangeSetInfo
import kpn.api.custom.Tag
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ChangeSetInfoCodec(registry: CodecRegistry) extends Codec[ChangeSetInfo] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val tagCodec = registry.get(classOf[Tag])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ChangeSetInfo = {
    bsonReader.readStartDocument()

    var _id: Long = 0
    var id: Long = 0
    var createdAt: Timestamp = null
    var closedAt: Option[Timestamp] = None
    var open: Boolean = false
    var commentsCount: Long = 0
    var tags: Seq[Tag] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "createdAt") {
        createdAt = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "closedAt") {
        closedAt = Some(timestampCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "open") {
        open = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "commentsCount") {
        commentsCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "tags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        tags = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ChangeSetInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ChangeSetInfo(
      _id,
      id,
      createdAt,
      closedAt,
      open,
      commentsCount,
      tags,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ChangeSetInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    longCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("createdAt")
    timestampCodec.encode(bsonWriter, value.createdAt, encoderContext)

    if (value.closedAt.isDefined) {
      bsonWriter.writeName("closedAt")
      timestampCodec.encode(bsonWriter, value.closedAt.get, encoderContext)
    }

    bsonWriter.writeName("open")
    booleanCodec.encode(bsonWriter, value.open, encoderContext)

    bsonWriter.writeName("commentsCount")
    longCodec.encode(bsonWriter, value.commentsCount, encoderContext)

    bsonWriter.writeName("tags")
    bsonWriter.writeStartArray()
    value.tags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ChangeSetInfo] = {
    classOf[ChangeSetInfo]
  }
}
