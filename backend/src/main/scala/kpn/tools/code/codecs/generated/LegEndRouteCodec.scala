// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.common.TrackPathKey
import kpn.api.common.planner.LegEndRoute
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LegEndRouteCodec(registry: CodecRegistry) extends Codec[LegEndRoute] {

  private val trackPathKeyCodec = registry.get(classOf[TrackPathKey])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LegEndRoute = {
    bsonReader.readStartDocument()

    var trackPathKeys: Seq[TrackPathKey] = null
    var selection: Option[TrackPathKey] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "trackPathKeys") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[TrackPathKey]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += trackPathKeyCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        trackPathKeys = valueBuffer.toSeq
      }
      else if (fieldName == "selection") {
        selection = Some(trackPathKeyCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LegEndRouteCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LegEndRoute(
      trackPathKeys,
      selection,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LegEndRoute, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("trackPathKeys")
    bsonWriter.writeStartArray()
    value.trackPathKeys.foreach(v => trackPathKeyCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.selection.isDefined) {
      bsonWriter.writeName("selection")
      trackPathKeyCodec.encode(bsonWriter, value.selection.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LegEndRoute] = {
    classOf[LegEndRoute]
  }
}
