// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.common.TrackPoint
import kpn.api.common.common.TrackSegmentFragment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TrackSegmentFragmentCodec(registry: CodecRegistry) extends Codec[TrackSegmentFragment] {

  private val longCodec = registry.get(classOf[Long])
  private val trackPointCodec = registry.get(classOf[TrackPoint])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TrackSegmentFragment = {
    bsonReader.readStartDocument()

    var trackPoint: TrackPoint = null
    var meters: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "trackPoint") {
        trackPoint = trackPointCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in TrackSegmentFragmentCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TrackSegmentFragment(
      trackPoint,
      meters,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TrackSegmentFragment, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("trackPoint")
    trackPointCodec.encode(bsonWriter, value.trackPoint, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TrackSegmentFragment] = {
    classOf[TrackSegmentFragment]
  }
}
