// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.filter.ChangesFilterOption
import kpn.api.common.route.RouteChangeInfo
import kpn.api.common.route.RouteChangesPage
import kpn.api.common.route.RouteInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteChangesPageCodec(registry: CodecRegistry) extends Codec[RouteChangesPage] {

  private val changesFilterOptionCodec = registry.get(classOf[ChangesFilterOption])
  private val routeChangeInfoCodec = registry.get(classOf[RouteChangeInfo])
  private val routeInfoCodec = registry.get(classOf[RouteInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteChangesPage = {
    bsonReader.readStartDocument()

    var routeInfo: RouteInfo = null
    var filterOptions: Seq[ChangesFilterOption] = null
    var changes: Seq[RouteChangeInfo] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "routeInfo") {
        routeInfo = routeInfoCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "filterOptions") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[ChangesFilterOption]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += changesFilterOptionCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        filterOptions = valueBuffer.toSeq
      }
      else if (fieldName == "changes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteChangeInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeChangeInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        changes = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteChangesPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteChangesPage(
      routeInfo,
      filterOptions,
      changes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteChangesPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("routeInfo")
    routeInfoCodec.encode(bsonWriter, value.routeInfo, encoderContext)

    bsonWriter.writeName("filterOptions")
    bsonWriter.writeStartArray()
    value.filterOptions.foreach(v => changesFilterOptionCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("changes")
    bsonWriter.writeStartArray()
    value.changes.foreach(v => routeChangeInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteChangesPage] = {
    classOf[RouteChangesPage]
  }
}
