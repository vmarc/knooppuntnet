// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.server.analyzer.engine.analysis.route.domain.RouteTileSegment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteTileSegmentCodec(registry: CodecRegistry) extends Codec[RouteTileSegment] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteTileSegment = {
    bsonReader.readStartDocument()

    var segmentId: Option[Long] = None
    var segmentElementId: Option[Long] = None
    var lines: Seq[String] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "segmentId") {
        segmentId = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "segmentElementId") {
        segmentElementId = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "lines") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        lines = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteTileSegmentCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteTileSegment(
      segmentId,
      segmentElementId,
      lines,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteTileSegment, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.segmentId.isDefined) {
      bsonWriter.writeName("segmentId")
      longCodec.encode(bsonWriter, value.segmentId.get, encoderContext)
    }

    if (value.segmentElementId.isDefined) {
      bsonWriter.writeName("segmentElementId")
      longCodec.encode(bsonWriter, value.segmentElementId.get, encoderContext)
    }

    bsonWriter.writeName("lines")
    bsonWriter.writeStartArray()
    value.lines.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteTileSegment] = {
    classOf[RouteTileSegment]
  }
}
