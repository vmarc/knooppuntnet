// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.filter.ChangesParameters
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ChangesParametersCodec(registry: CodecRegistry) extends Codec[ChangesParameters] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ChangesParameters = {
    bsonReader.readStartDocument()

    var year: Option[Long] = None
    var month: Option[Long] = None
    var day: Option[Long] = None
    var pageSize: Long = 0
    var pageIndex: Long = 0
    var impact: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "year") {
        year = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "month") {
        month = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "day") {
        day = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "pageSize") {
        pageSize = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "pageIndex") {
        pageIndex = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "impact") {
        impact = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ChangesParametersCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ChangesParameters(
      year,
      month,
      day,
      pageSize,
      pageIndex,
      impact,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ChangesParameters, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.year.isDefined) {
      bsonWriter.writeName("year")
      longCodec.encode(bsonWriter, value.year.get, encoderContext)
    }

    if (value.month.isDefined) {
      bsonWriter.writeName("month")
      longCodec.encode(bsonWriter, value.month.get, encoderContext)
    }

    if (value.day.isDefined) {
      bsonWriter.writeName("day")
      longCodec.encode(bsonWriter, value.day.get, encoderContext)
    }

    bsonWriter.writeName("pageSize")
    longCodec.encode(bsonWriter, value.pageSize, encoderContext)

    bsonWriter.writeName("pageIndex")
    longCodec.encode(bsonWriter, value.pageIndex, encoderContext)

    bsonWriter.writeName("impact")
    booleanCodec.encode(bsonWriter, value.impact, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ChangesParameters] = {
    classOf[ChangesParameters]
  }
}
