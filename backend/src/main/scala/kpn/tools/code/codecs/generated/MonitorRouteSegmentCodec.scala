// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.monitor.MonitorRouteSegment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteSegmentCodec(registry: CodecRegistry) extends Codec[MonitorRouteSegment] {

  private val boundsCodec = registry.get(classOf[Bounds])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteSegment = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var startNodeId: Long = 0
    var endNodeId: Long = 0
    var meters: Long = 0
    var bounds: Bounds = null
    var geoJson: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "startNodeId") {
        startNodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "endNodeId") {
        endNodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "bounds") {
        bounds = boundsCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "geoJson") {
        geoJson = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in MonitorRouteSegmentCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteSegment(
      id,
      startNodeId,
      endNodeId,
      meters,
      bounds,
      geoJson,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteSegment, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("startNodeId")
    longCodec.encode(bsonWriter, value.startNodeId, encoderContext)

    bsonWriter.writeName("endNodeId")
    longCodec.encode(bsonWriter, value.endNodeId, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("bounds")
    boundsCodec.encode(bsonWriter, value.bounds, encoderContext)

    bsonWriter.writeName("geoJson")
    stringCodec.encode(bsonWriter, value.geoJson, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteSegment] = {
    classOf[MonitorRouteSegment]
  }
}
