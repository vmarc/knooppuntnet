// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.route.RouteNode
import kpn.api.common.route.RouteNodes
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteNodesCodec(registry: CodecRegistry) extends Codec[RouteNodes] {

  private val routeNodeCodec = registry.get(classOf[RouteNode])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteNodes = {
    bsonReader.readStartDocument()

    var startNode: Option[RouteNode] = None
    var endNode: Option[RouteNode] = None
    var startTentacleNodes: Seq[RouteNode] = null
    var endTentacleNodes: Seq[RouteNode] = null
    var redundantNodes: Seq[RouteNode] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "startNode") {
        startNode = Some(routeNodeCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "endNode") {
        endNode = Some(routeNodeCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "startTentacleNodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNode]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNodeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        startTentacleNodes = valueBuffer.toSeq
      }
      else if (fieldName == "endTentacleNodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNode]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNodeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        endTentacleNodes = valueBuffer.toSeq
      }
      else if (fieldName == "redundantNodes") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteNode]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeNodeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        redundantNodes = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteNodesCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteNodes(
      startNode,
      endNode,
      startTentacleNodes,
      endTentacleNodes,
      redundantNodes,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteNodes, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.startNode.isDefined) {
      bsonWriter.writeName("startNode")
      routeNodeCodec.encode(bsonWriter, value.startNode.get, encoderContext)
    }

    if (value.endNode.isDefined) {
      bsonWriter.writeName("endNode")
      routeNodeCodec.encode(bsonWriter, value.endNode.get, encoderContext)
    }

    bsonWriter.writeName("startTentacleNodes")
    bsonWriter.writeStartArray()
    value.startTentacleNodes.foreach(v => routeNodeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("endTentacleNodes")
    bsonWriter.writeStartArray()
    value.endTentacleNodes.foreach(v => routeNodeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("redundantNodes")
    bsonWriter.writeStartArray()
    value.redundantNodes.foreach(v => routeNodeCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteNodes] = {
    classOf[RouteNodes]
  }
}
