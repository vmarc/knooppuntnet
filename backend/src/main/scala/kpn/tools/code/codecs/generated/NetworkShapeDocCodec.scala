// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.network.NetworkShape
import kpn.core.doc.NetworkShapeDoc
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkShapeDocCodec(registry: CodecRegistry) extends Codec[NetworkShapeDoc] {

  private val longCodec = registry.get(classOf[Long])
  private val networkShapeCodec = registry.get(classOf[NetworkShape])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkShapeDoc = {
    bsonReader.readStartDocument()

    var _id: Long = 0
    var shape: Option[NetworkShape] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "shape") {
        shape = Some(networkShapeCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkShapeDocCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkShapeDoc(
      _id,
      shape,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkShapeDoc, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    longCodec.encode(bsonWriter, value._id, encoderContext)

    if (value.shape.isDefined) {
      bsonWriter.writeName("shape")
      networkShapeCodec.encode(bsonWriter, value.shape.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkShapeDoc] = {
    classOf[NetworkShapeDoc]
  }
}
