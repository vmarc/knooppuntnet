// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LocationChangeSetInfo
import kpn.api.common.changes.filter.ChangesFilterOption
import kpn.api.common.location.LocationChangesPage
import kpn.api.common.location.LocationSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationChangesPageCodec(registry: CodecRegistry) extends Codec[LocationChangesPage] {

  private val changesFilterOptionCodec = registry.get(classOf[ChangesFilterOption])
  private val locationChangeSetInfoCodec = registry.get(classOf[LocationChangeSetInfo])
  private val locationSummaryCodec = registry.get(classOf[LocationSummary])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationChangesPage = {
    bsonReader.readStartDocument()

    var summary: LocationSummary = null
    var changeSets: Seq[LocationChangeSetInfo] = null
    var changesCount: Long = 0
    var filterOptions: Seq[ChangesFilterOption] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "summary") {
        summary = locationSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeSets") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationChangeSetInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationChangeSetInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        changeSets = valueBuffer.toSeq
      }
      else if (fieldName == "changesCount") {
        changesCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "filterOptions") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[ChangesFilterOption]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += changesFilterOptionCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        filterOptions = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LocationChangesPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationChangesPage(
      summary,
      changeSets,
      changesCount,
      filterOptions,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationChangesPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("summary")
    locationSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("changeSets")
    bsonWriter.writeStartArray()
    value.changeSets.foreach(v => locationChangeSetInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("changesCount")
    longCodec.encode(bsonWriter, value.changesCount, encoderContext)

    bsonWriter.writeName("filterOptions")
    bsonWriter.writeStartArray()
    value.filterOptions.foreach(v => changesFilterOptionCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationChangesPage] = {
    classOf[LocationChangesPage]
  }
}
