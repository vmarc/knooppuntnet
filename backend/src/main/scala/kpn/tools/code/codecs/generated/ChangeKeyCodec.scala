// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.TimeKey
import kpn.api.common.changes.details.ChangeKey
import kpn.api.custom.Timestamp
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ChangeKeyCodec(registry: CodecRegistry) extends Codec[ChangeKey] {

  private val longCodec = registry.get(classOf[Long])
  private val timeKeyCodec = registry.get(classOf[TimeKey])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ChangeKey = {
    bsonReader.readStartDocument()

    var replicationNumber: Long = 0
    var timestamp: Timestamp = null
    var changeSetId: Long = 0
    var elementId: Long = 0
    var time: TimeKey = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "replicationNumber") {
        replicationNumber = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestamp") {
        timestamp = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "changeSetId") {
        changeSetId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "elementId") {
        elementId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "time") {
        time = timeKeyCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ChangeKeyCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ChangeKey(
      replicationNumber,
      timestamp,
      changeSetId,
      elementId,
      time,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ChangeKey, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("replicationNumber")
    longCodec.encode(bsonWriter, value.replicationNumber, encoderContext)

    bsonWriter.writeName("timestamp")
    timestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    bsonWriter.writeName("changeSetId")
    longCodec.encode(bsonWriter, value.changeSetId, encoderContext)

    bsonWriter.writeName("elementId")
    longCodec.encode(bsonWriter, value.elementId, encoderContext)

    bsonWriter.writeName("time")
    timeKeyCodec.encode(bsonWriter, value.time, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ChangeKey] = {
    classOf[ChangeKey]
  }
}
