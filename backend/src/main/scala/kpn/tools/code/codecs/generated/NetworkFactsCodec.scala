// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.NetworkExtraMemberNode
import kpn.api.common.NetworkExtraMemberRelation
import kpn.api.common.NetworkExtraMemberWay
import kpn.api.common.NetworkFacts
import kpn.api.common.NetworkIntegrityCheck
import kpn.api.common.NetworkIntegrityCheckFailed
import kpn.api.common.NetworkNameMissing
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkFactsCodec(registry: CodecRegistry) extends Codec[NetworkFacts] {

  private val networkExtraMemberNodeCodec = registry.get(classOf[NetworkExtraMemberNode])
  private val networkExtraMemberRelationCodec = registry.get(classOf[NetworkExtraMemberRelation])
  private val networkExtraMemberWayCodec = registry.get(classOf[NetworkExtraMemberWay])
  private val networkIntegrityCheckCodec = registry.get(classOf[NetworkIntegrityCheck])
  private val networkIntegrityCheckFailedCodec = registry.get(classOf[NetworkIntegrityCheckFailed])
  private val networkNameMissingCodec = registry.get(classOf[NetworkNameMissing])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkFacts = {
    bsonReader.readStartDocument()

    var networkExtraMemberNode: Option[Seq[NetworkExtraMemberNode]] = None
    var networkExtraMemberWay: Option[Seq[NetworkExtraMemberWay]] = None
    var networkExtraMemberRelation: Option[Seq[NetworkExtraMemberRelation]] = None
    var integrityCheck: Option[NetworkIntegrityCheck] = None
    var integrityCheckFailed: Option[NetworkIntegrityCheckFailed] = None
    var nameMissing: Option[NetworkNameMissing] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "networkExtraMemberNode") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NetworkExtraMemberNode]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += networkExtraMemberNodeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        networkExtraMemberNode = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "networkExtraMemberWay") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NetworkExtraMemberWay]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += networkExtraMemberWayCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        networkExtraMemberWay = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "networkExtraMemberRelation") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[NetworkExtraMemberRelation]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += networkExtraMemberRelationCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        networkExtraMemberRelation = Some(valueBuffer.toSeq)
      }
      else if (fieldName == "integrityCheck") {
        integrityCheck = Some(networkIntegrityCheckCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "integrityCheckFailed") {
        integrityCheckFailed = Some(networkIntegrityCheckFailedCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "nameMissing") {
        nameMissing = Some(networkNameMissingCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkFactsCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkFacts(
      networkExtraMemberNode,
      networkExtraMemberWay,
      networkExtraMemberRelation,
      integrityCheck,
      integrityCheckFailed,
      nameMissing,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkFacts, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.networkExtraMemberNode.isDefined) {
      bsonWriter.writeName("networkExtraMemberNode")
      bsonWriter.writeStartArray()
      value.networkExtraMemberNode.get.foreach(v => networkExtraMemberNodeCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    if (value.networkExtraMemberWay.isDefined) {
      bsonWriter.writeName("networkExtraMemberWay")
      bsonWriter.writeStartArray()
      value.networkExtraMemberWay.get.foreach(v => networkExtraMemberWayCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    if (value.networkExtraMemberRelation.isDefined) {
      bsonWriter.writeName("networkExtraMemberRelation")
      bsonWriter.writeStartArray()
      value.networkExtraMemberRelation.get.foreach(v => networkExtraMemberRelationCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    if (value.integrityCheck.isDefined) {
      bsonWriter.writeName("integrityCheck")
      networkIntegrityCheckCodec.encode(bsonWriter, value.integrityCheck.get, encoderContext)
    }

    if (value.integrityCheckFailed.isDefined) {
      bsonWriter.writeName("integrityCheckFailed")
      networkIntegrityCheckFailedCodec.encode(bsonWriter, value.integrityCheckFailed.get, encoderContext)
    }

    if (value.nameMissing.isDefined) {
      bsonWriter.writeName("nameMissing")
      networkNameMissingCodec.encode(bsonWriter, value.nameMissing.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkFacts] = {
    classOf[NetworkFacts]
  }
}
