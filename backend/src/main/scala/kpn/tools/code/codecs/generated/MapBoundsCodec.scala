// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.common.MapBounds
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MapBoundsCodec(registry: CodecRegistry) extends Codec[MapBounds] {

  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MapBounds = {
    bsonReader.readStartDocument()

    var latMin: String = null
    var latMax: String = null
    var lonMin: String = null
    var lonMax: String = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "latMin") {
        latMin = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "latMax") {
        latMax = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lonMin") {
        lonMin = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lonMax") {
        lonMax = stringCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in MapBoundsCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MapBounds(
      latMin,
      latMax,
      lonMin,
      lonMax,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MapBounds, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("latMin")
    stringCodec.encode(bsonWriter, value.latMin, encoderContext)

    bsonWriter.writeName("latMax")
    stringCodec.encode(bsonWriter, value.latMax, encoderContext)

    bsonWriter.writeName("lonMin")
    stringCodec.encode(bsonWriter, value.lonMin, encoderContext)

    bsonWriter.writeName("lonMax")
    stringCodec.encode(bsonWriter, value.lonMax, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MapBounds] = {
    classOf[MapBounds]
  }
}
