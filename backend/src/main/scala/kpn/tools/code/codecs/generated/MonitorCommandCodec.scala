// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorCommand
import kpn.api.common.monitor.MonitorCommandAction
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorCommandCodec(registry: CodecRegistry) extends Codec[MonitorCommand] {

  private val monitorCommandActionCodec = registry.get(classOf[MonitorCommandAction])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorCommand = {
    bsonReader.readStartDocument()

    var action: MonitorCommandAction = null
    var stepId: String = null
    var description: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "action") {
        action = monitorCommandActionCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "stepId") {
        stepId = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "description") {
        description = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in MonitorCommandCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorCommand(
      action,
      stepId,
      description,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorCommand, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("action")
    monitorCommandActionCodec.encode(bsonWriter, value.action, encoderContext)

    bsonWriter.writeName("stepId")
    stringCodec.encode(bsonWriter, value.stepId, encoderContext)

    if (value.description.isDefined) {
      bsonWriter.writeName("description")
      stringCodec.encode(bsonWriter, value.description.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorCommand] = {
    classOf[MonitorCommand]
  }
}
