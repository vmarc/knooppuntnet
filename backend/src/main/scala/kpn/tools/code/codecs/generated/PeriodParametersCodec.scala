// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.status.PeriodParameters
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PeriodParametersCodec(registry: CodecRegistry) extends Codec[PeriodParameters] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PeriodParameters = {
    bsonReader.readStartDocument()

    var period: String = null
    var year: Long = 0
    var month: Option[Long] = None
    var week: Option[Long] = None
    var day: Option[Long] = None
    var hour: Option[Long] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "period") {
        period = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "year") {
        year = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "month") {
        month = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "week") {
        week = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "day") {
        day = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "hour") {
        hour = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in PeriodParametersCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PeriodParameters(
      period,
      year,
      month,
      week,
      day,
      hour,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PeriodParameters, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("period")
    stringCodec.encode(bsonWriter, value.period, encoderContext)

    bsonWriter.writeName("year")
    longCodec.encode(bsonWriter, value.year, encoderContext)

    if (value.month.isDefined) {
      bsonWriter.writeName("month")
      longCodec.encode(bsonWriter, value.month.get, encoderContext)
    }

    if (value.week.isDefined) {
      bsonWriter.writeName("week")
      longCodec.encode(bsonWriter, value.week.get, encoderContext)
    }

    if (value.day.isDefined) {
      bsonWriter.writeName("day")
      longCodec.encode(bsonWriter, value.day.get, encoderContext)
    }

    if (value.hour.isDefined) {
      bsonWriter.writeName("hour")
      longCodec.encode(bsonWriter, value.hour.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PeriodParameters] = {
    classOf[PeriodParameters]
  }
}
