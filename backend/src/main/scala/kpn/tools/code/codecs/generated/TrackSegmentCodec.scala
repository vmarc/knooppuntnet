// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.common.TrackPoint
import kpn.api.common.common.TrackSegment
import kpn.api.common.common.TrackSegmentFragment
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TrackSegmentCodec(registry: CodecRegistry) extends Codec[TrackSegment] {

  private val stringCodec = registry.get(classOf[String])
  private val trackPointCodec = registry.get(classOf[TrackPoint])
  private val trackSegmentFragmentCodec = registry.get(classOf[TrackSegmentFragment])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TrackSegment = {
    bsonReader.readStartDocument()

    var surface: String = null
    var source: TrackPoint = null
    var fragments: Seq[TrackSegmentFragment] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "surface") {
        surface = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "source") {
        source = trackPointCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "fragments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[TrackSegmentFragment]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += trackSegmentFragmentCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        fragments = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in TrackSegmentCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TrackSegment(
      surface,
      source,
      fragments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TrackSegment, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("surface")
    stringCodec.encode(bsonWriter, value.surface, encoderContext)

    bsonWriter.writeName("source")
    trackPointCodec.encode(bsonWriter, value.source, encoderContext)

    bsonWriter.writeName("fragments")
    bsonWriter.writeStartArray()
    value.fragments.foreach(v => trackSegmentFragmentCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TrackSegment] = {
    classOf[TrackSegment]
  }
}
