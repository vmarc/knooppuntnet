// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.changes.filter.ChangesFilterOption
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class ChangesFilterOptionCodec(registry: CodecRegistry) extends Codec[ChangesFilterOption] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): ChangesFilterOption = {
    bsonReader.readStartDocument()

    var level: String = null
    var name: String = null
    var year: Option[Long] = None
    var month: Option[Long] = None
    var day: Option[Long] = None
    var totalCount: Long = 0
    var impactedCount: Long = 0
    var current: Boolean = false

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "level") {
        level = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "year") {
        year = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "month") {
        month = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "day") {
        day = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "totalCount") {
        totalCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "impactedCount") {
        impactedCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "current") {
        current = booleanCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in ChangesFilterOptionCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    ChangesFilterOption(
      level,
      name,
      year,
      month,
      day,
      totalCount,
      impactedCount,
      current,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: ChangesFilterOption, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("level")
    stringCodec.encode(bsonWriter, value.level, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    if (value.year.isDefined) {
      bsonWriter.writeName("year")
      longCodec.encode(bsonWriter, value.year.get, encoderContext)
    }

    if (value.month.isDefined) {
      bsonWriter.writeName("month")
      longCodec.encode(bsonWriter, value.month.get, encoderContext)
    }

    if (value.day.isDefined) {
      bsonWriter.writeName("day")
      longCodec.encode(bsonWriter, value.day.get, encoderContext)
    }

    bsonWriter.writeName("totalCount")
    longCodec.encode(bsonWriter, value.totalCount, encoderContext)

    bsonWriter.writeName("impactedCount")
    longCodec.encode(bsonWriter, value.impactedCount, encoderContext)

    bsonWriter.writeName("current")
    booleanCodec.encode(bsonWriter, value.current, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[ChangesFilterOption] = {
    classOf[ChangesFilterOption]
  }
}
