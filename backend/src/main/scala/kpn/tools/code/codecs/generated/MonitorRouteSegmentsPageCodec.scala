// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.monitor.MonitorRouteRelationInfo
import kpn.api.common.monitor.MonitorRouteSegmentsPage
import kpn.api.common.monitor.MonitorRouteSummary
import kpn.api.common.route.SegmentInfo
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteSegmentsPageCodec(registry: CodecRegistry) extends Codec[MonitorRouteSegmentsPage] {

  private val longCodec = registry.get(classOf[Long])
  private val monitorRouteRelationInfoCodec = registry.get(classOf[MonitorRouteRelationInfo])
  private val monitorRouteSummaryCodec = registry.get(classOf[MonitorRouteSummary])
  private val segmentInfoCodec = registry.get(classOf[SegmentInfo])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteSegmentsPage = {
    bsonReader.readStartDocument()

    var summary: MonitorRouteSummary = null
    var meters: Long = 0
    var relations: Seq[MonitorRouteRelationInfo] = null
    var segments: Seq[SegmentInfo] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "summary") {
        summary = monitorRouteSummaryCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "relations") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[MonitorRouteRelationInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += monitorRouteRelationInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        relations = valueBuffer.toSeq
      }
      else if (fieldName == "segments") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[SegmentInfo]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += segmentInfoCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        segments = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in MonitorRouteSegmentsPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteSegmentsPage(
      summary,
      meters,
      relations,
      segments,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteSegmentsPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("summary")
    monitorRouteSummaryCodec.encode(bsonWriter, value.summary, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeName("relations")
    bsonWriter.writeStartArray()
    value.relations.foreach(v => monitorRouteRelationInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("segments")
    bsonWriter.writeStartArray()
    value.segments.foreach(v => segmentInfoCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteSegmentsPage] = {
    classOf[MonitorRouteSegmentsPage]
  }
}
