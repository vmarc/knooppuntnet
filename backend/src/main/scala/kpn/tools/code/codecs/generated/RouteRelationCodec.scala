// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.doc.RouteRelation
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteRelationCodec(registry: CodecRegistry) extends Codec[RouteRelation] {

  private val longCodec = registry.get(classOf[Long])
  private val routeRelationCodec = registry.get(classOf[RouteRelation])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteRelation = {
    bsonReader.readStartDocument()

    var relationId: Long = 0
    var name: String = null
    var role: Option[String] = None
    var relations: Option[Seq[RouteRelation]] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "relationId") {
        relationId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "role") {
        role = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relations") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[RouteRelation]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += routeRelationCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        relations = Some(valueBuffer.toSeq)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteRelationCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteRelation(
      relationId,
      name,
      role,
      relations,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteRelation, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("relationId")
    longCodec.encode(bsonWriter, value.relationId, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    if (value.role.isDefined) {
      bsonWriter.writeName("role")
      stringCodec.encode(bsonWriter, value.role.get, encoderContext)
    }

    if (value.relations.isDefined) {
      bsonWriter.writeName("relations")
      bsonWriter.writeStartArray()
      value.relations.get.foreach(v => routeRelationCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteRelation] = {
    classOf[RouteRelation]
  }
}
