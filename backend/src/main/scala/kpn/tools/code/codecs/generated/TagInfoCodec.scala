// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.core.taginfo.TagInfo
import kpn.core.taginfo.TagInfoProject
import kpn.core.taginfo.TagInfoTag
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class TagInfoCodec(registry: CodecRegistry) extends Codec[TagInfo] {

  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val tagInfoProjectCodec = registry.get(classOf[TagInfoProject])
  private val tagInfoTagCodec = registry.get(classOf[TagInfoTag])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): TagInfo = {
    bsonReader.readStartDocument()

    var data_format: Long = 0
    var data_url: String = null
    var data_updated: String = null
    var project: TagInfoProject = null
    var tags: Seq[TagInfoTag] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "data_format") {
        data_format = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "data_url") {
        data_url = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "data_updated") {
        data_updated = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "project") {
        project = tagInfoProjectCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "tags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[TagInfoTag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagInfoTagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        tags = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in TagInfoCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    TagInfo(
      data_format,
      data_url,
      data_updated,
      project,
      tags,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: TagInfo, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("data_format")
    longCodec.encode(bsonWriter, value.data_format, encoderContext)

    bsonWriter.writeName("data_url")
    stringCodec.encode(bsonWriter, value.data_url, encoderContext)

    bsonWriter.writeName("data_updated")
    stringCodec.encode(bsonWriter, value.data_updated, encoderContext)

    bsonWriter.writeName("project")
    tagInfoProjectCodec.encode(bsonWriter, value.project, encoderContext)

    bsonWriter.writeName("tags")
    bsonWriter.writeStartArray()
    value.tags.foreach(v => tagInfoTagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[TagInfo] = {
    classOf[TagInfo]
  }
}
