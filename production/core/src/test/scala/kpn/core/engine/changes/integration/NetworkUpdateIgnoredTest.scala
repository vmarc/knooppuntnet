package kpn.core.engine.changes.integration

class NetworkUpdateIgnoredTest extends AbstractTest {

  test("network update - a network that no longer contains any network nodes becomes an ignored network") {
    // assert analysisRepository.deleteNetwork called  OR data changed so that now an ignored network
    // this can result in orphan routes and orphan nodes

    // TODO CHANGE
    pending
    ()
  }

}
