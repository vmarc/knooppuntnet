// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.route.RouteEdge
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class RouteEdgeCodec(registry: CodecRegistry) extends Codec[RouteEdge] {

  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): RouteEdge = {
    bsonReader.readStartDocument()

    var pathId: Long = 0
    var sourceNodeId: Long = 0
    var sinkNodeId: Long = 0
    var meters: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "pathId") {
        pathId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "sourceNodeId") {
        sourceNodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "sinkNodeId") {
        sinkNodeId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "meters") {
        meters = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in RouteEdgeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    RouteEdge(
      pathId,
      sourceNodeId,
      sinkNodeId,
      meters,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: RouteEdge, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("pathId")
    longCodec.encode(bsonWriter, value.pathId, encoderContext)

    bsonWriter.writeName("sourceNodeId")
    longCodec.encode(bsonWriter, value.sourceNodeId, encoderContext)

    bsonWriter.writeName("sinkNodeId")
    longCodec.encode(bsonWriter, value.sinkNodeId, encoderContext)

    bsonWriter.writeName("meters")
    longCodec.encode(bsonWriter, value.meters, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[RouteEdge] = {
    classOf[RouteEdge]
  }
}
