// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.LatLonImpl
import kpn.api.common.diff.node.NodeMoved
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NodeMovedCodec(registry: CodecRegistry) extends Codec[NodeMoved] {

  private val latLonImplCodec = registry.get(classOf[LatLonImpl])
  private val longCodec = registry.get(classOf[Long])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NodeMoved = {
    bsonReader.readStartDocument()

    var before: LatLonImpl = null
    var after: LatLonImpl = null
    var distance: Long = 0

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "before") {
        before = latLonImplCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "after") {
        after = latLonImplCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "distance") {
        distance = longCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NodeMovedCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NodeMoved(
      before,
      after,
      distance,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NodeMoved, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("before")
    latLonImplCodec.encode(bsonWriter, value.before, encoderContext)

    bsonWriter.writeName("after")
    latLonImplCodec.encode(bsonWriter, value.after, encoderContext)

    bsonWriter.writeName("distance")
    longCodec.encode(bsonWriter, value.distance, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NodeMoved] = {
    classOf[NodeMoved]
  }
}
