// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.location.LocationNode
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class LocationNodeCodec(registry: CodecRegistry) extends Codec[LocationNode] {

  private val locationNodeCodec = registry.get(classOf[LocationNode])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): LocationNode = {
    bsonReader.readStartDocument()

    var name: String = null
    var nodeCount: Long = 0
    var routeCount: Long = 0
    var factCount: Long = 0
    var children: Option[Seq[LocationNode]] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "nodeCount") {
        nodeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeCount") {
        routeCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "factCount") {
        factCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "children") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[LocationNode]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += locationNodeCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        children = Some(valueBuffer.toSeq)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in LocationNodeCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    LocationNode(
      name,
      nodeCount,
      routeCount,
      factCount,
      children,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: LocationNode, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("nodeCount")
    longCodec.encode(bsonWriter, value.nodeCount, encoderContext)

    bsonWriter.writeName("routeCount")
    longCodec.encode(bsonWriter, value.routeCount, encoderContext)

    bsonWriter.writeName("factCount")
    longCodec.encode(bsonWriter, value.factCount, encoderContext)

    if (value.children.isDefined) {
      bsonWriter.writeName("children")
      bsonWriter.writeStartArray()
      value.children.get.foreach(v => locationNodeCodec.encode(bsonWriter, v, encoderContext))
      bsonWriter.writeEndArray()
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[LocationNode] = {
    classOf[LocationNode]
  }
}
