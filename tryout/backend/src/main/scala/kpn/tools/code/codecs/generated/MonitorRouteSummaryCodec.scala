// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Bounds
import kpn.api.common.monitor.MonitorRouteSummary
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class MonitorRouteSummaryCodec(registry: CodecRegistry) extends Codec[MonitorRouteSummary] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val boundsCodec = registry.get(classOf[Bounds])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): MonitorRouteSummary = {
    bsonReader.readStartDocument()

    var adminUser: Boolean = false
    var groupName: String = null
    var routeName: String = null
    var routeDescription: String = null
    var routeId: String = null
    var relationId: Option[Long] = None
    var relationIds: Seq[Long] = null
    var memberCount: Long = 0
    var segmentCount: Long = 0
    var deviationCount: Long = 0
    var bounds: Option[Bounds] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "adminUser") {
        adminUser = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "groupName") {
        groupName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeName") {
        routeName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeDescription") {
        routeDescription = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "routeId") {
        routeId = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "relationId") {
        relationId = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "relationIds") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Long]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += longCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        relationIds = valueBuffer.toSeq
      }
      else if (fieldName == "memberCount") {
        memberCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "segmentCount") {
        segmentCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "deviationCount") {
        deviationCount = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "bounds") {
        bounds = Some(boundsCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in MonitorRouteSummaryCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    MonitorRouteSummary(
      adminUser,
      groupName,
      routeName,
      routeDescription,
      routeId,
      relationId,
      relationIds,
      memberCount,
      segmentCount,
      deviationCount,
      bounds,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: MonitorRouteSummary, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("adminUser")
    booleanCodec.encode(bsonWriter, value.adminUser, encoderContext)

    bsonWriter.writeName("groupName")
    stringCodec.encode(bsonWriter, value.groupName, encoderContext)

    bsonWriter.writeName("routeName")
    stringCodec.encode(bsonWriter, value.routeName, encoderContext)

    bsonWriter.writeName("routeDescription")
    stringCodec.encode(bsonWriter, value.routeDescription, encoderContext)

    bsonWriter.writeName("routeId")
    stringCodec.encode(bsonWriter, value.routeId, encoderContext)

    if (value.relationId.isDefined) {
      bsonWriter.writeName("relationId")
      longCodec.encode(bsonWriter, value.relationId.get, encoderContext)
    }

    bsonWriter.writeName("relationIds")
    bsonWriter.writeStartArray()
    value.relationIds.foreach(v => longCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("memberCount")
    longCodec.encode(bsonWriter, value.memberCount, encoderContext)

    bsonWriter.writeName("segmentCount")
    longCodec.encode(bsonWriter, value.segmentCount, encoderContext)

    bsonWriter.writeName("deviationCount")
    longCodec.encode(bsonWriter, value.deviationCount, encoderContext)

    if (value.bounds.isDefined) {
      bsonWriter.writeName("bounds")
      boundsCodec.encode(bsonWriter, value.bounds.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[MonitorRouteSummary] = {
    classOf[MonitorRouteSummary]
  }
}
