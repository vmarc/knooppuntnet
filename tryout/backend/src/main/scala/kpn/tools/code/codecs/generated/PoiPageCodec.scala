// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.PoiAnalysis
import kpn.api.common.PoiPage
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PoiPageCodec(registry: CodecRegistry) extends Codec[PoiPage] {

  private val longCodec = registry.get(classOf[Long])
  private val poiAnalysisCodec = registry.get(classOf[PoiAnalysis])
  private val stringCodec = registry.get(classOf[String])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PoiPage = {
    bsonReader.readStartDocument()

    var elementType: String = null
    var elementId: Long = 0
    var latitude: String = null
    var longitude: String = null
    var analysis: PoiAnalysis = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "elementType") {
        elementType = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "elementId") {
        elementId = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "latitude") {
        latitude = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "longitude") {
        longitude = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "analysis") {
        analysis = poiAnalysisCodec.decode(bsonReader, decoderContext)
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in PoiPageCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PoiPage(
      elementType,
      elementId,
      latitude,
      longitude,
      analysis,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PoiPage, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("elementType")
    stringCodec.encode(bsonWriter, value.elementType, encoderContext)

    bsonWriter.writeName("elementId")
    longCodec.encode(bsonWriter, value.elementId, encoderContext)

    bsonWriter.writeName("latitude")
    stringCodec.encode(bsonWriter, value.latitude, encoderContext)

    bsonWriter.writeName("longitude")
    stringCodec.encode(bsonWriter, value.longitude, encoderContext)

    bsonWriter.writeName("analysis")
    poiAnalysisCodec.encode(bsonWriter, value.analysis, encoderContext)

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PoiPage] = {
    classOf[PoiPage]
  }
}
