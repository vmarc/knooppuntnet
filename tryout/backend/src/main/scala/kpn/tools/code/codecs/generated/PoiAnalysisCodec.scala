// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.PoiAnalysis
import kpn.api.custom.Tag
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class PoiAnalysisCodec(registry: CodecRegistry) extends Codec[PoiAnalysis] {

  private val stringCodec = registry.get(classOf[String])
  private val tagCodec = registry.get(classOf[Tag])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): PoiAnalysis = {
    bsonReader.readStartDocument()

    var layers: Seq[String] = null
    var mainTags: Seq[Tag] = null
    var extraTags: Seq[Tag] = null
    var name: Option[String] = None
    var subject: Option[String] = None
    var description: Option[String] = None
    var addressLine1: Option[String] = None
    var addressLine2: Option[String] = None
    var phone: Option[String] = None
    var email: Option[String] = None
    var fax: Option[String] = None
    var facebook: Option[String] = None
    var twitter: Option[String] = None
    var website: Option[String] = None
    var wikidata: Option[String] = None
    var wikipedia: Option[String] = None
    var molenDatabase: Option[String] = None
    var hollandscheMolenDatabase: Option[String] = None
    var image: Option[String] = None
    var imageLink: Option[String] = None
    var imageThumbnail: Option[String] = None
    var mapillary: Option[String] = None
    var wheelchair: Option[String] = None
    var onroerendErfgoed: Option[String] = None
    var openingHours: Option[String] = None
    var serviceTimes: Option[String] = None
    var cuisine: Option[String] = None
    var denomination: Option[String] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "layers") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[String]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += stringCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        layers = valueBuffer.toSeq
      }
      else if (fieldName == "mainTags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        mainTags = valueBuffer.toSeq
      }
      else if (fieldName == "extraTags") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Tag]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += tagCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        extraTags = valueBuffer.toSeq
      }
      else if (fieldName == "name") {
        name = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "subject") {
        subject = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "description") {
        description = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "addressLine1") {
        addressLine1 = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "addressLine2") {
        addressLine2 = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "phone") {
        phone = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "email") {
        email = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "fax") {
        fax = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "facebook") {
        facebook = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "twitter") {
        twitter = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "website") {
        website = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "wikidata") {
        wikidata = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "wikipedia") {
        wikipedia = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "molenDatabase") {
        molenDatabase = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "hollandscheMolenDatabase") {
        hollandscheMolenDatabase = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "image") {
        image = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "imageLink") {
        imageLink = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "imageThumbnail") {
        imageThumbnail = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "mapillary") {
        mapillary = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "wheelchair") {
        wheelchair = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "onroerendErfgoed") {
        onroerendErfgoed = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "openingHours") {
        openingHours = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "serviceTimes") {
        serviceTimes = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "cuisine") {
        cuisine = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "denomination") {
        denomination = Some(stringCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in PoiAnalysisCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    PoiAnalysis(
      layers,
      mainTags,
      extraTags,
      name,
      subject,
      description,
      addressLine1,
      addressLine2,
      phone,
      email,
      fax,
      facebook,
      twitter,
      website,
      wikidata,
      wikipedia,
      molenDatabase,
      hollandscheMolenDatabase,
      image,
      imageLink,
      imageThumbnail,
      mapillary,
      wheelchair,
      onroerendErfgoed,
      openingHours,
      serviceTimes,
      cuisine,
      denomination,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: PoiAnalysis, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("layers")
    bsonWriter.writeStartArray()
    value.layers.foreach(v => stringCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("mainTags")
    bsonWriter.writeStartArray()
    value.mainTags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeName("extraTags")
    bsonWriter.writeStartArray()
    value.extraTags.foreach(v => tagCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    if (value.name.isDefined) {
      bsonWriter.writeName("name")
      stringCodec.encode(bsonWriter, value.name.get, encoderContext)
    }

    if (value.subject.isDefined) {
      bsonWriter.writeName("subject")
      stringCodec.encode(bsonWriter, value.subject.get, encoderContext)
    }

    if (value.description.isDefined) {
      bsonWriter.writeName("description")
      stringCodec.encode(bsonWriter, value.description.get, encoderContext)
    }

    if (value.addressLine1.isDefined) {
      bsonWriter.writeName("addressLine1")
      stringCodec.encode(bsonWriter, value.addressLine1.get, encoderContext)
    }

    if (value.addressLine2.isDefined) {
      bsonWriter.writeName("addressLine2")
      stringCodec.encode(bsonWriter, value.addressLine2.get, encoderContext)
    }

    if (value.phone.isDefined) {
      bsonWriter.writeName("phone")
      stringCodec.encode(bsonWriter, value.phone.get, encoderContext)
    }

    if (value.email.isDefined) {
      bsonWriter.writeName("email")
      stringCodec.encode(bsonWriter, value.email.get, encoderContext)
    }

    if (value.fax.isDefined) {
      bsonWriter.writeName("fax")
      stringCodec.encode(bsonWriter, value.fax.get, encoderContext)
    }

    if (value.facebook.isDefined) {
      bsonWriter.writeName("facebook")
      stringCodec.encode(bsonWriter, value.facebook.get, encoderContext)
    }

    if (value.twitter.isDefined) {
      bsonWriter.writeName("twitter")
      stringCodec.encode(bsonWriter, value.twitter.get, encoderContext)
    }

    if (value.website.isDefined) {
      bsonWriter.writeName("website")
      stringCodec.encode(bsonWriter, value.website.get, encoderContext)
    }

    if (value.wikidata.isDefined) {
      bsonWriter.writeName("wikidata")
      stringCodec.encode(bsonWriter, value.wikidata.get, encoderContext)
    }

    if (value.wikipedia.isDefined) {
      bsonWriter.writeName("wikipedia")
      stringCodec.encode(bsonWriter, value.wikipedia.get, encoderContext)
    }

    if (value.molenDatabase.isDefined) {
      bsonWriter.writeName("molenDatabase")
      stringCodec.encode(bsonWriter, value.molenDatabase.get, encoderContext)
    }

    if (value.hollandscheMolenDatabase.isDefined) {
      bsonWriter.writeName("hollandscheMolenDatabase")
      stringCodec.encode(bsonWriter, value.hollandscheMolenDatabase.get, encoderContext)
    }

    if (value.image.isDefined) {
      bsonWriter.writeName("image")
      stringCodec.encode(bsonWriter, value.image.get, encoderContext)
    }

    if (value.imageLink.isDefined) {
      bsonWriter.writeName("imageLink")
      stringCodec.encode(bsonWriter, value.imageLink.get, encoderContext)
    }

    if (value.imageThumbnail.isDefined) {
      bsonWriter.writeName("imageThumbnail")
      stringCodec.encode(bsonWriter, value.imageThumbnail.get, encoderContext)
    }

    if (value.mapillary.isDefined) {
      bsonWriter.writeName("mapillary")
      stringCodec.encode(bsonWriter, value.mapillary.get, encoderContext)
    }

    if (value.wheelchair.isDefined) {
      bsonWriter.writeName("wheelchair")
      stringCodec.encode(bsonWriter, value.wheelchair.get, encoderContext)
    }

    if (value.onroerendErfgoed.isDefined) {
      bsonWriter.writeName("onroerendErfgoed")
      stringCodec.encode(bsonWriter, value.onroerendErfgoed.get, encoderContext)
    }

    if (value.openingHours.isDefined) {
      bsonWriter.writeName("openingHours")
      stringCodec.encode(bsonWriter, value.openingHours.get, encoderContext)
    }

    if (value.serviceTimes.isDefined) {
      bsonWriter.writeName("serviceTimes")
      stringCodec.encode(bsonWriter, value.serviceTimes.get, encoderContext)
    }

    if (value.cuisine.isDefined) {
      bsonWriter.writeName("cuisine")
      stringCodec.encode(bsonWriter, value.cuisine.get, encoderContext)
    }

    if (value.denomination.isDefined) {
      bsonWriter.writeName("denomination")
      stringCodec.encode(bsonWriter, value.denomination.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[PoiAnalysis] = {
    classOf[PoiAnalysis]
  }
}
