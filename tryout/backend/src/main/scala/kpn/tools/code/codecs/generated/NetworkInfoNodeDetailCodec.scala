// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Fact
import kpn.api.custom.Day
import kpn.api.custom.Timestamp
import kpn.core.doc.NetworkInfoNodeDetail
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkInfoNodeDetailCodec(registry: CodecRegistry) extends Codec[NetworkInfoNodeDetail] {

  private val booleanCodec = registry.get(classOf[Boolean])
  private val dayCodec = registry.get(classOf[Day])
  private val factCodec = registry.get(classOf[Fact])
  private val longCodec = registry.get(classOf[Long])
  private val stringCodec = registry.get(classOf[String])
  private val timestampCodec = registry.get(classOf[Timestamp])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkInfoNodeDetail = {
    bsonReader.readStartDocument()

    var id: Long = 0
    var name: String = null
    var longName: String = null
    var latitude: String = null
    var longitude: String = null
    var connection: Boolean = false
    var roleConnection: Boolean = false
    var definedInRelation: Boolean = false
    var proposed: Boolean = false
    var timestamp: Timestamp = null
    var lastSurvey: Option[Day] = None
    var expectedRouteCount: Option[Long] = None
    var facts: Seq[Fact] = null

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "id") {
        id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "name") {
        name = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "longName") {
        longName = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "latitude") {
        latitude = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "longitude") {
        longitude = stringCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "connection") {
        connection = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "roleConnection") {
        roleConnection = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "definedInRelation") {
        definedInRelation = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "proposed") {
        proposed = booleanCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "timestamp") {
        timestamp = timestampCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "lastSurvey") {
        lastSurvey = Some(dayCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "expectedRouteCount") {
        expectedRouteCount = Some(longCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "facts") {
        bsonReader.readStartArray()
        val valueBuffer = scala.collection.mutable.Buffer[Fact]()
        while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
          valueBuffer += factCodec.decode(bsonReader, decoderContext)
        }
        bsonReader.readEndArray()
        facts = valueBuffer.toSeq
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkInfoNodeDetailCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkInfoNodeDetail(
      id,
      name,
      longName,
      latitude,
      longitude,
      connection,
      roleConnection,
      definedInRelation,
      proposed,
      timestamp,
      lastSurvey,
      expectedRouteCount,
      facts,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkInfoNodeDetail, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("id")
    longCodec.encode(bsonWriter, value.id, encoderContext)

    bsonWriter.writeName("name")
    stringCodec.encode(bsonWriter, value.name, encoderContext)

    bsonWriter.writeName("longName")
    stringCodec.encode(bsonWriter, value.longName, encoderContext)

    bsonWriter.writeName("latitude")
    stringCodec.encode(bsonWriter, value.latitude, encoderContext)

    bsonWriter.writeName("longitude")
    stringCodec.encode(bsonWriter, value.longitude, encoderContext)

    bsonWriter.writeName("connection")
    booleanCodec.encode(bsonWriter, value.connection, encoderContext)

    bsonWriter.writeName("roleConnection")
    booleanCodec.encode(bsonWriter, value.roleConnection, encoderContext)

    bsonWriter.writeName("definedInRelation")
    booleanCodec.encode(bsonWriter, value.definedInRelation, encoderContext)

    bsonWriter.writeName("proposed")
    booleanCodec.encode(bsonWriter, value.proposed, encoderContext)

    bsonWriter.writeName("timestamp")
    timestampCodec.encode(bsonWriter, value.timestamp, encoderContext)

    if (value.lastSurvey.isDefined) {
      bsonWriter.writeName("lastSurvey")
      dayCodec.encode(bsonWriter, value.lastSurvey.get, encoderContext)
    }

    if (value.expectedRouteCount.isDefined) {
      bsonWriter.writeName("expectedRouteCount")
      longCodec.encode(bsonWriter, value.expectedRouteCount.get, encoderContext)
    }

    bsonWriter.writeName("facts")
    bsonWriter.writeStartArray()
    value.facts.foreach(v => factCodec.encode(bsonWriter, v, encoderContext))
    bsonWriter.writeEndArray()

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkInfoNodeDetail] = {
    classOf[NetworkInfoNodeDetail]
  }
}
