// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.Relation
import kpn.core.doc.RouteRelation
import kpn.core.tools.next.domain.NextRouteRelation
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NextRouteRelationCodec(registry: CodecRegistry) extends Codec[NextRouteRelation] {

  private val longCodec = registry.get(classOf[Long])
  private val relationCodec = registry.get(classOf[Relation])
  private val routeRelationCodec = registry.get(classOf[RouteRelation])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NextRouteRelation = {
    bsonReader.readStartDocument()

    var _id: Long = 0
    var relation: Relation = null
    var structure: Option[RouteRelation] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "_id") {
        _id = longCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "relation") {
        relation = relationCodec.decode(bsonReader, decoderContext)
      }
      else if (fieldName == "structure") {
        structure = Some(routeRelationCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NextRouteRelationCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NextRouteRelation(
      _id,
      relation,
      structure,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NextRouteRelation, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    bsonWriter.writeName("_id")
    longCodec.encode(bsonWriter, value._id, encoderContext)

    bsonWriter.writeName("relation")
    relationCodec.encode(bsonWriter, value.relation, encoderContext)

    if (value.structure.isDefined) {
      bsonWriter.writeName("structure")
      routeRelationCodec.encode(bsonWriter, value.structure.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NextRouteRelation] = {
    classOf[NextRouteRelation]
  }
}
