// this file is generated by kpn.tools.code.CodecWriter

package kpn.tools.code.codecs.generated

import kpn.api.common.diff.NetworkData
import kpn.api.common.diff.NetworkDataUpdate
import kpn.tools.code.codecs.Codecs
import org.bson.BsonReader
import org.bson.BsonType
import org.bson.BsonWriter
import org.bson.codecs.Codec
import org.bson.codecs.DecoderContext
import org.bson.codecs.EncoderContext
import org.bson.codecs.configuration.CodecRegistry

class NetworkDataUpdateCodec(registry: CodecRegistry) extends Codec[NetworkDataUpdate] {

  private val networkDataCodec = registry.get(classOf[NetworkData])

  override def decode(bsonReader: BsonReader, decoderContext: DecoderContext): NetworkDataUpdate = {
    bsonReader.readStartDocument()

    var before: Option[NetworkData] = None
    var after: Option[NetworkData] = None

    while (bsonReader.readBsonType != BsonType.END_OF_DOCUMENT) {
      val fieldName = bsonReader.readName
      if (fieldName == "before") {
        before = Some(networkDataCodec.decode(bsonReader, decoderContext))
      }
      else if (fieldName == "after") {
        after = Some(networkDataCodec.decode(bsonReader, decoderContext))
      }
      else {
        Codecs.warn(s"Unknown field name: $fieldName in NetworkDataUpdateCodec.decode()")
        bsonReader.skipValue()
      }
    }

    bsonReader.readEndDocument()

    NetworkDataUpdate(
      before,
      after,
    )
  }

  override def encode(bsonWriter: BsonWriter, value: NetworkDataUpdate, encoderContext: EncoderContext): Unit = {
    bsonWriter.writeStartDocument()

    if (value.before.isDefined) {
      bsonWriter.writeName("before")
      networkDataCodec.encode(bsonWriter, value.before.get, encoderContext)
    }

    if (value.after.isDefined) {
      bsonWriter.writeName("after")
      networkDataCodec.encode(bsonWriter, value.after.get, encoderContext)
    }

    bsonWriter.writeEndDocument()
  }

  override def getEncoderClass: Class[NetworkDataUpdate] = {
    classOf[NetworkDataUpdate]
  }
}
